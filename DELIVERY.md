# QQ 群管理与空间限制巡检：配套交付说明

本说明面向公司部署人员、操作人员和维护人员。采用同一代码仓库、同一来源提交、两套操作入口的配套交付方式。当前是**交付候选版，未完成接收方现场验收，不代表正式生产交付已通过**。

## 从这里开始

| 需要做什么 | 阅读文件 |
| --- | --- |
| 安装和使用群管理后台 | [群管理操作手册](docs/delivery/group-management.md) |
| 安装和使用空间巡检 | [空间巡检操作手册](docs/delivery/space-inspector.md) |
| 核对验收、升级、备份、故障与待办 | [验收与维护清单](docs/delivery/acceptance-maintenance.md) |

部署人员负责账号、环境和连接配置；操作人员在已配置电脑上使用；维护人员负责版本、备份和故障处理。不要把开发交接历史或另一台电脑的验收记录当作本公司的已通过记录。

## 两个工具如何配合

| 组成部分 | 输入与职责 | 输出及边界 |
| --- | --- | --- |
| QQ / NapCat | 使用公司指定 QQ 登录，提供群消息及成员目录 | 同机 WebSocket 连接供主服务使用；本机 HTTP 连接供巡检使用 |
| 群管理主服务 | 根据已批准的规则、群配置和动作权限处理消息 | 高置信违规仅自动撤回；保留逐条证据与既有案件，不自动禁言、群内警告或再次违规立案；踢人仍需人工审批 |
| 空间巡检桌面工具 | 从 NapCat 获取成员快照，用独立 Edge 会话查看 QQ 空间 | 保存观察和群关系、生成 CSV；不会导入主服务案件或自动执行处罚 |

推荐将 NapCat、群管理服务和巡检装在公司同一台受控 Windows 专用电脑上。巡检由指定 Windows 用户启动，同一用户数据目录只允许一个巡检窗口。当前不提供员工在不同电脑上远程共享一个 NapCat 的巡检模式，也不提供后台网页直接发起巡检。

日常先由群管理服务处理消息；需要专项排查时，操作人员另开巡检、选群、查看和导出。巡检发现限制提示后交由人工核实；不能因为导出表里有账号就跳过群管理的审批、身份核对和保护规则。关闭巡检不会关闭群管理服务，关闭后台网页也不会结束已注册的主服务。

## 账号关系

- **群目录账号**：项目配置的 `ONEBOT_SELF_ID`，必须与本机 NapCat 登录账号一致，决定巡检可选择哪些群。
- **空间访问账号**：在巡检专用浏览器中本人登录，决定访问各空间时能看到的页面。可以与群目录账号相同或不同；不要求两个账号共同加入所选群。
- 同群不等于好友或有空间访问权限。空间权限页、未开通、维护提示均不能判定为账号违规。明确空间限制也不等于 QQ 账号永久失效。
- 已有任务绑定原来的两个账号；换账号不能冒充同一任务继续观察。人员交接和换机恢复须按维护清单验证。

## 文件包与版本

本仓库继续作为唯一源码和版本来源，无需另建一个巡检仓库。交付文件按固定提交生成，名字含提交标识；以后可将验收通过的同一文件作为现有仓库 GitHub Release 的附件，也可经批准直接交给公司。创建 GitHub 仓库、发布 Release 或改变可见性不是制作候选包的自动步骤。

候选包包含应用源码、数据库迁移源码、锁定依赖、必要安装/维护脚本、公开配置模板、运行规则文件、现有 LICENSE，以及本说明和配套手册。没有附送 QQ/NapCat、Python、uv、Edge、NSSM 的安装程序；本包本身不是免安装 EXE，也不是离线依赖全集。

候选包内 `DELIVERY-MANIFEST.json` 记录来源提交、每个文件的哈希与长度及发布阻塞项。包由 Git 已提交文件按允许清单生成，不使用工作区未提交改动；不收集 `.env`、生产数据库、群成员记录、日志、AppData、浏览器会话、历史评审材料、测试夹具或 `.git` 历史。运行规则 `config/ai_prompt_rules.txt` 是程序需要的公开规则资源，须保留原内容。

维护人员在受控源码仓库中生成候选包：

```powershell
python scripts/build_companion_bundle.py --ref <完整提交SHA> --output <已有输出目录>/<全新文件名>.zip
```

输出文件已存在时拒绝覆盖；命令成功会输出包哈希和来源提交。生成结果不等于安装、现场验收或正式发布成功。当前项目元数据仍标 `Proprietary`，现有 LICENSE 标 MIT；打包保留现状，正式对外发布前须由负责人统一确认，不由工具改写许可。

## 当前正式交付前的阻塞项

- 在接收方干净环境完成安装、登录、检查、保存、导出与重启续扫，并形成现场记录。
- 适配 NapCat-only 的服务安装模式：主应用已承载 OneBot，现有双服务脚本还会无条件启动需要官方机器人凭据的 `app.runtime`，不能作为公司默认的一键安装入口。
- 现有每日备份检查要求 `.git`、干净工作区及可核对的提交。**普通 ZIP 解压目录不能直接使用该备份流程。** 需先适配无 Git 发行包的可核验来源，或由维护人员提供经批准的受控 Git 部署；不能伪造 Git 目录或取消备份检查。
- 主项目每日备份不覆盖巡检 AppData 的任务、缓存、设置和自定义导出目录，需补配套备份与恢复实演。
- 确认许可标注、公司接收范围、维护责任人和正式发布方式。
- 通过实际使用评估空间访问限制与人工介入情况；不承诺全群一次扫完、无限扫描或识别全部失效账号。

更多验收项及记录表见 [验收与维护清单](docs/delivery/acceptance-maintenance.md)。
