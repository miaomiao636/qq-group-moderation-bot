# PR #7：7d1f761 / R01–R11 复验与 R-108 修复交接

审核日期：2026-09-14。受审基线：`7d1f76152ac566bf5356557d567b461186f69c39`，分支 `windows-deploy-2026-09-10`。本报告记录基线问题与其后的本地补丁，不冒充基线本身已经包含修复。

## 结论与权限

**不能按“R01–R11 全部完成、全绿”直接验收或恢复真实撤回。** 删除入口的立即止损有效，但本次独立复验发现仍有下载安全、图文上下文、重试及规则分类缺陷；已在隔离工作树定向修复。最终本地验证见下文，Windows 部署与实测仍须另行完成。

- GitHub PR #7 仍 OPEN / MERGEABLE / UNSTABLE；受审 SHA 未变化。
- 同 SHA 的 [CI run 34835085195](https://github.com/miaomiao636/qq-group-moderation-bot/actions/runs/34835085195) 为 FAILURE。Ubuntu 与 Windows 均为 **1 failed / 899 passed / 3 skipped**；失败是 `test_r01_mixed_fake_and_private_rejected`。两平台的 lint、format、mypy 与干净运行时任务通过，不等于全 CI 通过。
- Windows Agent 回报 `ONEBOT_ACTIONS_ENABLED=false`、service ok/ready；主审未登录那台 Windows、未读取真实配置或独立证明外部处罚调用为零。
- 本轮只做本地修复、合成验证和交接；**未提交或推送远端、未合并、未部署/重启、未恢复动作、未删除生产数据、未重写或强推 Git 历史**。补丁不是部署授权。

## 逐项对账

| 原项 | 基线复验事实 | 本轮处理与剩余边界 |
|---|---|---|
| R01 Fake-IP 出口 | 首次 pin 失败后又查一次 DNS，第二次结果可重新开放兼容出口；混合 Fake-IP/公网分类错误；白名单仍包含整个 qq.com/qq.com.cn。原测试仅模拟第二次分类，受真实 DNS 影响 | 每跳用同次 DNS 分类并固定 IP；混合结果整批拒绝；仅四个既有明确媒体主机允许纯 Fake-IP，Host/SNI/TLS 保留。真实 TUN 兼容待 Windows，失败不得恢复域名 fallback |
| R02/R03 案件硬删除 | 两个阶段的删除风险已被停用入口阻断，不能经旧表单继续删除 | 实际 HTTP 覆盖登录/CSRF/合法请求拒绝，案件与共享违规全列不变；补正式测试。归档功能未实现，不冒称完成 |
| R04/R10 群硬删除 | 路由已拒绝，身份映射及跨 provider 归属未被删除 | 实际 HTTP 验证两 provider、后续精确误判纠错；隐藏仅影响列表，不是停用审核或动作 |
| R05 原子领取/重试 | 两个不同文字同时领取只有一个赢，这部分有效；但源图片重试会整块覆盖 detail，恢复已消费名额；跨账号历史外部 ID 可误复用旧绑定 | SQL 原子保留消费字段，领取只更新绑定字段；流水线使用完整事件键并限制同账号源图，旧外部绑定仅在同账号内兼容；补真实流水线重试、stale ORM、并发及账号隔离测试 |
| R06 时间/顺序 | 已记录 sent_at，但中间消息仍按完成时间筛选；下载/AI/队列中的前驱不可见，后文可能先被撤；带时区时间被当本地钟面值 | 统一 UTC 时刻，任何消息类型均参与紧邻；使用未完成 Inbox 与最小活跃上下文，待审前图转人工、不消费豁免、不自动补罚；同秒/缺时间不猜先后。原内容到期后的时间元数据兼容亦列入回归 |
| R07 办证例外 | 本地 ad 分支放宽，但 AI 可重新把同类广告升级；只按首个黑名单词分严重类别，混合办证/广告/博彩或枪支会错误豁免 | 本地受信策略 marker 贯穿 AI 冲突门；检查全部明确严重词，沿用原分类与阈值。诈骗、色情、暴力及显式动态规则不因“办证”豁免；与旧绝对放行口径的取舍仍需负责人统一 |
| R08 原文备份 | 当前版本已移除并加 ignore；Git 历史及其他副本仍可存在 | 不复制真实样本、不重写历史。所有副本 15 天政策已同步，但受控历史清理、克隆协调及生产副本处置仍未完成 |
| R09 结构校验 | 实际 AI 结果是 category/needs_review/degraded_reason，旧校验读 cat/nr/deg；未消疑视觉结果可被当白名单；中间图片未打断配对 | 按真实生产字段严格校验，完整视觉结果均确认正常、无降级/残留 veto；否定/缺字段/损坏 JSON 不给许可；任何中间消息都参与顺序判断 |
| R11 门禁/文档 | 远程 pytest 实际失败，旧 v8“现行”、record_only“已处理”、永久原件例外与当前决定不一致 | 修正不确定 DNS 测试，新增真实链路回归；文档区分历史回归、实施方回报、本地验证和待现场验收 |

## 修复证据与设计边界

### 安全下载

入口：`app/adapters/qq_official/media.py`；测试：`tests/test_media_dns_contract.py`、`tests/test_media_fakeip_whitelist.py`。

- 新增首批 20 项在基线为 14 失败/6 通过，修复后全过；含先私网后 Fake-IP、混合公网/Fake-IP、重定向非标准端口、非媒体主机、固定 Host/SNI 等。
- 原失败测试单独离线复现：相同旧测试，底层 DNS 固定为公网时失败、Fake-IP 时通过。现统一模拟 OS DNS/HTTP 边界，不替应用函数预设“安全结论”。
- 明确主机为 `multimedia.nt.qq.com.cn`、`gchat.qpic.cn`、`qpic.cn`、`group.e.qq.com`，不是认证所有子域。新增主机须核实后逐个纳入；原始媒体 URL、令牌及真实内容不进入报告。

### 图文顺序与单次消费

入口：`app/moderation/wall_pair.py`、`app/runtime/pipeline.py`、`app/runtime/pairing_context.py`。

- AI 慢/下载慢/尚在队列均有用例。待审前图只触发“配对资格未知，转人工”，不计为确认白名单；后续不会自动补罚该条文字。
- 活跃 worker 的补充上下文不含文字、附件或 URL，结束/异常/取消按唯一 token 清除；重复领取失败不能清掉首个 worker 的上下文。重启与排队恢复仍依赖已有持久 Inbox，未改为内存队列。
- 不提前插入 `ShadowDecision` 占位：曾在本地整合中发现这样会破坏通知 ID 游标，已撤回该方案。新增 3 项真实流水线/通知采集回归，验证正常消息不误通知、首次启用时在途消息完成后不漏计、乱序完成不丢不重。没有修改通知发送器或外发通知。
- 源图领取与后续流水线有多次提交，不能声称“领取与 mark_processed 同一事务”；保证来自条件更新与持久绑定的重试复用。
- 目前完成记录仍需按成员历史查上下文，未证明百群规模下的查询耗时；历史缺时间及同秒无法可靠排序，不能假装有可靠入站序号。扩容前需查询计划/索引及负载测试，不能随意截断最近完成记录来冒充发送顺序。

### 办证与严重类别

入口：`app/moderation/decision.py`、`rules.py`、`ai.py`；测试：`tests/test_certificate_policy_pipeline.py`。

- “本地办证广告记录 → AI 同类高置信 → 重新处罚”的两个文字/视觉回归先失败后通过。
- 8 个既有严重词 × 有/无办证的 16 项组合在旧分类下全失败，修复后通过；不增加敏感词，不调处罚阈值。
- 动态 ad/fraud/porn/violence、两独立视觉 ad/fraud/porn、伪造 marker 均有边界回归。受信 marker 由规则代码生成，不读取群成员或模型自由文本中的同名字符串。
- 这是本地已确认“办证广告”策略的贯穿修复，不等于所有图片办证识别都准确，亦不证明校园墙外观是真实身份认证。

## 本地验证

最终代码于 2026-09-14 全量验证：**1013 passed / 1 skipped / 1 warning，116.38 秒**，exit 0。唯一跳过为 `tests/test_image_engine.py` 的测试机私有真实媒体目录，不能视为真实媒体评测通过；SQLite 路径守卫未因服务运行而跳过。警告为既有 Python 3.12 SQLite datetime adapter 弃用提示。

- `ruff check app tests alembic`：通过。
- `ruff format --check app tests alembic`：198 文件通过。
- `mypy app` 与 `mypy --platform win32 app`：各 85 源文件通过。Windows 目标静态检查不是 Windows 执行测试；新补丁尚未有远程 CI。
- `git diff --check`：通过；原开发工作树仍为 clean。

完整 pytest 是源代码冻结后的整套重跑；此前定向通过不替代该结果。危险删除 HTTP 6 项、通知消费者 3 项等是防回归验证；没有为“先红”故意回改已经正确的生产代码。新补丁仍须在 Windows 和新远程 SHA 上复验。

所有测试使用合成消息、临时 SQLite 与模拟模型/网络边界，不访问真实群、Windows 服务或生产库。未增加依赖或数据库迁移；原开发工作树保持不变。针对安全/顺序/通知消费者进行了分工复核及主审整合重跑。

## 给 Windows Agent 的安排（按依赖，不按盲目赶工）

### 补丁接收

交付文件名：`qqbot-r108-fixes.patch`，对应上述完整基线 SHA。先检查 `git status --short` 与 `git rev-parse HEAD`；保留本机未提交改动，在独立分支/工作树应用。先运行 `git apply --check <补丁路径>`，通过后才运行 `git apply <补丁路径>`。有冲突就停下来逐文件合并，不能用强拉、reset 或覆盖整个目录绕过。应用后用项目锁定环境执行：

```text
ruff check app tests alembic
ruff format --check app tests alembic
mypy app
pytest
```

补丁没有新增迁移、依赖或生产配置；发布前仍核查本机实际迁移状态和配置，不据此跳过常规部署检查。代码回退可用独立提交回退；已发生的 QQ 撤回和文件清理不能靠代码回退恢复。本轮未生成或删除含原文的生产备份。

以下工时仅为规划估算，需按实机数据和负责人可配合时间调整；不是已完成或承诺的 SLA。

| 优先级 / 顺序 | 工作 | 可关闭条件 |
|---|---|---|
| P1 / 第 1 步，预计半天 | 核对本机修改与补丁基线，在独立分支应用补丁；完整 Ruff/format/mypy/pytest，获准推送后核对同 SHA 双平台 CI；部署仍需授权 | 无冲突遗漏，不以强拉覆盖 Windows 改动；新 SHA 三项 CI 通过，实际服务加载版本有证据，动作仍关闭 |
| P1 / 第 2 步，预计半天 | 在真实 TUN/透明代理用合成图片测四个 CDN、重定向与失败；重放先图后文、队列积压、中间图片/文字、重试、同秒、旧格式记录 | 安全下载可用或明确降级；只有真实确认且符合规则的一次配对得到豁免；未知上下文不造成先撤后解释 |
| P1 / 并行准备，预计 1–2 天 | 原内容及所有副本生命周期：来源时间台账、备份/快照/导出到期、dry-run、容量告警、任务漏跑 | 按原消息 15 天而非 mtime/复制日期；旧内容无法从受控备份恢复续期；16–30 天累计/纠错及规则保留可用；清理失败可见 |
| P1 / 规则冻结后 | 当前 v11+补丁的新独立 W2，近重复模板隔离；广告/诈骗/色情/困难正常/例外/失败样本分别看 | 人工真值在看到模型结果前确认，未参与调参样本；误撤与漏放均计入，降级不删分母；缺类别/缺视频明确未测 |
| P1 / 负载实测，至少覆盖工作日峰值 | 百群流量和延迟：每秒/每分钟峰值、图片占比、队列等待、AI/复核/下载分段、E2E p95/p99、每日调用与磁盘 | 不用总平均或轻路径稀释文本延迟；积压可回落、故障有告警、费用可承受。旧 text p95=17.4s/3s 门槛仍未解决，不擅自放宽门槛 |
| P1 / 故障演练，预计半天至一天 | QQ 失败→邮件、应用内 ack、超时备份接管、整机断网/关机、无人登录重启与计划任务恢复 | 真实收件+真人接手+失联/恢复证据；同一台已关机电脑不能作为自己的独立告警来源 |
| P2 / 上述稳定后 | 用归档替代危险删除，补必要查询索引与有限上下文结构 | 不删除共享违规/映射/动作归属，不复用编号，不重弹通知；索引根据查询计划与负载证据选择，保留真实发送顺序 |

重要数据清理、部署、外部通知测试及恢复真实撤回，必须有覆盖具体范围的授权。先在隔离副本确认，不运行未知目录的递归删除。不为本轮整理而重新导出整批原始聊天。

### 必须由负责人决定，不能由 Agent 自行选择

1. **规则优先级**：旧记录的“校园墙外观绝对放行/办证一律正常”与当前“严重类别及显式动态规则不豁免”存在张力。建议普通广告可按例外放宽；即使带墙标或办证词，疑似诈骗/色情/暴力也至少人工复核，不把外观当认证。本轮未擅改全局提示词与既有 v10 绝对放行政策；请明确统一后冻结版本、补正反例再评测。
2. **Git 历史清理**：建议在本轮修复基线冻结、保留不含原文的对账和 SHA 映射后，另行授权清除原文文件历史。执行前盘点各分支/标签、PR 引用及受控克隆，按需联系 GitHub 处理残留引用/缓存。重写不能召回别人已有副本；目前没有执行授权。本地移入 data/ 不等于消除历史，也不延长 15 天期限。

## 何时可以交付

当前只是代码整改与后续验收交接，**尚不建议签收“百群无人值守自动处罚”**。交付应至少满足：新 SHA CI 通过；当前规则独立效果达标；目标峰值延迟与容量经实测；负责人授权的隔离真实动作无误撤、无重复、急停有效；通知/接管/关机恢复有效；15 天副本政策有实证。再按少量群到目标规模逐批观察，不能一次开一百多个群。

建议把交付约定写清“监测哪些类型、哪些例外不自动撤、人工多久接手、故障由谁负责”，而不是承诺所有违规都能自动处理。已有 24 小时影子记录有历史价值，但不能覆盖新代码、新规则、百群流量和未演练故障。

AI 学习仍是人工反馈→候选规则→复测→人工发布/版本回滚，不是模型自动训练。最易长期失效的是规则例外扩大/模板变化、模型或 QQ 接口变化、队列与成本超限，以及告警没人接手。改进优先级是固定规则真值、独立留出集、持续小比例正常消息抽检找漏报、版本回滚和运维接管，不是无条件让每条消息跑两个模型。
