r132 第十批复核整改回评（基线 194eb0b，本轮整改后受审 SHA 见下方最新推送回执）

一、证据
- 您本轮 31 项探针先原样入库（入库时本机复现 20 failed / 11 passed），整改后 31/31 通过；累计入库 reviewer 探针 227/0 failed。
- 仓库全量 1828 用例 / 0 failed / 0 error / 15 skipped；ruff check + format、mypy app（89 文件）通过。
- 生产身份关联（收紧后的工具）：IDENTITY_OK allowed=68 (mismatch 0) rejected=1 (mismatch 0) snapshot_state=ok。
- CI 更正：194eb0b attempt 1 Windows 失败 job 105927237916，attempt 2 成功 job 105929274765；3a0f3de run 35455925261 三 job 全 success（Ubuntu 105931053235 / Windows 105931053266 / clean 105931053132）。超时按您的措辞登记为"疑似 CI 环境瞬时超时、重跑未复现"，不作为已证明根因；3a0f3de 如实写为"测试 + 回评文档"。

二、C01~C05 整改
- C01/P1：authorize_group_routes 在最终复核与首次写入之前显式 BEGIN IMMEDIATE 取写锁，使"复核 → 赋权 → 提交"整体序列化；新探针接受"竞争者被锁阻塞"或"变化被发现后零赋权"两种结果。
- C02/P2：enable_group_actions 同样 BEGIN IMMEDIATE，并**在同一写事务内**复核新建行的 route_ready；预览后路由被撤销 → 不新建、不开启，计入阻塞报告，不再虚报成功。
- C03/P2：补偿改为"有归属"——①仅当该行仍等于本操作写入后的完整状态（id/enabled/note）才动手；②若快照现存决定的 state 与"回滚后应有的生效位"矛盾（如已 approved 而回滚会置 0），判为有更晚决定 → 跳过并打印 COMPENSATION_CONFLICT；③恢复精确的原 note 字符串（不再全局删后缀）；未完全回滚如实报告。
- C04/P2：身份工具改为按 dHash 在该批次里定位当前图（编号可合法变更），再核清单 SHA-256（12/64 位）/dHash → 磁盘字节 → DECISIONS.json（声明批次 == 目录名、结论 == 期望）；approved 来源缺失/不可解析、条目非法、来源批次找不到该图 → 一律不完整并非零；清单与结论编号归一化冲突拒绝；快照 missing 如实报告并声明不覆盖拒绝链。
- C05/P2：窗口参数先解析并规范化到 UTC（支持空格格式与带 T/Z/±HH:MM 的 ISO 8601），再校验起点早于终点；非法/逆序 → WINDOW_INVALID、非零返回且不生成报告；窗口分支措辞改为"窗口内判定 N 条（同一 UTC 半开范围）"。
- 非阻塞诊断（删除重建 ABA）：enable 的 CAS 追加 ifnull(updated_at,'')，直接 SQL 删除后用相同 version 重建的新行不属于旧计划，该诊断现已通过（未引入迁移）。

三、您五个决定的落地
1) injected_loader：已删除生产兼容分支（无结构化快照即拒绝）；旧 group 夹具改为写入合法快照（测试输入准备，业务断言逐字未改，已在文件头登记）。
2) 补偿：本轮按"只看并发/失败结果"的口径实现并通过全部反例；若您要求改为"决定/来源/操作版本/生效位同库事务权威保存、JSON 仅导出"，我可在下一批重构（需新增表/迁移）。
3) 跨 provider 已启用 settings 视为冲突：保留该保守门禁，并补上复核与写入的原子性（C01）。
4) UTF-8 适配：接受；AST 口径按您更正为 22 文件 / 18 一致 / 4 处已解释差异。
5) 超时：接受并按"疑似 CI 环境瞬时超时、重跑未复现"登记。

四、仍未证明
- A09 前 6 秒、历史授权快照（NOT_PROVEN）、导出多查询一致性读事务、Windows 回滚全链实机演练、一次未复现失败、enforce 最终批准与阈值校准、B 的实际动作效果。审计 stage_runtime_proven=false 只是如实标注，不构成"服务已加载 recall_only"的证据。

五、请重点复核
1) C03 的实现形态（归属 + 状态一致性补偿 vs 同库事务权威保存），是否需要我下一批按后者重构；
2) C04 的定位策略（按 dHash 定位当前图，编号可合法变更；同批次编号歧义重复已强制失败）；
3) C05 我选择解析并规范化两种写法而非只收空格格式，是否需要改为严格拒绝 ISO 写法；
4) 夹具变更登记：test_r132_review_group_enable_boundaries.py 的 setup 改为注入合法快照（断言未改），请计入"已解释差异"。

六、边界
- 未启用 enforce、未执行回滚、未改判定逻辑、未扩大授权到其它 provider、未做新的生产赋权；生产仍为 67 群可路由 / recall_only 只撤回 / IMAGE_HASH_MODE=shadow。
