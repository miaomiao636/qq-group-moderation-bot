# r132 第十二批送审材料

日期：2026-09-20。**基线**：`194eb0b`（您第十批复核的受审版本）。本批 = 您 C01～C05 整改 + 您对我五个问题的决定落地；**未夹带新功能、未做新的生产变更**。

---

## 一、证据塔

| 检查 | 结果 |
| --- | --- |
| **您本轮 31 项探针** | 【更正 2026-09-20】~~先原样入库~~ → **并未入库**：当时只在**外部目录**跑过（31 passed），`194eb0b..6505a79` 无任何新探针文件。**现已于 `2f69fdf` 真正入库**（见 `docs/2026-09-20-r132-round13-review-submission.md`） |
| 全部入库 reviewer 探针 | 【更正 2026-09-20】~~227 passed（196 + 31）~~ → **该口径不成立**：仓库 reviewer 当时实为 **196**，同 SHA CI **不覆盖**外部的 31 项。当前口径：仓库 **250**（196 + 31 + 23） |
| 仓库全量（本机） | **1828 用例 / 0 failed / 0 error / 15 skipped** |
| `ruff check` + `format`、`mypy app`（89 文件） | 全绿 |
| 生产身份关联（**收紧后**的工具） | `IDENTITY_OK allowed=68 (mismatch 0) rejected=1 (mismatch 0) snapshot_state=ok` |
| 收尾 SHA CI | `3a0f3de` [run 35455925261](https://github.com/miaomiao636/qq-group-moderation-bot/actions/runs/35455925261)：Ubuntu `105931053235` / Windows `105931053266` / clean `105931053132` 全 success（该提交为**测试超时修改 + 回评文档**，已按您更正登记） |
| **本批整改 SHA CI** | **`6505a79`** [run 35457796682](https://github.com/miaomiao636/qq-group-moderation-bot/actions/runs/35457796682)：`ubuntu-latest` `jobId=105936065773`、`windows-latest` `jobId=105936065788`、`clean runtime-deps` `jobId=105936065651` —— **三 job 首跑全部 success**（本轮无超时复现） |

> CI 更正：`194eb0b` attempt 1 的 Windows 失败 job 是 `105927237916`，attempt 2 的成功 job 是 **`105929274765`**；不再沿用误写。超时按您的措辞登记为"**疑似 CI 环境瞬时超时、重跑未复现**"，不作为已证明的根因。

---

## 二、C01～C05 整改

| 编号 | 整改 |
| --- | --- |
| **C01 / P1**（最终复核未在写事务内） | `authorize_group_routes` 在复核与首次写入之前显式 **`BEGIN IMMEDIATE`** 取写锁，使"复核 → 赋权 → 提交"整体序列化。新探针接受"竞争者被锁阻塞"或"变化被发现后零赋权"，两种路径均可 |
| **C02 / P2**（新建行沿用过期路由资格） | `enable_group_actions` 同法加 `BEGIN IMMEDIATE`；**新建行在同一写事务内复核 `route_ready`**，预览后被撤销 → **不新建、不开启**（跳过并计入阻塞报告，不再虚报成功） |
| **C03 / P2**（补偿覆盖后来的成功决定） | 补偿改为**有归属**：①只在该行**仍等于本操作写入后的完整状态**（id/enabled/note）时动手；②若快照现存决定的 `state` 与"回滚后应有的生效位"**矛盾**（例如已记 approved 而回滚会置 0）→ 判为有更晚决定，**跳过并报 `COMPENSATION_CONFLICT`**；③恢复**精确的原 note 字符串**（不再全局删后缀）；未完全回滚如实报告，不假称已回滚 |
| **C04 / P2**（只核旧编号） | 身份工具改为**按 dHash 在该批次里定位当前图**（编号可合法变更 → `test_valid_reapproval_can_move_to_different_manifest_number` 通过），再走完整链条：清单 SHA-256（12/64 位）/dHash → 磁盘字节 → `DECISIONS.json`（声明批次 == 目录名、结论 == 期望）；`approved` 来源缺失/不可解析、条目非法、来源批次找不到该图 → 一律**不完整**（非零）；清单与结论的编号**归一化冲突拒绝**；快照 `missing` 如实报告并声明"不覆盖拒绝链" |
| **C05 / P2**（时间参数未解析） | 窗口参数**先解析并规范化到 UTC**（支持 `YYYY-MM-DD HH:MM:SS` 与带 `T`/`Z`/`±HH:MM` 的 ISO 8601），再校验**起点早于终点**；非法/逆序 → `WINDOW_INVALID`、**非零返回且不生成报告**；窗口分支的报告措辞改为"窗口内判定 N 条（同一 UTC 半开范围）" |

### 您五个决定的落地

1. **injected_loader**：**已删除生产兼容分支**——无结构化快照即拒绝；旧 `test_r132_review_group_enable_boundaries.py` 的 `setup` 夹具改为**写入合法快照**（测试输入准备，业务断言逐字未改，已在文件头登记）。
2. **补偿**：按上面 C03 收敛为"操作归属 + 决定状态一致性"判定；若您仍希望改为"决定/来源/操作版本/生效位同库事务权威保存、JSON 仅导出"，我可以按该形态再改一版（本轮先按您"只看并发/失败结果"的口径实现并通过全部反例）。
3. **跨 provider 已启用 settings 视为冲突**：保留该保守门禁，本轮补上复核与写入的原子性（C01）。
4. **UTF-8 适配**：接受；**AST 口径按您更正为 22 文件 / 18 一致 / 4 处已解释差异**（mode 输入隔离、scope 契约、群探针 Windows 路径、窗口探针读取编码）。
5. **超时**：接受，已按"疑似 CI 环境瞬时超时、重跑未复现"登记；`3a0f3de` 如实写为"测试 + 回评文档"。

### 删除重建 ABA（您列为非阻塞诊断）

`enable_group_actions` 的 CAS 追加 `ifnull(updated_at,'')` 条件：直接 SQL 删除后用相同 version 重建的新行不属于旧计划 —— 该诊断现已通过（未引入迁移）。

---

## 三、仍未证明（不变）

A09 前 6 秒归属、历史授权快照（`NOT_PROVEN`）、导出多查询一致性读事务、**Windows 回滚全链实机演练**、一次未复现失败、enforce 最终批准与阈值校准、B 的实际动作效果。审计 `stage_runtime_proven=false` 仍只是如实标注。

## 四、请重点复核

1. **C03 的实现形态**：本轮用"归属 + 状态一致性"的补偿（通过全部 4 个反例 + 旧并发正控）；是否要求改为同库事务权威保存决定（我可在下一批按该形态重构，需新增表/迁移）；
2. **C04 的定位策略**：按 dHash 定位当前图（编号可变更）——若您希望同时要求"编号在同一批次内不得歧义重复"，我已强制（清单归一化冲突即失败）；
3. **C05 的输入语法**：我选择"解析并规范化两种写法"，而非严格只收空格格式；若您偏好严格拒绝 ISO 写法，请示下；
4. **夹具变更登记**：`test_r132_review_group_enable_boundaries.py` 的 `setup` 已改为注入合法快照（断言未改）——请在您的 AST 核对中把它计入"已解释差异"。

## 五、边界

本批未启用 enforce、未执行回滚、未改判定逻辑、未扩大授权到其它 provider、未做新的生产赋权；生产仍为「67 群可路由 / `recall_only` 只撤回 / `IMAGE_HASH_MODE=shadow`」。
