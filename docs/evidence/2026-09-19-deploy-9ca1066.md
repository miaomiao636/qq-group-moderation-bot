# 部署证据：`9ca1066`（2026-09-19 01:01，负责人授权）

## 1. 部署对象

| 项 | 值 |
| --- | --- |
| 代码 SHA | `9ca1066`（r132 六轮整改后的 head；工作区 0 未提交改动） |
| 部署前运行版本 | 2026-09-18 17:18 加载的旧版（`t204-v15` + 旧 `wall_pair`/`allowlist`/`routes`） |
| 提示词版本 | `AI_PROMPT_VERSION=t204-v16` |
| `config/ai_prompt_rules.txt` SHA256（前 16） | `21984EFF4F1A1AFF` |
| `app/moderation/ai.py` SHA256（前 16） | `5F95E4EEB4C7F53A` |
| 代码改动时间 | `ai.py` 09-18 19:42、`wall_pair.py` 21:54、`allowlist.py` 22:36 |
| 生效时间 | **2026-09-19 01:01**（两服务重启完成） |

**生效判据**：`QQBotWeb` / `QQBotRuntime` 新进程 PID `42060` / `53540`（01:01:22）与 `53668` / `50684`（01:01:28），
**启动时间晚于**上述全部代码与提示词改动 → 本次整改（含 R09 完整附件来源资格、小程序码放行、
口径 C 窗口豁免、名单同步保护、失败计划终态）**已随本次重启加载**。

## 2. 部署后核对

- `healthz`：`status=ok`、`env=prod`、`mode=SAFE`；OneBot `state=ready` / `connected=true` /
  `login_state=online` / `connect_count=1`；心跳年龄 7.3s（阈值 90s）；`queue_backlog=0`；
  `processed_total=0`（重启后尚无新消息）。
- 启动日志：Web `Uvicorn` 正常起、`GET /healthz 200`；Runtime `[runner] connected, shadow mode (record only)`；
  无启动错误（`service_QQBotRuntime.err.log` 未见异常）。
- **急停**：`runtime_emergency_stop=false`（**已解除**，真实动作按配置生效）。
- **群动作**：**13 / 16** 个群 `action_enabled=1`（另 3 个群只审核不动手）；阶段仍为 `recall_only`（只撤回，不禁言）。
- 判定累计分布：`allow 3529 / record_only 7735 / violation_high 3219`。

## 3. 真实使用测试期间的观察与"一键停手"

| 场景 | 手段 | 生效方式 |
| --- | --- | --- |
| 发现误撤/异常 | 后台**急停** | 运行期立即，无需重启 |
| 某群不想动手 | 群管理里关该群动作开关 | 运行期立即 |
| 白名单成员被误伤（**注意方向**） | **正确做法：先急停或关闭该群动作**，再核对 provider / 身份 / 白名单启用状态并恢复正确保护 | 立即（运行期）；**不要用"停用白名单成员"来止损**——那会撤销放行保护，下一条反而更容易被处罚（主审 A10） |

观察重点（本轮改动行为）：① 带小程序码的图是否放行（色情/暴力除外）；② 图后 2 分钟内该成员的
文字与图片是否只记录不撤回（色情/暴力/刷屏除外）；③ 合并转发、群名片是否照旧一律撤回；
④ 名单同步若提示"名单在确认期间被其它操作改动"，属并发保护生效，**重新预览**即可。

## 4. 仍待补（未完成，不得以本记录替代）

- **Windows 隔离环境回滚全链演练**（成功 / 版本错配 / 单服务启动失败三类证据）——需维护窗口，不动生产。
- **固定 UTC 半开窗口 `[start, end)` 统计**：绑定本部署 SHA、账号、授权群集合、导出时刻、去重键、
  状态分母与越界计数；`code 1200` 只支持"调用超时"，不等于客户端未撤回。
- 主审 r132 六轮结论为"代码审查通过"，本记录为**部署与生效证据**，二者不互相替代。
