# 现场验证合集（2026-09-15）：通知链路 / 清理与恢复演练 / 抽样启动

执行背景：负责人授权"所有都进行"（费用台账除外）。本文件汇总当日完成的现场验证与遗留边界。所有操作限本机；未恢复真实动作；未合并前不进行生产删除。

## 1. 通知链路实测（QQ + 邮件 + 确认接手）——全部通过

**送达（真实运行记录，非模拟）**：

- QQ 管理群（`1043951076`）：摘要通知每 2 小时稳定送达，最近 `SENT` = #205（2026-09-15 06:45）、#204（04:44）、#203（02:44）、#202（00:44）、#201（09-14 22:44）……
- 邮件（`<OWNER_EMAIL>`）：#200 / #199 于 09-14 21:08 / 21:02 `SENT`
- 结论：QQ 与邮件双通道**真实送达已验证**（`notification_deliveries` 表 `SENT` 记录为证）。

**确认接手（ack）——首次实测通过**：

- 通过后台真实操作：对通知 #191 点击"确认接手（不执行处罚）"。
- 结果：`acknowledged_at` 写入、`acknowledged_by=human:admin:…`（真实管理员身份）、审计 `notification_ack` 记录、页面按钮变为"接手时间"、不再显示接手按钮。
- 语义确认：接手仅登记处理责任，不执行任何处罚。

**升级机制核查**：升级仅对 `severity='page'`（严重告警）生效；摘要类（`ticket`）不升级——现行通知全部为摘要，因此无升级记录属**正确行为**，非缺陷。

**未完成边界**：

- 外部心跳（整机失联告警）：`NOTIFICATION_HEARTBEAT_*` 未配置 → 未启用；需要负责人提供外部监测服务（如 Healthchecks.io）的 URL 与专用 token 后启用并实测。
- 故障演练（断网/关机/崩溃后恢复）：需要停机窗口，待负责人安排。

## 2. 保留期清理与恢复演练（隔离副本，零生产写入）

工具：`scripts/purge_drill.py`（可复现；SQLite backup 快照到 `data/purge_drill/`，`MEDIA_DIR` 替换为隔离目录）。

**清理验证（注入超期/未到期对照数据后运行 `purge_expired`）**：

| 检查项 | 结果 | 判定 |
| --- | --- | --- |
| 超期违规快照（45 天） | `{"purged": true, "reason": "raw_retention_expired"}` | ✅ 原文清除 |
| 未到期违规快照 | `{"text": "未到期原文-应保留"}` | ✅ 保留 |
| 超期 processed_events（60 天） | 删除（剩余 0） | ✅ |
| 未到期 processed_events | 保留 | ✅ |
| 超期 action_logs（200 天） | 删除 | ✅ |
| 超期影子原文（45 天） | `text_preview` 清空、结构保留 | ✅ |
| 超期反馈原文（45 天） | 清空（保留标签行） | ✅ |
| 隔离媒体：20 天前文件 | 删除 | ✅ |
| 隔离媒体：今天文件 | 保留 | ✅ |

另外顺带清理 `ai_cache_deleted=602`（缓存过期），案件/候选/通知/inbox 无超期项（0）。

**R-112 N05 重新取证（2026-09-15 深夜）**：校验改为**按注入返回的精确 rowid 判定**并显式检查全部预期（`_evaluate`，任何失败非零退出）——此前 `action_logs` 用固定日期窗口 `< '2026-01-01'`，注入"200 天前"（=2026-02-27）时故意不清理仍报 0（假通过，主审复现 + 本机内存复现）。重跑结果：**`passed=true`、failures=[]`**（超期项 rowid 全部已清、未到期对照保留；`ai_cache_deleted=1096`）；报告见 `data/purge_drill/report.json`。

**备份可读性检查（真实备份 `backups/w0-20260910-175738/moderation.db`）**：

- 可打开、23 张表、`alembic_version=e1a4b8c2d3f5`（09-10 时点版本）、`shadow_decisions=2004` 行——**备份可读性检查通过；完整恢复演练未完成**（R-112 N06 更正：未做 integrity_check、未升级迁移到当前 head 并启动当前应用验证；排入下个维护窗口的隔离恢复演练，与整机故障演练区分）。
- 说明：该备份为 09-10 时点（尚无违规记录，`violation_records=0` 属预期）。

## 3. 旧目录盘点（只读，不删除）

| 路径 | 文件数 | 大小 | 归属与到期评估 |
| --- | --- | --- | --- |
| `data/media_snapshot_20260911` | 747 | **291.7 MB** | 09-10 手工快照（含 `_frames` 视频帧）；**不在自动清理范围**；按源时间 15 天窗口 ≈**09-20~09-25 到期**，到期后需人工核实清理 |
| `data/t002_media` | 13 | 1.28 MB | T-002 时代原始违规图（09-05，到期 ≈09-20） |
| `data/_dbg_tmp` | 1 | ~0 MB | 调试残留（09-14），可随时清理 |
| `data/backups/w0-20260910-175738` | 2 | 4.54 MB | W0 备份（建议保留） |
| `data/backup_shadow_1090875633.json` | 1 | 0.55 MB | 影子数据手工导出（09-11；到期 ≈09-26） |

- 自动清理任务 `QQBotAutoCleanup`：每天 04:00 运行，最近一次（09-15 04:00）**Last Result 0（成功）**；生产库最老数据 09-08（未超 15 天）→ 当前生产无超期数据可清，属正常。

## 4. 线上抽样复测（新 W2 替代）正式启动

工具：`scripts/sample_draw.py`（分层抽样、只读生产库、输出本地样本池不进 git）。

- 首次抽样：`data/sample_pool/sample_20260915_1532.jsonl`，**150 条**（9/15 以来未参与调参数据，候选池 799 条）。
- 分布：text 91 / image 46 / mixed 5 / share_card 4 / unknown 2 / video 1 / forward_record 1；系统判定：violation_high 31 / allow 56 / record_only 63。
- 下一步：人工在 JSONL 上标注 `label` 与 `truth_category` 后，用 `app.reports.evaluation` 复测（得到"未参与调参样本"的正式指标）。

## 5. 边界声明

- 本文件所有验证均为本机操作：清理/恢复在隔离副本、抽样只读、通知为真实发送（负责人授权范围内，QQ 群与邮箱均在负责人名下）。
- 未恢复真实动作（`ONEBOT_ACTIONS_ENABLED=false`）；未进行生产删除；W3–W5 仍需另行授权。
