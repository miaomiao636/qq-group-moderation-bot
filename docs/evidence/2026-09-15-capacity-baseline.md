# 百群容量与延迟·现状基线（第①步）

日期：2026-09-15。范围：只读分析生产库（`data/moderation.db`、`ai_usage_logs`、`shadow_decisions`），未做任何写入或配置变更。目的：为"百群容量与延迟"门槛建立基线，定位历史 17.4s 问题。

## 1. 规模与吞吐现状

- 活跃群（7 天）：**13 个真实群**（主力：544140282 ~1684 条/7d、869142826 ~1091、6638171 ~1082）
- 日均消息：331–2082 条/天（近 7 天均值约 800）
- 峰值时段：**~313 条/小时**（≈5 条/分钟，2026-09-15 01 时）
- 队列积压：当前 **0**（healthz `queue_backlog=0`）

## 2. AI 调用延迟（关键）

**近 24 小时（当前真实水平）**：

| 模型 | 调用数 | p50 | p95 |
| --- | --- | --- | --- |
| deepseek-flash（主力，文本+视觉） | 1648 | **1.6 s** | **5.3 s** |
| qwen3.8-flash（少量路径） | 49 | 9.1 s | 20.9 s |

**近 7 天（含历史包袱）**：合计 p50 2.0s / p90 26.2s / p95 32.8s——被 **`mimo-v2.5`（p50 27.8s、p95 42.7s）** 拉高；**mimo 自 9/10 后已停用**（最后调用 2026-09-10），9/11 起不再参与。

**结论**：历史"文字 p95 17.4s"的根因基本可定位为旧模型 mimo 的响应慢（28s 级）；当前主力模型下 AI 调用 p95 已降至 5.3s。系统的调用效率高：平均每条消息仅 **1.16 次** AI 调用（多数 1 次），缓存键命中大量重复请求。

## 3. 可靠性

- 7 天成功率：**97.1%**（8005/8241）
- 失败分布：provider_http_error 134、**ai_rate_or_budget_limited 79**（服务商限流/预算触发，扩容需关注）、provider_timeout 9、其他 14

## 4. 费用与存储

- `cost_cents` 台账全部为 0（当前渠道无计费或未接入计价字段）——**费用估算缺少计价依据**，压测阶段需按实际渠道价格补
- 存储：`shadow_decisions` 累计 6015 行、`violation_records` 172 行（SQLite 单文件，体量小）

## 5. 端到端延迟（初步，样本语义待校验）

`detail.sent_at → shadow.created_at` 抽样 56 条：p50 6.3s / p80 15.9s / p90 38.5s / p95 292s。
逐条核查：**分钟级慢样本（172s–712s）集中在 2026-09-14 19:26–21:07 一个时段（5 条，均为 text/record_only）**，其余样本正常（含视觉二审的图片 29–38s）。
注意：`sent_at` 的时区语义未在本轮校验（多数记录的差值呈 8 小时偏移特征），**严格端到端 p95 须用 `scripts/w2_e2e_latency_report.py` 工具复算**；本节仅供线索（9/14 晚存在一次疑似积压/服务商慢事件）。

## 6. 百群外推与风险点

- 按当前主力群量外推：100 群 × ~160 条/天 ≈ **1.6–2 万条/天**（平均 0.2–0.23 条/秒，峰值 10× ≈ 2 条/秒）
- 当前系统处理 ~5 条/分钟无积压 → **平均负载有富余**；主要风险在**峰值/突发**（worker 并发 3、队列上限、AI 服务商限流已见 79 次）
- 第②步（优化建议）与第③步（隔离实例合成压测：模拟 100 群 2 万条/天、峰值 10×、假模型响应，观测积压/延迟/资源/DB 增长）待执行
