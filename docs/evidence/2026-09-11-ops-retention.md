# 运维变更记录：保留期 15/15 + 每日自动清理（2026-09-11）

批准：负责人（2026-09-11 决定"保留期设 15/15 和定时清理"）。执行：Windows Agent。

## 变更内容

| 项 | 旧 | 新 |
|---|---|---|
| 原始消息/媒体保留期 | 30 天（默认） | **15 天**（`RAW_RETENTION_DAYS=15`） |
| 判定/动作记录保留期 | 180 天（默认） | **15 天**（`DECISION_RETENTION_DAYS=15`） |
| 自动清理许可 | 未允许 | **已允许**（`auto_cleanup_enabled=1`） |
| 计划任务 | 无 | **QQBotAutoCleanup**（每日 04:00） |

## 安全性核验（执行前）

- "同群同人 30 天内第二次违规加重"依赖 `violation_records`（记录本体不清空，仅超期清"消息快照"内容）。主审用合成数据确认：20 天前首违经 15/15 清理后，次违仍累计为 2，精确映射的误判反馈仍可撤销首违。
- **案件记录可以关联，不等于两次原始证据仍完整**：首违超过 15 天时快照和媒体可能已清，案件虽含两个记录 ID，人工只能看到第二次原内容。负责人已在 2026-09-12 明确选择“全部原始内容仍只保留 15 天，接受首违内容缺失”（D-024）；不新增违规证据 30 天豁免。
- ddf1f73 的清理遗漏 `ShadowDecision.detail_json` 等原文副本；R-107 依 D-024 补影子原文、反馈原文、违规证据引用、无近期有效来源支持的未确认自动候选原句及过期 AI 缓存清理，保留列级身份/判定与必要元数据。已人工复制/发布的规则保持。15/15 仍非“所有记录 15 天后消失”，也不能从原生产计数全 0 推断完整生命周期已验收。新清理尚须 Windows 在隔离副本核验后更新，当前生产不会由主审操作。
- 清理范围：事件去重记录、违规消息快照、动作日志、媒体文件、inbox、通知——均按各自保留期。

## 执行与验证证据

1. **媒体快照备份**：`data/media_snapshot_20260911/`（747 文件）——防 15 天后样本图被清（主审复核仍可用）。
2. **配置生效**：`Settings` 读取确认 `raw=15 / decision=15`；服务已重启恢复 `ready`。
3. **计划任务注册**：`Register-ScheduledTask`（脚本 `scripts/register_cleanup_task.ps1`，可复用于换机安装），
   状态 `Ready`。
4. **任务端到端验证**：手动触发一次 → `last_cleanup_status=succeeded`（2026-09-11T16:00:15Z），
   计数全 0（当前无超 15 天数据，符合预期）。

## 备注

- 计划任务以注册用户身份运行（登录状态下执行）；换机安装时运行同脚本即可。
- 备份通道：用户本次未启用定时备份（保留现有手动「创建一致性数据库备份」按钮）。
- 2026-09-12 主审已修复媒体删除失败被吞掉、计划清理仍记 succeeded 的实现问题：真实权限/I/O 失败现在进入 failed，CLI 非零退出；并发已删除文件仍可忽略。先前已删除的媒体不会随数据库回滚恢复。仅在临时数据上验证，未在生产执行清理。
- 当前每日 04:00 任务依赖注册用户登录，且没有错过运行补跑的配置；不能作为无人登录/重启后的定时执行保证。任务未运行与清理失败均需独立监测，W4 再核验运行身份、漏跑与恢复。本轮未改任务权限或注册状态。
- `media_snapshot_20260911` 不在现有媒体清理路径内，须依 D-024 的 15 天原始内容期限由 Windows 盘点并受控处理；它不是自动过期或异机灾备。手工导出、W2 标注及备份也不得无限延长原内容保留。
- R-107 的视频抽帧改为每条消息独立临时目录，正常完成和异常返回均清理。历史 `_frames/frame_*.png` 以及进程强制终止留下的 `video-*` 目录仍须 Windows 确认归属/年龄后按 15 天政策盘点；本轮未对生产目录执行递归删除。旧共享帧不再被后续消息读取。

## D-024 副本期限落实（2026-09-12 负责人批准）

| 副本 | 决定 |
|---|---|
| data/media_snapshot_20260911（747 文件） | 保留至 **2026-09-26**（15 天期满）后删除——用途（W2 复核）已完成，到期由负责人/Agent 执行并记录 |
| data/backups（数据库备份 2 份） | 轮换保留最近 2 份，生成新备份时删除更旧 |
| data/w2_*.jsonl（38 个手工标注/导出） | 保留至 **W2 新样本复测完成后**清理 |
| data/media/_frames（16 个历史抽帧） | **已删除**（2026-09-12） |
| data/_dbg*（调试残留 8 个） | **已删除**（2026-09-12） |
| data/t303*、drill*（历史证据原件 8 个） | **永久保留**（主审 P1-13 核验对象，不属清理范围） |

已删项合计：抽帧 16 文件 + 调试残留 8 个；证据原件核验完好（8/8）。

## 群级授权生效（2026-09-12）

负责人确认 5 群开启真实动作（recall_only）：`17598122` `470794920` `6638171` `869142826` `894723164`
（provider=onebot，`provider_group_settings.action_enabled=1` 已核验；`1090875633` 保持不参与）。
授权生效时 `action_intents=0`；首笔动作发生后按记录核验执行链路（NapCat 撤回结果 + 审计表）。
