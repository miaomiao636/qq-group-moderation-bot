# 通知通道验收证据（2026-09-11）

口径：答复主审"通知缺送达证据"，区分**已验证 / 未验证 / 待真人确认**，不把实施方自述写成已验收。

## 通道架构

| 通道 | 角色 | 配置 |
|---|---|---|
| QQ 群（群 1043951076） | primary | `NOTIFICATION_QQ_ENABLED=true` |
| 邮件（smtp.qq.com:465/ssl） | **QQ 失败时的 fallback 升级通道**（`qq_fallback:{notice_id}` → email） | `NOTIFICATION_EMAIL_ENABLED=true`，收发件人同 `3573002001@qq.com` |

## 已验证

### QQ 群通道（真实运营通知）

`notification_deliveries` 实测记录（2026-09-11）：

| 项 | 值 |
|---|---|
| 通知数 | **8 条**（`review_summary`「疑难消息待人工复核摘要」，12:36–14:19 UTC+8） |
| 投递状态 | **全部 SENT**，各 1 次尝试，无 FAILED/UNKNOWN |
| 内容设计 | 单位化摘要，**不含群内容**（隐私边界）；含 ack 机制（`acknowledged_at`） |
| **真人确认** | ✅ **负责人 2026-09-11 确认收到**（"看到了"）——QQ 群通道验收闭环 |

### 邮件通道（SMTP 端到端）

- 2026-09-11 使用生产同款 `EmailSender` 代码路径发送验证邮件：`status=SENT`（TLS 握手+认证+SMTP 接受）。
- **负责人收件确认**：负责人截图确认收到验证邮件（含重复发送的 2 封，原因已说明=测试脚本重跑）。

## 未验证（NOT_TESTED）

1. **QQ 失败 → 邮件 fallback 实战**：机制存在且已配置，但本窗口 QQ 通道零失败，未经实战触发。
2. **备份收件人（backup audience）**：`NOTIFICATION_EMAIL_BACKUP_TO` 未配置。
3. **整机失联/恢复演练**：未做（W3 范围）。
4. **真人确认（本轮）**：8 条真实运营通知是否被负责人看到/确认——待负责人答复。

## 结论

- QQ 群通道：**实施方侧已验证投递成功**（8/8 SENT），真人确认待补。
- 邮件通道：**实施方侧已验证 + 负责人收件确认**（测试邮件）。
- fallback 升级路径：**NOT_TESTED**（建议 W3 演练时覆盖）。
