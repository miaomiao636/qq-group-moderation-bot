# 部署生效证据：d388ab5（R-111 两轮 P2 修复）

日期：2026-09-15。授权：负责人明确授权部署 `d388ab5`，并批准用生产后台的一条真实影子记录走反馈纠正闭环实测（写反馈表、可审计、无处罚）。

## 1. 部署与运行证据（Windows 本机）

- 部署对象：`d388ab5`（工作树 HEAD `8ae6fdb` = d388ab5 代码 + 仅 6 个文档文件差异，应用/测试/配置/依赖/迁移与 d388ab5 一致）
- 服务重启：QQBotWeb / QQBotRuntime 重启后 `GET /healthz` → `status=ok / mode=SAFE / onebot=ready / connected=true / queue_backlog=0`
- 规则版本：`AI_PROMPT_VERSION=t204-v13`；`config/ai_prompt_rules.txt` SHA256 = `8608ABC2E7BD0EC5E533AE45B6F36B7FCBF02A30C9D51F3678DD2A9E310EE5D6`
- 迁移版本：`alembic current` = `af66bc20f1ed (head)`
- 动作开关：`ONEBOT_ACTIONS_ENABLED=false`（全局总开关，关闭）、`EMERGENCY_STOP=false`
  - 群级「真实动作」开关有 5 个历史授权群（D-025 试点遗留配置）；运行证据：最近 `action_intents` 全部 `SKIPPED`——无真实动作执行
- **生效版本行为证据**：下方实测流程只有在修复后代码上才能通过（旧代码列表/详情会回显最早值且只改原因会反转真值）

## 2. 实测 a：生产后台真实记录走反馈纠正闭环（全部通过）

目标记录：`onebot:<OLD_BOT_QQ>:887445705`（群 `868995385`，`record_only`，AI 疑似 porn 低置信 → 保留类别转人工，属 B-2 行为产物）

| 步骤 | 结果 |
| --- | --- |
| 登录后台 → 选中该记录（系统类别回显 `porn`） | 通过 |
| 第一次纠正（→ `fraud`） | 成功 |
| 第二次纠正（→ `porn`） | 成功；历史 `['fraud','porn']` |
| 列表回显 | 最新 `porn` ✓（修复前：最早 `fraud`） |
| 详情回显 | 最新 `porn` ✓ |
| 只改原因再次提交（按页面值） | 历史 `['fraud','porn','porn']`——**最新真值未被回退** ✓ |
| 审计 | `admin_audits` +3（`feedback_record`） |
| 副作用 | `rule_candidates` 281→281、`rule_versions` 156→156、`action_intents` 887→887、`violation_records` 172→172——**无自动规则发布、无动作、无处罚** |

说明：该记录新增的 3 条人工反馈与 3 条审计为本次验证写入（理由含"部署验证R-111"字样，可审计追溯）；反馈为追加模型，历史保留。

## 3. 边界

- 未合并 main；未恢复真实动作；未做生产删除或 Git 历史重写。
- 本证据限本机部署验证，不替代主审独立复验，也不关闭既有现场门槛（15 天清理/恢复、真实 TUN 下载、独立新 W2、百群容量与延迟、通知/整机故障接管、W3–W5）。
