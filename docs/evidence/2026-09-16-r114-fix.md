# R-114 主审复验整改证据（2026-09-16）

主审报告：`qqbot-review-42d2e89.md`（对 `d4a9d00` / main `42d2e89`）。本记录为整改与复验证据。

## 1. 主审结论核实（Windows 侧独立复现——三项全部属实）

| 项 | 核实方式 | 结果 |
| --- | --- | --- |
| **F01（P1）** 完整清理入口先走未加固的 `purge_media`（`cleanup.py:713` → `media.py:435`） | **实跑主审探针**（`mklink /J` 真建 junction + 内存 SQLite + `purge_expired`） | **复现失败**：未登记原件被真删（`media_files_deleted=1`）——属实 |
| **F02（P2）** 五类异常输入被当"正常" | 代码静态核实 + 临时 SQLite 反例 | `sample_draw.py` 仅认 `degraded_reason`；`sample_to_eval.py` 用 `or ""` 洗值——属实 |
| **F03（P2）** 固定 `.tmp` 名可覆盖输入/他人文件；exists 检查后竞争窗口 | 代码静态核实 + 文件行为反例 | `sample_to_eval.py:117` 固定名 + 无条件 `os.replace`——属实 |

## 2. 修复（保留原 F 编号）

| 项 | 修复 |
| --- | --- |
| **F01** | 新建 `app/core/fs_guard.py`（`is_link_like` / `chain_has_link`，从 cleanup 抽出共享）；`purge_media` **fail-closed**：媒体根/祖先为链接或重解析点 → 整体拒绝（返回 0 不删任何文件）；每个候选文件扫描后与 `unlink` 前按完整祖先链复核（TOCTOU）；`cleanup.py` 委托共享实现（私有包装保持既有引用/回归不动） |
| **F02** | `sample_draw.unavailable_from_detail` 纯函数：空/NULL 详情、损坏 JSON、非对象、`parse_error`、`ai_results` 缺失/null/非列表、元素非对象或空对象 → `"error"`；`degraded_reason` 非空 → `"degraded"`；**显式 `[]`（未调用 AI）与正常元素 → `""`**。`sample_to_eval` 严格校验 `unavailable ∈ {"", "degraded", "error"}`（拒绝 null/布尔/数字/容器，不再洗值） |
| **F03** | `tempfile.mkstemp`（随机独占临时名，前缀 `dst.name + "."`）替换固定 `.tmp`；默认发布 `os.link` **原子独占**（目标已存在含竞争创建 → `FileExistsError` → 拒绝且不覆盖）；`--force` 才 `os.replace`；`finally` 只清理本次创建的文件 |

## 3. 回归与门禁

- **主审探针转正式**：`tests/test_r114_f01_cleanup_entry.py`——junction 布局（Windows `mklink /J`，创建失败不得当通过）+ **正常清理控制**（无链接布局下过期文件仍被清理，防一刀切）。
- **F02/F03 回归**：`tests/test_r114_sample_pipeline.py`——五类反例 + 合法对照（含 `[]`、清理后 purged 形态）+ 转换器非法类型 + 输入撞临时名/他人 tmp/目标竞争/发布失败保留字节。
- 本机全量：**1175 收集 / 1160 passed / 15 skipped / 0 失败**；ruff / mypy 通过。
- 既有回归不动：`test_media_retention.py`（6 项）继续通过——PermissionError 仍传播（不吞错）、并发 FileNotFoundError 仍忽略。

## 4. 补正（主审 §4 / §6.4）

- **150 条状态重导出重算**：修复后逻辑逐条自存储详情恢复 `unavailable` → **150/150 与包内一致（0 变化）**；正式 CLI 重聚合与 `eval_report.json` **逐字节一致**（LF 口径 `0f3fe447…`，CRLF 口径 `dd67f369…`，已补 MANIFEST 说明）。
  **口径**：给定包内 1 条 degraded 的比例 <5% 本就判完整；本补充证明 150 条中无额外被洗掉的未知/错误——不改变历史包的任何字节与指标，也不据此宣称"生产源全部恢复完整"。
- **三条 FP 动作对账**（metadata-only，写入数据包 MANIFEST）：
  - 行 44 `onebot:<OLD_BOT_QQ>:1503091020` → intent **#822** SKIPPED（09-15 01:37 UTC）"动作开关未开启"
  - 行 51 `onebot:<OLD_BOT_QQ>:1818434381` → intent **#825** SKIPPED（09-15 01:41 UTC）"动作开关未开启"
  - 行 100 `onebot:<OLD_BOT_QQ>:1675671342` → intent **#858** SKIPPED（09-15 02:57 UTC）"群动作已禁用（管理员设置）"
  - 三条 FP 均**零实际执行**；范围仅限该样本窗，不扩大为"所有线上零误撤"。
- 数据包 MANIFEST 增加字节/换行口径、R-114 复验、sample_id 分发提示。

## 5. 运行边界（主审 §5 只读核查结果）

| 项 | 现场实况 |
| --- | --- |
| 计划任务 | `QQBotAutoCleanup`：**每天 04:00**（非 09-20 首发）；实测 LastRun 09-16 04:00、**NextRun 09-17 04:00**；执行 = 项目 `.venv` python + `-m app.reports.maintenance cleanup`，工作目录 = 项目根（每次新进程→加载当时代码） |
| 自动清理许可 | `auto_cleanup_enabled=1`（已开启）；最近一次 09-16 04:00 `succeeded` |
| 生产链接扫描 | `data/` 全部 **4191 条目 0 个符号链接/junction/重解析点**——扫描时未发现链接，**尚未发现既成误删**；本次扫描不证明历史零损失。F01 修复后已无绕行路径 |
| 常驻 Web 入口 | `POST /admin/cleanup` 依赖进程内已导入函数——**修复要重启 QQBotWeb 才在该入口生效**；修复完成前已叮嘱不使用该入口做真实清理 |
| 维护任务生效 | 明早 04:00 任务为新进程，自动加载修复后代码，无需额外操作 |
