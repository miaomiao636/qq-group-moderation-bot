# 负责人图片审核结论（2026-09-19，以负责人为准）

审核对象：`IMAGE_REVIEW.md` 中 31 张唯一图片（命中阈值 汉明距离 <= 2）。

## 结论

| 编号 | 图片文件 | 结论 |
| --- | --- | --- |
| 01–29 | `img-01` … `img-29` | **放行** |
| **30** | `img-30_2c2f279f133373a6.jpg` | **撤回（不得自动放行）** |
| 31 | `img-31_94a480808000ac8c.jpg` | **放行** |

即：**除第 30 张外，其余 30 张均为"确定放行"的图**。

## 落地方式

- 第 30 张的哈希 `2c2f279f133373a6` 写入 **排除清单**
  `docs/evidence/image-review/exclude_hashes.txt`；
- 种子导入脚本 `scripts/image_allowlist_seed.py` **自动读取该文件并跳过**这些哈希
  （`--exclude-file`，默认即上述路径），因此即使它来自历史放行图也不会进入白名单；
- 白名单其余条目不因此放宽或收紧；阈值维持 **<= 2**（本批审核即在该阈值下进行）。
