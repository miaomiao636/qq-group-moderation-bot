# 后续任务

## 当前状态

- 当前阶段：T-003与T-101实现已交付；提交 `7fca851` 主审复验未通过，第四轮整改（测试安全、真实Alembic端到端、盘符相对路径、文档状态）已完成并通过干净副本全量验证，等待主审复验；复验通过前不开始T-102。
- 当前最高目标：主审复验通过R-101；远程CI证据（复验项11）需项目负责人提供私有远程仓库后补齐；可并行准备T-001官方能力验证和T-002验收样本。
- 任务执行原则：每个Agent一次只认领一个边界清晰的任务；完成后更新 `PROGRESS.md` 和 `HANDOFF.md`，架构变化同步更新 `DECISIONS.md` 和 `PROJECT_CONTEXT.md`。
- 禁止事项：在身份链路验证完成前，不实现真实NapCat批量踢人；任何模型结果都不能直接创建踢人动作。

## P0：实施前置验证

### T-001 QQ官方机器人能力验证

- [ ] 创建QQ官方机器人测试应用和隔离测试群。
- [ ] 确认目标群可以开启全量消息事件并授予机器人管理员权限。
- [ ] 保存脱敏事件样本：文字、图片、GIF、表情、语音、视频、文件、转发和卡片。
- [ ] 实测撤回普通成员消息。
- [ ] 实测禁言3,600秒和86,400秒。
- [ ] 记录权限不足、消息不可撤回、媒体不可下载和限流响应。
- [ ] 将验证结论写入 `DECISIONS.md`，将脱敏样本放入未来的 `tests/fixtures/qq_official/`。

完成标准：所有计划依赖的官方能力都有真实测试结果；不支持的能力有明确降级方案，不能只依据文档假设。

### T-002 群规与训练/验收样本准备

- [ ] 为广告、引流、诈骗、色情、赌博、违禁交易、刷屏、骚扰和隐私泄露定义边界。
- [ ] 每个主要类别准备至少20个违规正例和30个相似合法反例。
- [ ] 图片、GIF、视频、语音、转发卡片和二维码分别准备至少10个违规与合法样本。
- [ ] 建立管理员、合作方、招聘、活动推广、允许域名和允许联系方式白名单。
- [ ] 对真实样本脱敏，禁止将密钥、身份证、支付信息或无关私聊写入仓库。

完成标准：样本可以直接用于离线评测，并且每个样本都有期望类别、是否处罚及理由。

### T-003 固化首批技术决策

- [x] 在 `DECISIONS.md` 记录QQ官方机器人主通道、人工/NapCat执行出口及其替代方案。
- [x] 记录Python 3.12、FastAPI、SQLAlchemy、SQLite WAL和Windows专用机的选择及替代方案。
- [x] 确定依赖管理（uv）、数据库迁移（Alembic）、测试（pytest）、格式化（ruff format）、静态检查（ruff check）和日志（JSON）方案。
- [x] 确定本地、测试、生产配置名称（APP_ENV），文件只提交安全模板。

完成标准：后续Agent不需要重新讨论已经确定的架构，有技术解决方案和负责人。

## P1：最小可运行审核链路

### T-101 项目脚手架与质量门禁

依赖：T-003。T-001可并行进行，不阻塞通用脚手架。

- [x] 创建Python 3.12项目、包结构、测试目录和配置模板。
- [x] 建立单元测试、类型检查、代码格式化和静态检查命令。
- [x] 增加 `.env.example`，只包含变量名和安全说明。
- [x] 更新 `README.md`，写明安装、运行、测试和目录结构。
- [x] 建立首个持续集成流程，至少运行测试和静态检查。

完成标准：全新环境可按README完成安装；测试、类型检查和静态检查全部通过。

### R-101 T-101主审整改

优先级：阻塞T-102及后续业务开发。

- [x] 将 `aiosqlite` 从开发依赖移入运行时依赖，并用不安装开发组的全新环境验证 `import app.main` 和启动。
- [x] 删除或严格限制 `Base.metadata.create_all`；生产启动必须使用Alembic迁移并校验数据库版本，拒绝创建无 `alembic_version` 的漂移数据库。
- [x] 为 `APP_ENV`、`RUN_MODE`、端口、保留天数和日志级别增加类型与范围校验；生产环境拒绝空管理员密码。
- [x] 让实际启动入口读取 `WEB_HOST` 和 `WEB_PORT`（新增 `app/__main__.py`），不再出现“修改 `.env` 端口但进程仍监听8000”。
- [x] 将 `alembic/` 纳入ruff检查和格式检查，修复迁移文件的ruff问题。
- [x] CI使用锁文件安装，并增加Windows执行环境；在Linux和Windows上运行测试、ruff和mypy。
- [x] 使用临时目录隔离测试数据库，避免固定 `tests/test_data/test.db` 在本地残留或并发测试间互相影响。
- [x] 将README目录树标记为规划结构，区分当前实际存在与规划中目录。
- [x] 处理或明确锁定TestClient的弃用警告（安装 `httpx2`、锁定 `anyio` 警告、修复 Alembic `path_separator`）。
- [x] 初始化本地Git仓库并建立首个基线提交，使后续修改可审核和回退。
- [x] 数据库校验必须等于当前代码的Alembic head版本，拒绝 `stale_revision` 等过期版本（R-101复验项1）。
- [x] 生产环境仅含空白字符的密码被拒绝（`strip` 后非空）（R-101复验项2）。
- [x] CI新增 `runtime-deps` 回归检查：仅安装运行时依赖并验证 `import app.main`，防止运行时依赖被误放入开发组（R-101复验项3）。
- [x] 测试临时目录在会话结束后主动删除，不残留 `qqbot-test-*`/`qqbot-nomigrate-*`（R-101复验项5）。
- [x] 修复运行时依赖 CI 失效：`uv run` 会自动重装 dev 依赖，改用 `--no-sync` 并断言 pytest 不可导入（R-101复验项6）。
- [x] 修复非项目工作目录无法启动：`get_head_revision` 基于 `PROJECT_ROOT` 解析 `alembic.ini` 与 `script_location`（R-101复验项7）。
- [x] 修复 Windows 清理风险：删除临时目录前关闭全局数据库引擎，移除 `ignore_errors=True`（R-101复验项8）。
- [x] 修复相对SQLite路径依赖当前工作目录：`app/config.py` 新增 `_normalize_sqlite_url`，在配置层把相对路径统一解析到 `PROJECT_ROOT` 下；Windows 盘符相对路径 `C:relative\db.db` 明确拒绝（R-101复验项9）。
- [x] 增加复验项9的自动回归测试：`tests/test_sqlite_path.py` 共13项，完全使用 pytest `tmp_path` 并带真实数据目录守卫夹具，覆盖 README 默认配置、非项目工作目录启动、Windows 绝对路径（正/反斜杠）、盘符相对路径拒绝、Unix 绝对路径、`:memory:`、非 SQLite URL，以及子进程真实 `alembic upgrade head` 与跨目录启动应用的端到端测试（R-101复验项10；提交 `7fca851` 主审指出的缺陷已整改）。
- [ ] 将仓库推送到私有远程仓库并取得Linux、Windows质量任务及运行时依赖任务的真实成功记录，保存提交号和运行链接；静态CI配置不算通过（R-101复验项11；需项目负责人提供私有远程仓库）。

完成标准：全新运行时安装可启动；Alembic是唯一生产建表路径且校验head版本；相对SQLite配置不受启动目录影响；测试不触碰真实数据库；无效生产配置启动失败；`.env`端口设置生效；`app/tests/alembic`质量门禁及Linux/Windows CI真实通过；Git可以显示可审核差异。**当前主审未通过，第四轮整改已完成，重新提交主审复验。**

> 说明：本轮验证在含哨兵真实数据库的干净临时副本中执行（24项pytest、静态检查、Alembic升降级、构建、配置拒绝、实际端口、运行时依赖），哨兵数据库字节级未变；这不能替代远程Linux/Windows CI或正式Windows电脑测试。

### T-102 统一消息契约与官方机器人适配器

依赖：T-001、T-101。

- [ ] 定义统一消息封装，包含来源、官方群/成员标识、消息ID、时间、消息段、附件和上下文。
- [ ] 实现官方事件验签、解析、幂等去重和媒体元数据提取。
- [ ] 实现官方撤回、禁言和警告适配器，但不包含踢人逻辑。
- [ ] 对T-001保存的每类事件样本建立回归测试。

完成标准：所有样本可稳定转换成统一消息；重复事件不会重复处罚；动作失败返回可审计结果。

### T-103 规则引擎与文字审核

依赖：T-002、T-102。

- [ ] 实现Unicode归一化、谐音/拆字处理、链接、域名、QQ号、微信号和联系方式提取。
- [ ] 实现黑白名单、重复内容、刷屏和行为窗口。
- [ ] 实现中等风险只记录、高置信才产生撤回/禁言建议的决策结构。
- [ ] 使用正例和反例建立离线评测报告。

完成标准：文字链路满足已定义的精确率基线，任何决策结构都不存在 `kick` 动作。

### T-104 违规阶梯与案件证据

依赖：T-102、T-103。

- [ ] 实现同一成员、同群、30天有效违规窗口。
- [ ] 实现第一次撤回、禁言1小时和一次警告。
- [ ] 实现第二次撤回、禁言24小时、不警告和生成 `PENDING_REVIEW` 案件。
- [ ] 保存原始证据、上下文、命中规则、模型版本和动作结果。
- [ ] 实现误判撤销违规记录。

完成标准：两次违规状态机、时间窗口、白名单和误判恢复均有自动化测试；第二次违规不会自动创建踢人任务。

## P2：多模态审核

### T-201 图片、二维码和GIF审核

依赖：T-102、T-103。

- [ ] 集成OCR、二维码解析和图片感知哈希。
- [ ] 实现GIF最多8帧的代表帧抽取与聚合判断。
- [ ] 缓存相同媒体分析结果，避免重复模型费用。
- [ ] 对遮挡二维码、变形文字、广告图和正常表情包建立回归测试。

完成标准：图片/GIF P95审核延迟不超过15秒；分析失败不触发处罚。

### T-202 视频、语音和文件审核

依赖：T-102、T-103。

- [ ] 实现场景切分、最多16帧抽取、帧OCR和音轨转写。
- [ ] 接入ASR并记录转写来源、置信度和失败原因。
- [ ] 只提取PDF、Office和纯文本，不执行任何文件。
- [ ] 对超过10分钟或200MB的媒体进入异步队列或只告警。

完成标准：普通视频P95审核延迟不超过90秒；危险文件不会被执行；失败路径全部可审计。

### T-203 多模型复核与成本控制

依赖：T-002、T-201、T-202。

- [ ] 用真实脱敏样本比较候选文本、视觉和ASR服务。
- [ ] 实现主模型不低于0.90且独立复核确认的处罚门槛。
- [ ] 实现超时、重试、熔断、预算上限和媒体缓存。
- [ ] 记录模型、版本、费用、延迟和判定证据。

完成标准：模型不可用时降级只记录；离线精确率、召回率和费用报告可重复生成。

## P3：人工审核和双出口踢人

### T-301 管理后台与案件审批

依赖：T-104。

- [ ] 实现案件证据查看、批次编号、候选人编号、保留、误判和人工处理状态。
- [ ] 实现“踢2,5,8”“保留1,3,13其他踢”“全部保留”“全部踢”等命令解析。
- [ ] 自然语言解析后必须显示结构化预览，不得直接执行。
- [ ] 实现5分钟一次性确认码、管理员白名单和超过10人的额外确认。
- [ ] 实现人工出口与NapCat出口互斥状态锁。

完成标准：越权、编号越界、批次过期、跨群解释和重复确认均被拒绝；同一案件两个出口不能同时成功。

### T-302 人工QQ客户端工作流

依赖：T-301。

- [ ] 后台生成适合人工查找目标的证据和成员信息，但明确标识未验证的QQ号。
- [ ] 实现“转人工处理”“已人工踢出”“取消”“无法确认成员”状态。
- [ ] 使用成员变动事件或管理员二次确认完成结案。

完成标准：NapCat完全关闭时，案件仍能从审核到人工结案完整运行。

### T-303 NapCat身份镜像验证

依赖：T-001、T-301；启用前必须得到负责人明确确认。

- [ ] 在隔离测试账号和测试群部署NapCat/OneBot。
- [ ] 人工配置 `official_group_openid ↔ napcat_group_id`。
- [ ] 记录数字发送者身份和消息指纹，验证唯一匹配、重复内容碰撞、时间偏差和映射冲突。
- [ ] 只允许唯一无冲突映射进入 `VERIFIED`。
- [ ] 验证NapCat离线或风控异常时自动关闭该出口。

完成标准：昵称、头像或群名片不能单独形成映射；映射缺失、冲突或过期时NapCat踢人调用为0。

### T-304 NapCat人工批准踢人执行器

依赖：T-303通过验收；启用前必须得到负责人明确确认。

- [ ] 仅接受人工审批产生的一次性有效令牌。
- [ ] 执行前重新查询成员角色，排除群主、管理员和白名单。
- [ ] 使用案件、群号和数字QQ号生成幂等键，串行执行并逐人记录结果。
- [ ] 默认不设置拒绝再次入群。
- [ ] 执行失败转人工队列，不无限重试。

完成标准：无审批、令牌过期、重复令牌、非 `VERIFIED` 映射或白名单目标均无法调用踢人接口；测试和7天分级期误踢人数为0。

## P4：报告、运维和上线

### T-401 日报、周报和实时告警

- [ ] 实现严重事件实时通知。
- [ ] 每天09:00和21:00生成日报，每周一09:10生成周报。
- [ ] 报告消息量、异常、动作、待审核、误判、运行状态、队列和模型费用。

完成标准：缺少数据时明确显示“不可用”，报告任务失败可告警和补发。

### T-402 安全、隐私和数据生命周期

- [ ] 管理后台只监听本机或可信内网，启用身份认证、权限控制和审计。
- [ ] 原始媒体与数据库分离并加密存储。
- [ ] 实现30天原始数据、180天判断和动作记录的清理任务。
- [ ] 完成群成员隐私告知和云模型数据边界文档。
- [ ] 验证日志、异常和测试快照不包含密钥或不必要个人信息。

完成标准：权限测试、数据清理测试、备份恢复和敏感信息扫描通过。

### T-403 分阶段上线

- [ ] 第1–3天影子审核，只记录模拟动作。
- [ ] 第4–10天启用官方自动撤回和禁言，踢人只允许人工客户端。
- [ ] 指标达标后再启用NapCat身份镜像。
- [ ] NapCat身份和审批链验收通过后，才启用“人工批准后踢人”。
- [ ] 每次规则或模型大版本更新先进入至少24小时影子模式。

完成标准：连续运行48小时无不可解释的重复事件、权限越界或状态错乱；所有紧急降级开关经过演练。

### T-404 Windows无人值守运行与灾难恢复

依赖：T-101；完整验收依赖T-102、T-104、T-301和T-401。

- [ ] 按 `docs/windows-operations.md` 配置并记录屏幕关闭、禁止睡眠/休眠、网卡节能、合盖行为和来电自动开机要求。
- [ ] 将审核服务、媒体任务、报告调度和健康监控安装为Windows Service或等效受监督进程。
- [ ] 实现自动延迟启动、失败重启、指数退避、连续失败熔断和独立告警。
- [ ] 实现启动顺序、`live`、`ready` 和 `degraded` 健康状态。
- [ ] 实现重启后的任务恢复和外部动作 `UNKNOWN` 人工复核，禁止盲目重复处罚。
- [ ] 实现Windows更新维护流程：暂停、排空、数据库检查与备份、重启、健康验证、恢复和结果通知。
- [ ] 验证NapCat启动方式；需要QQ验证时关闭NapCat出口并回退人工。
- [ ] 完成锁屏、服务崩溃、系统重启、断网、模拟断电恢复、磁盘不足和备份恢复演练。
- [ ] 将实际服务安装、启动、停止、状态查询和恢复命令写入 `README.md` 与 `docs/windows-operations.md`。

完成标准：Windows重启后无需人工打开终端即可恢复官方审核；结果未知的处罚不会重复；NapCat失败不影响官方审核；所有演练有时间、结果和日志证据。

## Windows正式测试时间点

- **现在可以准备电脑，但不能开始整套验收**：完成Windows更新，记录系统版本和CPU架构，准备专用目录、测试网络、隔离QQ账号和隔离群；不要写入生产密钥。
- **R-101通过后开始W0**：在空白Windows电脑跑干净安装、迁移和全部质量命令，建立Windows基线。
- **T-001与T-102通过后开始W1**：验证QQ官方消息与官方撤回、禁言、警告。
- **核心审核、案件、人工流程和多模态任务通过后开始W2**：验证NapCat关闭时的完整安全模式。
- **T-404实现后开始W3**：验证锁屏、Windows Service、重启、断网、更新、备份和恢复。
- **T-303/T-304及负责人批准后才开始W4**：验证NapCat，不得与安全模式整体验收绑定。
- **上述适用阶段通过后执行W5/T-403**：先影子运行，再逐步开放自动撤回和禁言，达到稳定性指标后才进行最终验收。

## 建议的任务分工

- Agent A：T-001、T-102，负责QQ官方接入与动作验证。
- Agent B：T-002、T-103、T-201，负责规则和图片/GIF审核。
- Agent C：T-104、T-301、T-302，负责案件、审批和人工工作流。
- Agent D：T-202、T-203，负责视频、语音、文件和模型评测。
- Agent E：T-303、T-304，仅在负责人批准后负责NapCat隔离验证和执行器。
- Agent F：T-401、T-402、T-403，负责报告、运维、安全和上线。
- 最终整体审核：由项目负责人指定的主审Agent按照验收指标完成，不由单个实现Agent自行宣布项目完成。

## 待确认事项

- [ ] QQ官方机器人应用是否已经创建并获得目标群所需的全量消息、撤回和禁言权限。
- [ ] 目标群清单、群规模、每群日均消息量和管理员权限。
- [ ] 专用NapCat管理员QQ是否已准备；若未准备，首版保持安全模式。
- [ ] 所有者QQ、备用人工管理员QQ和允许审批的管理员名单。
- [ ] 首批群规、白名单、违规正例和合法反例。
- [ ] 云模型候选、预算上限、数据是否允许发送到境外服务。
- [ ] 日报、周报和严重告警最终发送到QQ私聊、网页后台还是两者同时。
- [ ] 人工确认“已踢出”后，是否要求第二位管理员复核。
