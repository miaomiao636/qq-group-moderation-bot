# 后续任务

## 当前状态

- 当前阶段（2026-09-08）：生产大群主通道为NapCat/OneBot。**T-305主审整改已通过本地门禁与提交 `b3a107b` 的跨平台CI，现已关闭；下一项为T-306。** 真实NapCat、MiMo和Windows 24×7尚未验收。
- 当前最高目标：T-306 NapCat入站与影子运行；随后依次完成T-303 Windows隔离群链路验证、T-307 NapCat撤回/禁言/警告动作，再进入T-404无人值守和T-403分阶段上线。
- 任务执行原则：每个Agent一次只认领一个边界清晰的任务；完成后更新 `PROGRESS.md` 和 `HANDOFF.md`，架构变化同步更新 `DECISIONS.md` 和 `PROJECT_CONTEXT.md`。
- 禁止事项：不得把OneBot数据伪装成 `group_openid/member_openid`、不得跳过T-305直接接管官方动作编排、不得在影子链路和保护测试通过前对真实群执行动作；任何模型结果都不能直接创建踢人动作。

## P0：实施前置验证

### R-104 恢复当前 `main` 质量门禁

> **状态（2026-09-08）**：已完成。

- [x] 只格式化 `app/reports/stats.py` 的已有差异，不混入NapCat功能。（提交 `2d8f405`）
- [x] 重跑pytest、mypy、ruff check和ruff format check。（203 passed / 1 skipped；mypy 49个源文件通过；ruff两项通过）
- [x] 推送后取得Ubuntu、Windows与干净运行时依赖三个任务的真实成功证据。（GitHub Actions运行 `34186194703`）

完成证据：失败基线运行 `34083954491` @ `88ac433` 已由成功运行 `34186194703` @ `2d8f405` 替代，三个远程任务全部成功。其后的Node.js 20维护项已由T-405关闭。

### T-001 QQ官方机器人能力验证

> **状态（2026-09-07更新）**：官方接口在可接入的4个少人隔离群中的核心技术能力已实测（D-012）；但个人认证无法开启“添加到任意群聊”，目标大群不显示机器人（D-019）。因此T-001只作为可选官方Adapter的技术证据，不是目标大群上线前置。

- [x] 创建QQ官方机器人测试应用和隔离测试群。（2026-09-05负责人完成：AppID 1905561634，测试群4个，机器人已入群）
- [x] 确认隔离测试群可开启全量消息事件并授予机器人管理员权限。（2026-09-05：全量消息事件实测收到（GROUP_MESSAGE_CREATE，含不带@消息）；机器人已获群管理员身份，撤回/禁言实测通过）
- [x] 保存脱敏事件样本：文字、图片、GIF、表情、语音、视频、文档、转发和卡片。（2026-09-05：已入库9类——文字@/普通文字/表情/图片/GIF/语音AMR/视频MP4/文件PDF/转发记录；分享卡片的事件形态待负责人确认）
- [x] 实测撤回普通成员消息。（2026-09-05实测成功，机器人需群管理员；重复撤回返回HTTP 200，接口幂等）
- [x] 实测禁言3,600秒和86,400秒。（2026-09-05实测成功；mute_expire_at到期时间制；op=del解除禁言成功）
- [ ] 记录权限不足、被撤回、媒体不可下载和限流响应。（已记录：撤回权限不足40062003、禁言保护角色40103004、重复撤回200；限流响应与媒体URL失效未实际触发，待T-102/W1验证）
- [x] 将验证结论写入 `DECISIONS.md`，将脱敏样本放入未来的 `tests/fixtures/qq_official/`。（2026-09-05：决策D-012；样本9份入库）

完成标准：所有官方Adapter计划依赖的技术能力都有实际结果；正式群可添加范围也必须单独验证。当前结论为个人主体不满足目标大群接入需求，已通过D-019改用NapCat主通道。

### T-002 群规与训练/验收样本准备

> **状态（2026-09-05）**：负责人已提供首批群规、白名单原则、8张违规例图与5条违规文字（已归档脱敏）；规则基线见 `docs/group-rules.md`。**仍需**：万能校园墙允许海报原图归档、刷屏量化定义、色情/暴力类样本、各类别补齐至验收数量。未达标前不得宣称完成。

- [x] 为广告、引流、诈骗、色情、赌博、违禁交易、刷屏、骚扰和隐私泄露定义边界。（2026-09-05完成初版：广告/引流/刷屏/诈骗边界已明确；色情/暴力类仅有原则无样本）
- [ ] 每个主要类别准备至少20个违规正例和30个相似合法反例。（现有：违规图8张+文字5条；远未达标，持续补充）
- [ ] 图片、GIF、视频、语音、转发卡片和二维码分别准备至少10个违规与合法样本。
- [ ] 建立管理员、合作方、招聘、活动推广、允许域名和允许联系方式白名单。
- [ ] 对真实样本脱敏，禁止将密钥、身份证、支付信息或无关私聊写入仓库。

完成标准：样本可以直接用于离线评测，并且每个样本都有期望类别、是否处罚及理由。

### T-003 固化首批技术决策

- [x] 在 `DECISIONS.md` 记录QQ官方机器人主通道、人工/NapCat执行出口及其替代方案。
- [x] 记录Python 3.12、FastAPI、SQLAlchemy、SQLite WAL和Windows专用机的选择及替代方案。
- [x] 确定依赖管理（uv）、数据库迁移（Alembic）、测试（pytest）、格式化（ruff format）、静态检查（ruff check）和日志（JSON）方案。
- [x] 确定本地、测试、生产配置名称（APP_ENV），文件只提交安全模板。

完成标准：后续Agent不需要重新讨论已经确定的架构，有技术解决方案和负责人。

## P1：最小可运行审核链路

### T-101 项目脚手架与质量门禁

依赖：T-003。T-001可并行进行，不阻塞通用脚手架。

- [x] 创建Python 3.12项目、包结构、测试目录和配置模板。
- [x] 建立单元测试、类型检查、代码格式化和静态检查命令。
- [x] 增加 `.env.example`，只包含变量名和安全说明。
- [x] 更新 `README.md`，写明安装、运行、测试和目录结构。
- [x] 建立首个持续集成流程，至少运行测试和静态检查。

完成标准：全新环境可按README完成安装；测试、类型检查和静态检查全部通过。

### R-101 T-101主审整改

优先级：阻塞T-102及后续业务开发。

- [x] 将 `aiosqlite` 从开发依赖移入运行时依赖，并用不安装开发组的全新环境验证 `import app.main` 和启动。
- [x] 删除或严格限制 `Base.metadata.create_all`；生产启动必须使用Alembic迁移并校验数据库版本，拒绝创建无 `alembic_version` 的漂移数据库。
- [x] 为 `APP_ENV`、`RUN_MODE`、端口、保留天数和日志级别增加类型与范围校验；生产环境拒绝空管理员密码。
- [x] 让实际启动入口读取 `WEB_HOST` 和 `WEB_PORT`（新增 `app/__main__.py`），不再出现"修改 `.env` 端口但进程仍监听8000"。
- [x] 将 `alembic/` 纳入ruff检查和格式检查，修复迁移文件的ruff问题。
- [x] CI使用锁文件安装，并增加Windows执行环境；在Linux和Windows上运行测试、ruff和mypy。
- [x] 使用临时目录隔离测试数据库，避免固定 `tests/test_data/test.db` 在本地残留或并发测试间互相影响。
- [x] 将README目录树标记为规划结构，区分当前实际存在与规划中目录。
- [x] 处理或明确锁定TestClient的弃用警告（安装 `httpx2`、锁定 `anyio` 警告、修复 Alembic `path_separator`）。
- [x] 初始化本地Git仓库并建立首个基线提交，使后续修改可审核和回退。
- [x] 数据库校验必须等于当前代码的Alembic head版本，拒绝 `stale_revision` 等过期版本（R-101复验项1）。
- [x] 生产环境仅含空白字符的密码被拒绝（`strip` 后非空）（R-101复验项2）。
- [x] CI新增 `runtime-deps` 回归检查：仅安装运行时依赖并验证 `import app.main`，防止运行时依赖被误放入开发组（R-101复验项3）。
- [x] 测试临时目录在会话结束后主动删除，不残留 `qqbot-test-*`/`qqbot-nomigrate-*`（R-101复验项5）。
- [x] 修复运行时依赖 CI 失效：`uv run` 会自动重装 dev 依赖，改用 `--no-sync` 并断言 pytest 不可导入（R-101复验项6）。
- [x] 修复非项目工作目录无法启动：`get_head_revision` 基于 `PROJECT_ROOT` 解析 `alembic.ini` 与 `script_location`（R-101复验项7）。
- [x] 修复 Windows 清理风险：删除临时目录前关闭全局数据库引擎，移除 `ignore_errors=True`（R-101复验项8）。
- [x] 修复相对SQLite路径依赖当前工作目录：`app/config.py` 新增 `_normalize_sqlite_url`，在配置层把相对路径统一解析到 `PROJECT_ROOT` 下；Windows 盘符相对路径 `C:relative\db.db` 明确拒绝（R-101复验项9）。
- [x] 增加复验项9的自动回归测试：`tests/test_sqlite_path.py` 共13项，完全使用 pytest `tmp_path` 并带真实数据目录守卫夹具，覆盖 README 默认配置、非项目工作目录启动、Windows 绝对路径（正/反斜杠）、盘符相对路径拒绝、Unix 绝对路径、`:memory:`、非 SQLite URL，以及子进程真实 `alembic upgrade head` 与跨目录启动应用的端到端测试（R-101复验项10；提交 `7fca851` 主审指出的缺陷已整改）。
- [x] 将仓库推送到私有远程仓库并取得Linux、Windows质量任务及运行时依赖任务的真实成功记录，保存提交号和运行链接；静态CI配置不算通过（R-101复验项11）。
  - 远程仓库：`https://github.com/miaomiao636/qq-group-moderation-bot`（私有）。
  - 证据提交：`0e0dd73`；CI 运行（success）：`https://github.com/miaomiao636/qq-group-moderation-bot/actions/runs/33877462326`，三个任务链接记录于 `HANDOFF.md`。

完成标准：全新运行时安装可运行；Alembic是唯一生产建表路径且校验head版本；相对SQLite配置不受启动目录影响；测试不触碰真实数据库；无效生产配置启动失败；`.env`端口设置生效；`app/tests/alembic`质量门禁及Linux/Windows CI真实通过；Git可以显示可审核差异。**R-101已由主审复验通过，T-102可在T-001完成后开始。**

> 说明：本轮验证在含哨兵真实数据库的干净临时副本中执行（24项pytest、静态检查、Alembic升降级、构建、配置拒绝、实际端口、运行时依赖），哨兵数据库字节级未变；这不能替代远程Linux/Windows CI或正式Windows电脑测试。

### R-103 R-102独立复验整改

优先级：P0，阻塞动态规则、AI和真实自动处罚。设计见 `docs/superpowers/specs/2026-09-06-ai-rule-learning-design.md`。

- [x] 将事件领取改为带令牌和租约的原子领取；未过期 `PROCESSING` 不可被第二处理器领取，过期后才允许接管。
- [x] 区分永久契约错误与临时错误；永久解析失败只保存一次人工记录并终止重试，判定写入必须幂等。
- [x] 修复媒体配额越界、`.part`未计入、文件名截尾碰撞和嗅探时全文件读入问题。
- [x] 重新校准“兼职/刷单/一单”等词：单个词不再是硬证据；加入否定、咨询、反诈教育和引用语境反例。
- [x] 修复R006：允许来源卡片放行，未知来源只记录，只有明确引流证据才高风险。
- [x] 所有 `/admin/*` 页面统一登录保护；全部状态修改POST加入CSRF和审计。
- [x] 清理过期文档状态，确保代码、任务、进度和README一致。
- [x] 将功能分支推送到私有远程仓库并取得真实Ubuntu、Windows质量任务及干净运行时依赖任务的成功记录（PR #1，运行 `34028509570`，head提交 `d00960d`）。

完成标准：并发领取成功数恒为1；永久失败重复投递无唯一键异常；媒体配额永不超限；已确认语境反例不自动处罚；允许卡片来源不处罚；管理页鉴权/CSRF测试通过；全量质量门禁和Linux/Windows CI通过。

### T-102 统一消息契约与官方机器人适配器

依赖：T-001、T-101。

- [x] 定义统一消息封装，包含来源、官方群/成员标识、消息ID、时间、消息段、附件和上下文。（`app/adapters/qq_official/contract.py`：StandardMessage/Sender/Attachment/ShareCardInfo）
- [x] 实现官方事件验签、解析、幂等去重和媒体元数据提取。（parser.py + dedup.py，`processed_events` 表为最终防线；验签在 T-402 WebHook 接入时补充——当前 WebSocket 直连形态无 HTTP 签名头）
- [x] 实现官方撤回、禁言和警告适配器，但不包含踢人逻辑。（actions.py：超时10s、幂等动作重试1次、警告不重试、结果 ActionResult 可审计、audit.py 落库 action_logs）
- [x] 对T-001保存的每类事件样本建立回归测试。（12份样本契约测试全部通过）

完成标准：所有样本可稳定转换成统一消息；重复事件不会重复处罚；动作失败返回可审计结果。✅ 已达成（2026-09-05，62项测试全绿）。

### T-103 规则引擎与文字审核

依赖：T-002、T-102。

- [x] 实现Unicode归一化、谐音/拆字处理、链接、域名、QQ号、微信号和联系方式提取。（app/moderation/normalization.py + extract.py，结果遮蔽）
- [x] 实现黑白名单、重复内容、刷屏和行为窗口。（黑名单0.70/弱信号组合/保护角色只记录；刷屏1分钟>5条相同指纹=确定性违规）
- [x] 实现中等风险只记录、高置信才产生撤回/禁言建议的决策结构。（decision.py：verdict=record_only/violation_high，动作仅recall/mute/warn）
- [x] 使用正例和反例建立离线评测报告。（5条真实违规文字全命中高置信、5条正常聊天全放行，测试即报告）

完成标准：文字链路满足已定义的精确率基线，任何决策结构都不存在 `kick` 动作。✅ 已达成（2026-09-05）。

### T-104 违规阶梯与案件证据

依赖：T-102、T-103。

- [x] 实现同一成员、同群、30天有效违规窗口。（service.count_active_violations，revoked不计）
- [x] 实现第一次撤回、禁言1小时和一次警告。
- [x] 实现第二次撤回、禁言24小时、不警告和生成 `PENDING_REVIEW` 案件。（合并窗口内全部证据，批次号R{日期}-{seq}）
- [x] 保存原始证据、上下文、命中规则、模型版本和动作结果。（message_snapshot_json/rule_hits_json/action_result_json；模型版本字段在T-203接入模型时补充）
- [x] 实现误判撤销违规记录。（撤销联动案件 FALSE_POSITIVE→STRIKE_REVOKED→CLOSED）

完成标准：两次违规状态机、时间窗口、白名单和误判恢复均有自动化测试；第二次违规不会自动创建踢人任务。✅ 已达成（2026-09-05）。

### T-105 后台可编辑的版本化动态规则

依赖：R-103、T-103。

- [x] 新增全局/群级规则集、不可变版本、规则条目和规则审计表及Alembic迁移。
- [x] 将系统默认结构化规则种子化为首个数据库规则版本；运行时加载数据库 `ACTIVE` 版本。
- [x] 后台实现草稿创建、添加规则项、发布和回滚；不允许任意代码、SQL或不受限正则。
- [x] 发布后5秒内热更新内存快照，不改代码、不重启；常驻文字引擎保留刷屏状态并刷新规则快照。
- [x] 每条判定保存规则版本；允许/禁止冲突降级人工复核。
- [ ] 增强：发布前差异预览、历史消息模拟回放和并发发布冲突提示仍需补强。

完成标准：草稿不影响运行；发布/回滚并发安全；热更新和版本追溯自动化测试通过；后台操作具备登录、CSRF和审计。

### T-106 官方撤回/禁言/警告运行编排

依赖：R-103、T-104、T-105；真实启用依赖W1/W2与负责人确认。

- [x] 增加 `SHADOW`/`OFFICIAL` 动作模式，默认并始终安全回退到 `SHADOW`。
- [x] 高置信决策先持久化动作意图和幂等键，再调用QQ官方撤回、分级禁言和首次警告。
- [x] 数据库失败时不调用外部动作；结果未知进入人工复核，不能盲目重放。
- [x] 群主、管理员和白名单在编排层再次拦截；任何路径都不得产生踢人动作。
- [x] 提供紧急停止开关、降级原因和动作审计。

完成标准：模拟重复、超时、进程中断和未知结果均不重复处罚；默认配置和测试环境外部动作调用数为0；正式开关条件有自动化拒绝测试。

## P2：多模态审核

### T-201 图片、二维码和GIF审核

依赖：T-102、T-103。

- [x] 集成OCR、二维码解析和图片感知哈希。（dHash自实现；二维码zxing-cpp解码；OCR为可选本地组件，缺失时降级record_only——零预算决策D-014，付费OCR不引入）
- [x] 实现GIF最多8帧的代表帧抽取与聚合判断。（任一帧命中黑名单即违规）
- [x] 缓存相同媒体分析结果，避免重复模型费用。（dHash键内存缓存；零预算下模型费用为0）
- [x] 对遮挡二维码、变形文字、广告图和正常表情包建立回归测试。（18项：合成图+负责人真实样本本地回归）

完成标准：图片/GIF P95审核延迟不超过15秒；分析失败不触发处罚。✅ 已达成（实测0.78s跑完18项）。

### T-202 视频、语音和文件审核

依赖：T-102、T-103。

- [x] 实现场景切分、最多16帧抽取、帧OCR和音轨转写。（v1顺序取前16帧复用图片引擎；场景切分与帧OCR留待T-203优化；音轨转写采用官方asr_refer_text零成本方案，见D-014）
- [x] 接入ASR并记录转写来源、置信度和失败原因。（ASR=官方自带转写字段，来源/失败原因记录于决策reason；外部ASR不引入）
- [x] 只提取PDF、Office和纯文本，不执行任何文件。（PDF用pypdf提取；Office版v1仅元数据记录；可执行文件绝不解析）
- [x] 对超过10分钟或200MB的媒体进入异步队列或只告警。（超限即record_only告警文案）

完成标准：普通视频P95审核延迟不超过90秒；危险文件不会被执行；失败路径全部可审计。✅ 已达成（12项测试含真实ffmpeg视频）。

### T-203 多模型复核与成本控制

依赖：T-002、T-201、T-202。

- [ ] 用真实脱敏样本比较候选文本、视觉和ASR服务。
- [ ] 实现主模型不低于0.90且独立复核确认的处罚门槛。
- [ ] 实现超时、重试、熔断、预算上限和媒体缓存。
- [ ] 记录模型、版本、费用、延迟和判定证据。

完成标准：模型不可用时降级只记录；离线精确率、召回率和费用报告可重复生成。

> 当前说明：T-203已有本地复核门、成本台账和熔断器基础，但没有真实远程模型适配、供应商契约或脱敏样本评测；这些由T-204完成前不得把T-203描述为“远程多模型已完成”。

### T-204 远程文本/视觉AI辅助审核

依赖：R-103、T-105、T-201、T-202、T-203基础组件。

- [x] 定义供应商无关的 `TextModerator`/`VisionModerator` 和严格JSON响应契约。
- [x] 实现OpenAI-compatible适配器，支持分别配置MiMo文本与视觉模型；API Key只从环境变量或系统凭据读取。
- [x] 按群显式启用；文本、卡片文本、官方ASR文本和已下载图片/GIF可进入AI辅助；行为规则不发给模型。
- [x] 实现最小脱敏上传、超时、限流、每日预算、版本化缓存、AI用量日志和无密钥/供应商失败降级。
- [x] 单模型结果只作为软证据；未配置独立复核模型时不得触发自动处罚。
- [x] 使用固定假响应覆盖成功、非法JSON、提示词注入式动作字段、供应商失败、缓存、无密钥/未启用降级和隐私脱敏；真实MiMo由负责人本地配置后单独评测。
- [ ] 增强：视频代表帧远程视觉AI、外部OCR/外部ASR、持久化跨进程熔断和独立复核模型升级策略仍需T-203/T-204后续补强。

完成标准：AI关闭或不可用不影响本地审核；任何非法响应只记录；数据库和日志无密钥；每次结果可追溯模型、提示词、规则版本、费用和延迟。

### T-205 管理员反馈与候选规则学习

依赖：T-105、T-204。

- [x] 后台允许对消息标记确认违规、确认正常、误判、未知原因撤回和其他撤回原因；案件级批量标注仍可在后续增强。
- [x] 未撤回保持未标注；未知原因撤回不成为真值；AI/系统自身判定不成为人工真值。
- [x] 从确认反馈提取短语、域名和联系方式组合候选；二维码、来源、媒体哈希和行为候选保留为后续增强。
- [x] 候选达到支持阈值后由本地挖掘器生成允许的结构化规则类型；AI自然语言总结留待真实模型接入后补强。
- [x] 候选记录正例覆盖、确认负例误伤和支持样本；未标注影响和完整历史回放仍需增强。
- [x] 候选只能复制为草稿，必须再次预览和人工发布；支持版本审计和回滚。
- [x] 预留首版后台权威反馈入口。NapCat主通道的撤回通知将在T-306/T-303验证，但“发生撤回”仍只是待标注事件，不得自动当作违规真值。

完成标准：不存在自动发布路径；反馈标签语义和防投毒测试通过；候选规则无法包含代码、任意正则或动作；回放和人工发布可审计。

## P3：传输中立、NapCat主通道与人工审核

### T-301 管理后台与案件审批

依赖：T-104。

- [x] 实现案件证据查看、批次编号、候选人编号、保留、误判和人工处理状态。（app/web/routes.py：案件列表/详情/证据页，成员标注"未验证QQ号"）
- [x] 实现"踢2,5,8""保留1,3,13其他踢""全部保留""全部踢"等命令解析。（v1适配单成员案件：预览→确认码→执行；批量候选人编号解析随真实批量案件需求在验收期迭代）
- [x] 自然语言解析后必须显示结构化预览，不得直接执行。（approve-manual先返回结构化预览页+确认码，无直接执行路径）
- [x] 实现5分钟一次性确认码、管理员白名单和超过10人的额外确认。（confirm.py 5分钟TTL一次性；登录凭据即管理员白名单；批量>10人额外确认随批量功能迭代）
- [x] 实现人工出口与NapCat出口互斥状态锁。（case_sm双出口互斥，Web强制经状态机转换）

完成标准：越权、编号越界、批次过期、跨群解释和重复确认均被拒绝；同一案件两个出口不能同时成功。✅ 已达成（11项Web集成测试，2026-09-05）。

### T-302 人工QQ客户端工作流

依赖：T-301。

- [x] 后台生成适合人工查找目标的证据和成员信息，但明确标识未验证的QQ号。（证据页OpenID+警告标注）
- [x] 实现"转人工处理""已人工踢出""取消""无法确认成员"状态。（approve-manual→MANUAL_PENDING；confirm-kick→KICKED；cancel→CANCELLED→CLOSED）
- [x] 使用成员变动事件或管理员二次确认完成结案。（管理员确认码二次确认为v1结案方式；成员变动事件随官方事件开通后在T-102适配器层补充）

完成标准：自动踢人执行器完全关闭时，案件仍能从审核到人工结案完整运行。✅ 已达成（2026-09-05）。

### T-305 传输中立消息、身份和动作契约

依赖：R-104、T-102、T-104、T-106。

> **状态（2026-09-08）：已验收关闭。** 主审独立复现并修复4类问题：OneBot错误回退官方动作通道、跨provider违规/动作串扰、核心模块反向导入Adapter、通用消息段缺失。本地234项测试中233通过/1跳过，mypy 57源文件、ruff和Alembic完整升降级通过；提交 `b3a107b` 的CI运行 `34200777456` 中Ubuntu、Windows和干净运行时三项全绿。

- [x] 定义中立消息契约，至少包含 `provider`、`external_group_id`、`external_user_id`、`external_message_id`、角色、消息段、附件和时间，不暴露OneBot或QQ官方原始结构。（`app/core/contracts.py`；旧字段为双向同步的镜像视图，contract阶段另行移除）
- [x] 定义小而稳定的 `MessageSource` 与 `ModerationActionClient` 接口，官方和NapCat Adapter都从该seam接入。（协议均为runtime-checkable、位置限定参数；官方 `QQOfficialMessageSource` 与 `OfficialActionAdapter` 已验证结构性满足）
- [x] 将现有 `StandardMessage`、审核、案件、动作意图和报告从官方专用命名迁移到中立ID；旧的 `group_openid/member_openid` 使用expand/migrate/contract步骤兼容，不原地改名或删列。（核心模块顶层零Adapter导入；`shadow_decisions/violation_records/cases/action_intents/action_logs/processed_events/feedback_records/ai_usage_logs` 均已加中立列并双写回填）
- [x] 动作路由以 `message_provider + external_group_id` 联合定位，每群只有一个出口。未配置官方群保留历史默认，未配置OneBot或交叉provider路由一律fail-closed；`ACTION_MODE=OFFICIAL` 永不启用OneBot动作。
- [x] 保留QQ官方Adapter现有契约样本和功能，通过兼容Adapter保证旧回归测试继续通过。（`contract.py`/`actions.py` 兼容再导出；12份官方fixture回归全过）
- [x] 为数据迁移编写可回滚Alembic脚本、回填校验和混合版本兼容测试。（迁移 `b8e2f6a4c1d9` 可完整降级；回填SQL单一事实来源在 `app/core/identity_backfill.py`；`tests/test_migration_t305.py` 覆盖回填幂等/混合行兼容/双写；`tests/fixtures/onebot/` + `test_onebot_fixture.py` 证明同链路可被OneBot fixture驱动）

完成标准：同一审核/案件链路可使用官方fixture和OneBot fixture驱动；核心模块不导入任一供应商Adapter；旧数据可读、新数据可回滚，不破坏已有官方通道测试。→ 已达成并有跨平台CI证据。

### T-306 NapCat/OneBot入站Adapter与影子运行器

依赖：T-305。

- [ ] 支持NapCat OneBot 11的反向WebSocket主入站方式，管理端点仅监听本机或可信内网，强制访问令牌并校验事件结构。
- [ ] 解析文字、图片、GIF/表情、语音、视频、文件、回复/引用、合并转发和卡片；未知消息段保留元数据并降级人工。
- [ ] 媒体必须按现有大小、类型、配额、超时和安全规则即时下载，不执行群成员文件。
- [ ] 使用 `provider + self_id + message_id` 或经契约证明等价的稳定键去重；重连、重复推送和进程重启不得重复处理。
- [ ] 默认强制影子模式，记录拟执行动作但不调用OneBot管理操作。
- [ ] 提供NapCat连接、QQ登录态、最后心跳、每群最后事件和队列积压的 `ready/degraded` 证据；不得只以Python进程存活判断就绪。

完成标准：脱敏OneBot fixtures覆盖主要消息类型、非法事件、重放、断线和媒体失败；在无NapCat真机时全部契约测试可运行，外部处罚调用数为0。

### T-303 NapCat主通道Windows隔离验证

依赖：R-104、T-305、T-306；使用项目负责人准备的专用Windows 10电脑、专用QQ和隔离测试群。

- [ ] 安装并固定验收的QQ、NapCat和OneBot配置版本，记录版本及回退方法，真实令牌只存Windows本机。
- [ ] 使用普通QQ管理员账号验证目标格式的文字与全部媒体事件，核对数字 `group_id/user_id/message_id`、角色、回复和撤回通知。
- [ ] 在影子模式中完整跑通标准化、规则/AI、媒体、违规累计、案件和报告，证明OneBot管理动作调用数为0。
- [ ] 演练WebSocket断开、QQ退出/安全验证、NapCat进程崩溃、重复事件和版本不兼容；失效时必须告警并转人工监管。
- [ ] 不尝试绕过QQ安全验证、设备指纹或风控。

完成标准：隔离群连续影子运行至少24小时，消息类型、丢失/重复计数、延迟、断线恢复和降级均有脱敏证据；不允许用Mac、CI或mock代替此实机验收。

### T-307 NapCat撤回、禁言和警告动作Adapter

依赖：T-303影子链路通过、T-305、T-106；真实动作必须在隔离群由负责人明确启用。

- [ ] 将已有动作意图交给provider-neutral orchestrator，由NapCat Adapter实现撤回、1小时/24小时禁言和首次警告，不在Adapter内重复审核逻辑。
- [ ] 外部动作前持久化意图、provider、数字目标ID、请求摘要和幂等键；超时或断线为 `UNKNOWN`，不盲目重放。
- [ ] 执行前确认群路由、QQ管理员权限、目标角色、白名单、急停和当前NapCat/QQ就绪状态。
- [ ] 提供按群的影子/真实动作开关；代码同步、服务重启或NapCat重连不得自动开启真实处罚。
- [ ] 使用假OneBot响应覆盖成功、明确失败、超时、连接中断、权限不足、保护角色、重复事件、重启和急停。

完成标准：影子模式与保护角色外部调用为0；同一动作意图最多成功一次；隔离群实测撤回、禁言3,600/86,400秒和首次警告成功，所有结果可审计。

### T-304 NapCat人工批准踢人执行器

依赖：T-303、T-305和T-307通过验收；启用前必须得到负责人明确确认。

- [ ] 仅接受人工审批产生的一次性有效令牌；无审批时踢人调用永远为0。
- [ ] 执行前重新查询成员角色，排除群主、管理员和白名单。
- [ ] 使用案件、群号和数字QQ号生成幂等键，串行执行并逐人记录结果。
- [ ] 默认不设置拒绝再次入群。
- [ ] 执行失败转人工队列，不无限重试。

完成标准：无审批、令牌过期、重复令牌、数字目标ID缺失、角色未复核、跨通道映射不可靠或白名单目标均无法调用踢人接口；测试和7天分级期误踢人数为0。

## P4：报告、运维和上线

### T-401 日报、周报和实时告警

- [x] 实现严重事件实时通知。（v1：动作失败记录于action_logs并在报告页展示；负责人2026-09-05确认报告渠道=仅管理后台网页，不实现推送）
- [x] 每天09:00和21:00生成日报，每周一09:10生成周报。（build_daily/build_weekly可重复生成；调度由Windows计划任务/部署层驱动（T-404），应用内不内置定时线程）
- [x] 报告消息量、异常、动作、待审核、误判、运行状态、队列和模型费用。（零预算下模型费用恒0，CostLedger记录）

完成标准：缺少数据时明确显示"不可用"，报告任务失败可告警和补发。✅ 已达成（2026-09-05，决策D-015确认渠道）。

### T-402 安全、隐私和数据生命周期

- [x] 管理后台只监听本机或可信内网，启用身份认证、权限控制和审计。（强制登录+会话令牌；案件转换记录operator审计；启动绑定127.0.0.1）
- [x] 原始媒体与数据库分离并加密存储。（媒体存本机data/目录与库分离（gitignored）；静态加密留待验收期按负责人要求开启BitLocker级方案）
- [x] 实现30天原始数据、180天判断和动作记录的清理任务。（reports/cleanup.py：快照占位替换+日志删除，阈值读settings）
- [x] 完成群成员隐私告知和云模型数据边界文档。（D-014数据不出本机；告知文案已起草待负责人审定后公示，见D-015）
- [x] 验证日志、异常和测试快照不包含密钥或不必要个人信息。（fixtures全脱敏并有测试守护；审计证据遮蔽联系方式）

完成标准：权限测试、数据清理测试、备份恢复和敏感信息扫描通过。✅ 核心达成（清理与权限有自动化测试；备份恢复演练属T-404实机项）。

### T-403 分阶段上线

- [ ] 先在隔离群完成W1至W4；不得把官方小群实测当成NapCat大群验收证据。
- [ ] 目标群第1–3天仅影子审核，只记录模拟动作，人工核对误报、漏报和消息接收缺口。
- [ ] 指标达标后只选一个低风险目标群开启NapCat撤回，观察至少24小时后再开启1小时/24小时禁言和首次警告。
- [ ] 踢人默认只允许人工QQ客户端；T-304独立验收通过后，才允许“人工批准后由NapCat踢人”。
- [ ] NapCat/QQ掉线、安全验证、异常失败率、媒体队列积压或无法证明持续收消息时，立即停止真实动作并转人工监管。
- [ ] 每次规则或模型大版本更新先进入至少24小时影子模式。

完成标准：连续运行48小时无不可解释的消息缺口、重复事件、重复处罚、权限越界或状态错乱；所有紧急降级开关和人工接管经过演练。

### T-404 Windows无人值守运行与灾难恢复

依赖：T-101；完整验收依赖T-303、T-305、T-306、T-307、T-104、T-301和T-401。

- [ ] 按 `docs/windows-operations.md` 配置并记录屏幕关闭、禁止睡眠/休眠、网卡节能、合盖行为和来电自动开机要求。
- [ ] 将审核服务、媒体任务、报告调度和健康监控安装为Windows Service或等效受监督进程。
- [ ] 实现自动延迟启动、失败重启、指数退避、连续失败熔断和独立告警。
- [ ] 实现启动顺序、`live`、`ready` 和 `degraded` 健康状态。
- [ ] 实现重启后的任务恢复和外部动作 `UNKNOWN` 人工复核，禁止盲目重复处罚。
- [ ] 实现Windows更新维护流程：暂停、排空、数据库检查与备份、重启、健康验证、恢复和结果通知。
- [ ] 将QQ客户端会话、NapCat与OneBot连接纳入就绪检查；需要QQ验证时关闭所有真实动作、告警并回退人工监管。
- [ ] 完成锁屏、服务崩溃、系统重启、断网、模拟断电恢复、磁盘不足和备份恢复演练。
- [ ] 将实际服务安装、启动、停止、状态查询和恢复命令写入 `README.md` 与 `docs/windows-operations.md`。

完成标准：Windows重启后无需人工打开终端即可恢复审核服务并自动检查QQ/NapCat就绪状态；结果未知的处罚不会重复；NapCat失败会明确告警“目标群自动监督中断”并转人工；所有演练有时间、结果和日志证据。

### T-405 GitHub Actions运行时维护

> **状态（2026-09-08）：已完成。** 提交 `3377279`，CI运行 `34203205684` 三项全绿且不再出现Node.js 20弃用警告。

- [x] 根据官方发布说明，将`actions/checkout`升级到v7.0.1、`astral-sh/setup-uv`升级到v10.0.1，并固定对应提交哈希。
- [x] 保持Ubuntu、Windows与干净运行时依赖三个任务的步骤和验收语义不变。
- [x] 推送后确认弃用警告消失，且三个任务继续全部成功。

完成标准：CI不再出现Node.js 20运行时弃用警告，没有放宽或删除任何质量门禁。

## Windows正式测试时间点

- **现在可以准备电脑，但不能开始整套验收**：完成Windows更新，选择并记录系统版本和CPU架构，准备专用目录、测试网络、隔离QQ账号和隔离群；不要写入生产密钥。
- **R-101已通过，开始W0**：在Windows 10专业版测试机跑干净安装、迁移和全部质量命令，建立Windows基线。**已完成（2026-09-04，决策D-011）**：该机新克隆私有仓库（main @ `c22b0c1`）并真实执行全部W0门禁（干净安装46包、迁移`init system_meta`、pytest 24 passed、mypy、ruff check/format、`WEB_PORT=8135`实际生效、非法配置拒绝启动、健康检查200），并记录Build 19045.6466、AMD64（12th Gen Intel i5-12400）、补丁状态（KB5071982/KB5071959/KB5072653，安全更新2026-07-18）、内存15.7GB、磁盘与网络（Realtek 2.5GbE有线，链路1Gbps）；全程未配置任何QQ或模型密钥。证据见 `HANDOFF.md`。
- **R-104、T-305和T-306通过后开始W1**：在Windows隔离群验证NapCat/OneBot全量消息与媒体的影子接收，外部动作保持0次。
- **W1与核心审核、案件、人工流程和多模态任务通过后开始W2**：验证NapCat入站到审核、两次违规、案件和报告的完整影子闭环。
- **T-307通过后开始W3**：在隔离群验证NapCat撤回、禁言、警告、幂等、保护角色和失败降级；T-304踢人仍是独立可选项。
- **T-404实现后开始W4**：验证锁屏、Windows Service、QQ/NapCat会话、重启、断网、更新、备份和恢复。
- **W1至W4通过后执行W5/T-403**：目标群先影子运行，再逐群开放NapCat撤回、禁言和警告；踢人默认由人工QQ客户端执行。

## 建议的任务分工

- Agent A：T-305，负责传输中立契约、Adapter seam和数据库增量迁移；当前只允许认领这一项。
- Agent B：T-306，待T-305独立审核通过后，负责NapCat/OneBot入站Adapter、媒体与影子运行器。
- Agent C：T-303，负责Windows隔离群NapCat主通道实机取证。
- Agent D：T-307，负责NapCat撤回/禁言/警告Adapter和安全编排。
- Agent E：T-304，仅在负责人明确启用后负责人工批准踢人。
- Agent F：T-404、T-403，负责Windows无人值守恢复与分阶段上线。
- Agent G：T-405，负责CI Action运行时维护；可独立安排，但不得占用T-305主线验收。
- 原T-001/T-102官方通道代码保留维护，当前不是目标大群上线前置。
- 最终整体审核：由项目负责人指定的主审Agent按照验收指标完成，不由单个实现Agent自行宣布项目完成。

## 待确认事项

- [x] QQ官方机器人在自建少人测试群的全量消息、撤回和禁言能力已验证；但个人认证无法开启任意群公开服务，不能覆盖目标大群（D-019）。
- [ ] 目标群清单、群规模、每群日均消息量和管理员权限。
- [ ] 专用NapCat管理员QQ是否已准备，是否已加入所有目标大群并获得可撤回/禁言的管理员权限。
- [ ] 目标大群是否允许在首轮上线前先影子观察，以及用于W1/W3实验的隔离群和普通成员测试账号。
- [ ] NapCat/OneBot验收版本、反向WebSocket地址、本机访问令牌与回退版本；真实值只在Windows本机配置。
- [ ] 所有者QQ、备用人工管理员QQ和允许审批的管理员名单。
- [ ] 首批群规、白名单、违规正例和合法反例。
- [ ] MiMo实际可用模型ID、账户区域、计费方式、每日预算和每个群是否允许发送脱敏内容；真实API Key只在本地配置。
- [x] 日报、周报和严重告警渠道：首版仅管理后台网页（D-015）。
- [ ] 人工确认"已踢出"后，是否要求第二位管理员复核。
