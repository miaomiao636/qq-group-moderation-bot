# 项目记忆索引

本文件是所有参与本项目的 Agent 的入口索引。开始任务前，先根据本文件读取对应上下文，不要依赖历史聊天或自行猜测项目状态。

## 强制阅读顺序

1. `AGENTS.md`：项目级强制规则、安全边界和验证要求。
2. `PROJECT_CONTEXT.md`：项目目标、最终架构、业务规则、身份映射和验收指标。
3. `NEXT_TASKS.md`：当前任务优先级、依赖、完成标准和建议分工。
4. `PROGRESS.md`：实际已完成、进行中和已知问题。
5. `DECISIONS.md`：已接受或被替代的技术决策及原因。
6. `HANDOFF.md`：最近一个Agent的修改、验证和遗留问题。
7. `README.md`：项目具备可运行内容后使用的安装和运行入口。

## 信息存放位置

### 项目背景、需求和架构

→ `PROJECT_CONTEXT.md`

包含：

- 项目目标和首批规模。
- NapCat/OneBot目标大群主通道，QQ官方机器人保留为可选/测试通道。
- 第一次/第二次违规处罚规则。
- 人工QQ客户端与NapCat双出口。
- 传输中立身份契约、OneBot数字ID和可选跨通道映射限制。
- 多模态处理、数据保存、报告、风控和验收指标。

### 当前进度

→ `PROGRESS.md`

只记录实际状态，不写未来计划。每次完成可验证交付物后更新。

### 技术决策

→ `DECISIONS.md`

记录影响架构、接口、数据库、权限、安全或部署的决定。决策改变时保留旧记录并标记“已被替代”，不要静默覆盖历史原因。

### 后续任务

→ `NEXT_TASKS.md`

包含任务编号、依赖关系、完成标准、待确认事项和建议分工。Agent开始工作前先认领一个任务，不擅自扩大范围。

### 最近交接

→ `HANDOFF.md`

当前Agent结束任务前填写：任务编号、完成内容、修改文件、验证结果、遗留问题和下一步建议。

### 项目入口

→ `README.md`

当前README已包含脚手架安装、配置、运行和测试入口；尚未实现的目录与业务能力不得当作现状，实际状态以代码、测试和 `PROGRESS.md` 为准。

### Windows运行与恢复

→ `docs/windows-operations.md`

包含正式Windows测试机决策、分阶段测试门槛、单机24×7运行、电源设置、开机自启、服务恢复、系统更新、状态恢复、健康监控、备份和NapCat人工接管要求。具体实施状态仍以 `PROGRESS.md` 和 `T-404` 为准。

### 群规与审核规则基线

→ `docs/group-rules.md`

负责人提供的群规结构化基线（允许内容/禁止类别/白名单原则/样本现状），是T-103规则引擎的直接输入。原始违规媒体仅存本机 `data/t002_media/`（gitignored），仓库内只有脱敏fixture。

### AI辅助审核、动态规则与反馈学习设计

→ `docs/superpowers/specs/2026-09-06-ai-rule-learning-design.md`

包含R-103正确性整改、后台可编辑版本化规则、MiMo/OpenAI-compatible文本与视觉适配、人工反馈标签、候选规则回放/发布、官方动作安全编排和验收边界。

## 当前权威状态摘要

- 文档初始化日期：2026-09-03。
- 当前阶段（2026-09-08）：D-019已将生产大群主通道改为NapCat/OneBot。T-305中立契约已完成主审整改并通过提交 `b3a107b` 的跨平台CI；下一项为T-306 OneBot入站与影子运行器。默认仍为 `ACTION_MODE=SHADOW`，真实NapCat、MiMo和Windows 24×7尚未实机验收。
- Windows测试：正式整机测试机已确认为Windows 10专业版（决策D-011，2026-09-04）；**W0门禁已于2026-09-04在该机实机通过（Build 19045.6466，AMD64），Windows基线已建立**；完整24×7验收在T-404及核心业务链路完成后进行。
- 主通道：目标大群使用NapCatQQ + OneBot 11；QQ官方机器人仅作可选/测试通道。
- 自动动作：T-106已有官方撤回、分级禁言和首次警告编排；T-305已建立中立契约与fail-closed路由，NapCat真实动作仍需T-306/T-303/T-307。
- 第一次违规：撤回、禁言1小时、警告。
- 第二次违规：撤回、禁言24小时、不警告、生成待审核案件。
- 踢人：必须人工批准；人工QQ客户端和NapCat是互斥出口。
- NapCat：生产大群的主传输和管理动作Adapter，不参与核心识别；断线或QQ要求安全验证时会中断自动监督，必须告警并转人工管理。
- 质量门禁：R-104已于2026-09-08关闭；提交 `2d8f405` 的GitHub Actions运行 `34186194703` 中Ubuntu、Windows与干净运行时依赖三个任务全部成功。CI同时提示所用部分Action的Node.js 20运行时已弃用，作为非阻塞维护项跟踪。
- 风险原则：不承诺或设计绕过QQ风控。
- 项目负责人期望：其他Agent负责具体搭建，主审Agent负责最终整体审核和验收。

如摘要与 `PROJECT_CONTEXT.md` 冲突，以 `PROJECT_CONTEXT.md` 中更新日期更晚且有决策记录支持的内容为准。

## 文档更新路由

- 需求、范围、架构或验收变化：更新 `PROJECT_CONTEXT.md`。
- 下一步工作、优先级或依赖变化：更新 `NEXT_TASKS.md`。
- 完成状态、测试结果或已知问题变化：更新 `PROGRESS.md`。
- 技术选择或架构原则变化：追加到 `DECISIONS.md`。
- 一次Agent任务结束：更新 `HANDOFF.md`。
- 安装、运行或命令变化：更新 `README.md`。
- 新增长期文档：在本索引中增加入口和用途。

## 协作纪律

- 项目实际文件和测试结果优先于文档；发现冲突先验证，再修正文档。
- 不把完整聊天记录、思考过程、临时日志或未验证猜测写入项目记忆。
- 不在Markdown、源码、测试、日志或提交记录中写入API Key、Token、密码、Cookie或私钥。
- 不把群成员原始敏感内容直接复制进文档；示例必须脱敏。
- 任何Agent不得自行宣布整体项目已验收；必须由指定主审Agent根据 `PROJECT_CONTEXT.md` 的验收指标统一审核。
