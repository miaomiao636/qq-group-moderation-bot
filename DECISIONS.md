# 技术决策记录

本文件记录影响架构、接口、数据库、权限、安全或部署的技术决策。决策改变时保留旧记录并标记“已被替代”，不要静默覆盖历史原因。

---

## 决策 D-001：主通道与执行出口

> 状态：**已被 D-019 替代**。本节仅保留2026-09-03时的决策背景。

### 决策日期

2026-09-03

### 决策内容

- 主消息通道：QQ 官方机器人，负责消息接收、官方撤回、禁言和首次警告。
- 审核服务：负责识别、复核、违规累计、案件、证据、审批和报告。
- NapCat/OneBot：仅作为可选身份镜像和踢人执行器，不参与核心审核判断。
- 踢人必须由白名单管理员明确批准，并在预览后完成二次确认。
- 人工 QQ 客户端与 NapCat 是同一案件的互斥执行出口，不能同时成功。

### 原因

- QQ 官方机器人是唯一受官方支持、可稳定撤回和禁言的通道。
- NapCat 属于非官方接入，存在风控与身份映射不确定性，不能承担核心判断。
- 踢人是高影响操作，必须人工批准，避免模型或群消息直接触发。

### 影响

- 所有处罚动作必须经过审核服务决策，NapCat 不参与识别。
- 身份映射缺失、冲突或过期时，强制回退人工客户端。
- 任何改变“人工批准才能踢人”“主通道使用官方机器人”的需求都属于架构变更。

---

## 决策 D-002：技术栈与运行环境

### 决策日期

2026-09-03

### 决策内容

- 开发语言：Python 3.12。
- 后端与管理接口：FastAPI。
- 数据访问：SQLAlchemy 2.x（ORM + Core）。
- 首版数据库：SQLite WAL 模式。
- 部署：审核服务运行于专用主机；如启用 NapCat，使用固定 Windows 11 专用机和没有重要资产的专用管理员 QQ。（注：Windows 11表述已被决策D-011替代，实际确认为Windows 10专业版）
- 管理后台：首版以服务端页面为主，不引入独立前端工程。

### 原因

- Python 3.12 提供现代类型语法与性能改进，符合项目技术栈要求。
- FastAPI 原生支持异步，适合高并发消息事件处理。
- SQLite WAL 满足首批 1–10 群、日 10,000 条消息的规模假设，零运维成本。
- 服务端页面避免过早引入前端工程复杂度。

### 影响

- 数据库迁移方案见 D-003。
- 规模或并发超过首版假设时，再评估迁移 PostgreSQL。

---

## 决策 D-003：依赖管理、数据库迁移与质量门禁

### 决策日期

2026-09-03

### 决策内容

- 依赖管理：使用 `uv`（Python 包与虚拟环境管理），`pyproject.toml` 作为唯一依赖声明来源。
- 数据库迁移：使用 `Alembic`，迁移脚本纳入版本控制。
- 测试框架：`pytest` + `pytest-asyncio`。
- 类型检查：`mypy`。
- 代码格式化：`ruff format`。
- 静态检查：`ruff check`。
- 日志：标准库 `logging`，结构化 JSON 输出，按级别与模块过滤。
- 配置：使用 `pydantic-settings` 读取环境变量，`.env.example` 只提交变量名与安全说明，真实凭据使用环境变量或系统凭据存储。

### 原因

- `uv` 提供快速、可复现的依赖解析与 Python 版本管理，符合 Python 3.12 要求。
- Alembic 是 SQLAlchemy 官方迁移方案，支持 SQLite 到 PostgreSQL 的平滑演进。
- pytest/ruff/mypy 是 Python 生态主流且低摩擦的质量门禁组合。

### 影响

- 所有安装、测试、类型检查、静态检查命令统一通过 `uv run` 执行。
- 真实凭据绝不写入源码、Markdown、测试、日志或 Git 历史。

---

## 决策 D-004：配置命名与运行模式

> 状态：**关于NapCat只在便捷模式启用的部分已被 D-019 替代**；`APP_ENV` 等环境命名仍有效。

### 决策日期

2026-09-03

### 决策内容

- 配置环境命名：`local`（本地开发）、`test`（测试）、`prod`（生产）。
- 通过 `APP_ENV` 环境变量选择配置，默认 `local`。
- 运行模式：`SAFE`（安全模式，默认）与 `CONVENIENT`（便捷模式，达到验收条件后启用）。
- 安全模式下 NapCat 关闭，踢人仅走人工 QQ 客户端。

### 原因

- 明确的环境与模式命名避免配置漂移，便于分阶段上线（见 T-403）。
- 安全模式作为默认，符合“先验证身份链路再启用便捷模式”的原则。

### 影响

- 所有环境相关配置通过 `APP_ENV` 隔离，真实凭据不进入仓库。
- 便捷模式（NapCat 踢人）必须在身份映射验证通过后由负责人确认启用。

---

## 决策 D-005：多模态与模型接入原则

### 决策日期

2026-09-03

### 决策内容

- 多模态工具：OCR、二维码解析、ASR、视觉模型、FFmpeg。
- 具体供应商需通过脱敏样本评测后决定（见 T-203）。
- 云模型只接收完成审核所必需的最小数据。
- 模型、规则、群消息或通用 Agent 都不得直接触发踢人。

### 原因

- 多模态供应商差异大，需以真实脱敏样本评测为准，不预先绑定。
- 最小数据原则降低隐私与成本风险。

### 影响

- 供应商选择在 T-203 完成前不写入代码。
- 所有模型调用必须有超时、限流、重试上限和熔断。

---

## 决策 D-006：Windows 24×7运行与恢复

> 状态：**关于NapCat不是核心恢复前置的部分已被 D-019 替代**；电源、服务自启、维护窗口和 `UNKNOWN` 动作规则仍有效。

### 决策日期

2026-09-04

### 决策内容

- 单机Windows部署允许关闭屏幕和锁屏，但不得进入睡眠或休眠。
- 核心服务使用Windows Service或等效监督器自动启动和有限重启，不依赖人工打开终端。
- Windows更新使用受控维护窗口；更新前暂停新处罚并备份，重启后健康检查通过才恢复自动动作。
- 重启前结果不确定的外部处罚动作转人工复核，不自动重放。
- NapCat恢复失败时关闭该执行出口，官方审核继续运行并回退人工QQ客户端。
- 详细运行和验收要求由 `docs/windows-operations.md` 和 `T-404` 管理。

### 原因

- 屏幕关闭不影响服务，但Windows睡眠会暂停CPU和网络，无法满足24×7实时审核。
- 自动重启只能解决进程存活，不能证明外部处罚是否成功；盲目重放可能造成重复处罚。
- QQ安全验证无法可靠无人值守处理，NapCat不能成为官方审核恢复的前置条件。

### 影响

- 所有有副作用的外部动作必须具有持久化幂等键和“结果未知”状态。
- 生产启动、更新、备份和恢复流程必须通过T-404演练后才能验收。
- 未完成T-404前，当前脚手架不得被描述为具备Windows无人值守生产能力。

---

## 决策 D-007：Alembic 为唯一生产建表路径（R-101）

### 决策日期

2026-09-04

### 决策内容

- 取消应用启动时通过 `Base.metadata.create_all` 建表的行为。
- 应用启动时校验数据库已通过 Alembic 迁移（存在 `alembic_version` 表且版本非空），否则拒绝启动。
- Alembic 是唯一的生产建表和升级入口。

### 原因

- `create_all` 会创建没有 `alembic_version` 记录的漂移数据库，导致后续迁移无法追踪。
- 校验迁移版本可防止在未迁移或迁移不完整的数据库上启动，保证数据一致性。

### 影响

- 所有环境（含测试）必须先执行 `uv run alembic upgrade head` 才能启动应用。
- 测试通过 session 级 fixture 对临时数据库执行迁移。

---

## 决策 D-008：启动入口读取 WEB_HOST/WEB_PORT（R-101）

### 决策日期

2026-09-04

### 决策内容

- 新增 `app/__main__.py` 作为实际启动入口，通过 `uv run python -m app` 启动。
- 启动入口读取 `WEB_HOST` 与 `WEB_PORT` 配置，确保 `.env` 中的端口设置实际生效。
- 开发热重载仍可用 `uv run uvicorn app.main:app --reload --host ... --port ...`，但需显式指定端口。

### 原因

- 原 README 要求修改 `WEB_PORT`，但 `uvicorn app.main:app` 不读取该配置，导致端口设置不生效。
- 提供明确的启动入口避免误导用户。

### 影响

- 生产运行使用 `uv run python -m app` 或由 Windows Service 管理。
- README 已更新启动命令与故障排查说明。

---

## 决策 D-009：配置校验与生产安全（R-101）

### 决策日期

2026-09-04

### 决策内容

- `APP_ENV` 仅允许 `local`/`test`/`prod`；`RUN_MODE` 仅允许 `SAFE`/`CONVENIENT`。
- `WEB_PORT` 范围 1–65535；保留天数必须 ≥ 1；`LOG_LEVEL` 仅允许标准日志级别。
- 生产环境（`APP_ENV=prod`）必须设置非空 `ADMIN_PASSWORD`，否则拒绝启动。

### 原因

- 非法配置会导致运行时行为不可预期，应在启动前快速失败。
- 生产环境空管理员密码是严重安全风险，必须拒绝。

### 影响

- 配置加载时进行类型与范围校验，非法值直接抛 `ValidationError`。
- 测试覆盖无效配置拒绝启动与生产密码校验。

---

## 决策 D-010：使用独立Windows电脑进行分阶段正式测试

> 状态：**Windows实机决策仍有效，W1至W5的阶段定义已被 D-019 替代**。

### 决策日期

2026-09-04

### 决策内容

- 使用一台空白Windows电脑作为项目正式整机测试环境；优先Windows 11 x64，首次测试前记录实际版本、CPU架构和补丁状态。（注：“优先Windows 11 x64”已被决策D-011替代，实际确认为Windows 10专业版）
- Windows验证分为W0基础兼容性、W1 QQ官方链路、W2核心安全模式、W3无人值守恢复、W4可选NapCat和W5最终分阶段验收。
- GitHub Actions或同等远程CI负责Linux/Windows代码兼容性门禁；真实Windows电脑负责QQ、NapCat、Windows Service、电源、锁屏、重启和恢复验证，两者不能互相替代。
- 核心安全模式可以在NapCat关闭时独立完成整体验收；W4只有在T-303、T-304通过且负责人明确批准后才执行。

### 原因

- GitHub托管Runner是短生命周期测试环境，无法代表持续QQ登录、交互式NapCat或24×7电源与恢复行为。
- Mac为ARM架构，Windows ARM虚拟机可用于辅助冒烟测试，但不能替代目标Windows实机的最终兼容性和稳定性证据。
- 分阶段测试能在较早阶段发现Windows路径、文件锁、权限和服务问题，同时避免未完成安全保护时接入真实处罚能力。

### 影响

- 进入每个Windows测试阶段前必须满足 `PROJECT_CONTEXT.md` 与 `docs/windows-operations.md` 的对应门槛。
- W0不使用真实QQ或模型密钥；W1至W4只使用隔离账号和隔离群。
- 任何Agent不得以“CI文件包含windows-latest”或“Mac本地测试通过”代替Windows真实证据。
- Windows测试记录必须包含代码提交号、系统版本、执行时间、命令或场景、结果和脱敏日志。

## 决策 D-011：正式测试机确认为Windows 10专业版

### 决策日期

2026-09-04

### 决策内容

- 项目负责人确认正式整机测试机为Windows 10专业版，替代D-010中“优先Windows 11 x64”的初始假设。
- 项目代码保持跨平台通用（Python 3.12 + FastAPI），不做Windows专用代码分支；远程CI的Windows任务继续只作为代码兼容性门禁。
- 首次W0测试时仍必须记录并补充：具体版本号（build号）、CPU架构、补丁日期、内存、磁盘和网络方式。
- 该机已由项目负责人通过CodeBuddy安装Node.js LTS、Git、Python 3.12、FFmpeg和uv（2026-09-04）；安装结果与W0门禁无关，W0以门禁命令真实通过为准。

### 原因

- 项目负责人实际可用的测试机为Windows 10专业版；等待Windows 11设备会无限期阻塞W0。
- 代码本身跨平台，操作系统版本只影响部署与验收环境，不影响领域逻辑。
- Windows 10专业版支持本项目所需的Windows Service、电源管理和本地安全策略。

### 影响

- `PROJECT_CONTEXT.md`、`docs/windows-operations.md`、`AGENTS.md`中“优先Windows 11 x64”的表述按本决策更新。
- W0门禁（干净安装、迁移、pytest、mypy、ruff、实际端口、配置拒绝）必须在Windows 10专业版实机上真实通过后才算建立Windows基线。
- 本决策不改变D-010的分阶段门槛，不降低任何验收标准；若该机后续无法获得安全更新，升级路径须在维护窗口中评估并补充决策。

---

## 决策 D-012：QQ官方机器人能力实测结论（T-001）

### 决策日期

2026-09-05

### 决策内容

以下结论全部来自2026-09-05在正式Windows测试机上的真实API实测（机器人AppID 1905561634，4个隔离测试群），不是文档推测：

- **域名与鉴权**：令牌签发 `POST https://api.bot.qq.com/app/getAppAccessToken`；业务API基址 `https://api.bot.qq.com`；请求头 `Authorization: QQBot <access_token>`（注意前缀是 `QQBot`）。实测旧组合 `api.sgroup.qq.com` + `QQey` 前缀返回401，适配器禁止使用。
- **全量群消息事件已生效**：经 WebSocket `wss://api.sgroup.qq.com/websocket`（intents=1<<25）收到 `GROUP_MESSAGE_CREATE` 事件，**包含不带@的普通消息**。事件字段：`id`、`group_openid`、`group_id`、`author{member_openid, member_role, bot, username, union_openid}`、`content`、`attachments[{content_type, filename, size, url, width, height}]`、`message_scene{source, ext[瞬时令牌]}`、`message_type`、`timestamp`。
- **身份映射关键事实**：事件中的 `group_id` 是32位不透明十六进制串，**不是真实数字群号**。D-001 的“官方身份与数字QQ无公开转换保证”结论维持不变，人工客户端回退与NapCat身份镜像设计不变。
- **撤回**：`DELETE /v2/groups/{group_openid}/messages/{message_id}`，机器人需群管理员身份；对普通成员消息实测成功；**对已撤回消息重复调用返回HTTP 200（接口幂等）**，动作重试设计可直接依赖。
- **禁言**：`POST /v2/groups/{group_openid}/restrict_chat_setting`，请求体 `members` 数组（单批≤20人），元素为 `op`（add/update/del）+ `member_openid` + `mute_expire_at`（RFC3339到期时间，非秒数）；最长30天；3600秒与86400秒实测成功；`op=del`+空 `mute_expire_at` 解除禁言实测成功。
- **保护角色为平台硬限制**：尝试禁言群主返回 HTTP 400 `40103004「目标成员为机器人/群主/管理员，不允许被禁言」`。项目“群主/管理员/白名单不自动处罚”约束在平台层有兜底，但业务层仍须自行拦截（白名单普通成员平台不保护）。
- **权限不足错误码**：机器人非管理员时撤回返回 HTTP 400 `40062003「无操作权限」`。
- **媒体可下载**：消息附件含 `url` 字段（图片/GIF另含 width/height），OCR/抽帧/ASR等媒体链路可直接下载。已实测收到的媒体类型：图片(jpeg)、GIF(image/gif)、语音(voice/amr)、视频(video/mp4)、文件(file/pdf)、转发记录（`[群聊的聊天记录]`文本骨架）。

### 原因

- T-001要求所有计划依赖的官方能力以实际结果为准，不能只依据文档假设。
- 撤回幂等与禁言到期时间语义直接影响动作层与状态机设计，必须先于T-102确认。

### 影响

- T-102 适配器按本结论实现：新域名、`QQBot` 前缀、`GROUP_MESSAGE_CREATE` 契约、到期时间制禁言。
- 动作层可安全重试撤回（幂等）；禁言计算 `mute_expire_at = now + duration`（时区 Asia/Shanghai）。
- 保护角色拦截仍需业务层实现，平台错误码作为最后防线记录审计。
- 未实测项：限流触发阈值与限流响应格式、媒体URL失效（不可下载）响应、群主消息撤回行为、分享卡片的事件形态。这些在T-102/W1中继续验证。

---

## 决策 D-014：不采购内容安全服务商，色情/暴力类采用零成本组合方案

### 决策日期

2026-09-05

### 决策内容

- 负责人明确**不考虑为内容安全识别付费**，不采用腾讯云内容安全等付费服务商（替代2026-09-05早前"由服务商评测集承担"的设想）。
- 已向负责人确认事实：QQ官方机器人平台**没有**面向机器人的内容鉴定API；平台自身风控不可控、无结果回调，只能作天然兜底。
- 色情/暴力/血腥/恐怖类采用零成本组合：①平台风控兜底；②文字黑名单/变体归一化（T-103）；③本地开源NSFW分类器（如opennsfw2类，T-203评测后决定是否可用）；④该类别默认**不自动处罚**——按项目"中等风险只记录+人工复核"原则处理。
- 验收口径相应调整：自动撤回/禁言精确率≥99%仍然成立（低置信度不自动处罚）；该类别召回率不做承诺，隐蔽违规依赖人工复核。

### 原因

- 零预算约束；自建色情样本库有法律与内容安全风险；平台无免费鉴定接口。

### 影响

- T-203 供应商评测范围缩小：不再评测付费内容安全服务，改为评测本地开源分类器的可用性（可选组件，不可用则该类别纯人工）。
- 群成员图片不再传第三方，隐私边界收窄为"数据不出本机"（排除后续接入云模型时的例外）。
- 日报需包含该类别的"仅记录待人工"列表，避免积压无人处理。

---

## 决策 D-015：报告渠道、D-014认可与隐私告知流程（负责人2026-09-05确认）

### 决策日期

2026-09-05

### 决策内容

- **报告渠道**：日报/周报/待人工清单**仅通过管理后台网页**呈现（`/admin/reports`），不实现QQ私聊或群内推送；调度由部署层（Windows计划任务/T-404）驱动。
- **D-014 正式认可**：色情/暴力类零预算方案（本地开源分类器+人工复核）获得负责人正式确认，不再是待定口径。
- **隐私告知流程**：机器人在测试群开始存档消息前，由开发Agent起草告知文案（说明消息会被本机存档用于审核），**经负责人审定后**在群内公示；审定前影子模式可以运行，但正式启用自动处罚前必须完成公示。

### 原因

- 负责人明确选择最简报告渠道；D-014获得正式背书消除验收口径悬置；存档前告知为合规最佳实践。

### 影响

- T-401 报告模块不再实现推送通道（大幅简化）；验收时报告以网页版为准。
- 影子测试启动前需完成文案审定与公示（负责人行动项）。
- 若未来需要QQ推送，须补充决策并评估私聊/群发接口能力。

---

## 决策 D-013：媒体附件时效、分享卡片形态与语音转写字段（T-001/T-002实测补充）

### 决策日期

2026-09-05

### 决策内容

基于2026-09-05下午媒体采集实测：

- **媒体URL有时效**：附件 `url` 含 `rkey` 签名参数，过期后返回 HTTP 400（实测：约数小时前的URL全部失效）。机器人必须在收到消息后**立即下载媒体**，不能长期囤积URL二次下载。
- **分享卡片事件形态确认**：分享卡片以 `GROUP_MESSAGE_CREATE` 文本形态到达，`content` 以 `[卡片消息] 小程序` 开头，携带 `摘要: [微信小程序]<名称>`、`source:`（来源小程序名）与 `source_logo:`（图标URL）字段。
- **语音附件含官方转写字段**：voice 附件除 `url` 外还有 `asr_refer_text`（官方参考转写文本）与 `voice_wav_url`（wav格式地址）。语音审核优先使用官方转写，与 PROJECT_CONTEXT 既定策略一致且实现成本大幅降低。

### 原因

- 三项均为实测发现，直接影响 T-102 适配器与 T-201/T-202 媒体链路设计，不记录会导致后续按错误假设实现。

### 影响

- T-102 适配器收到媒体消息后立即入队下载，本地归档与加密存储；URL仅作即时传输用途。
- T-202 语音链路优先取 `asr_refer_text`/`voice_wav_url`，外部ASR仅作兜底。
- 卡片审核解析 `[卡片消息]` 文本骨架中的 source/摘要字段。

---

## 决策 D-016：采用分层级联的远程AI辅助审核

### 决策日期

2026-09-06

### 决策内容

- 负责人允许提供远程AI API，小米MiMo作为首个候选；代码使用OpenAI-compatible供应商适配层，不把MiMo数据结构渗透到核心领域。
- 确定性规则、行为、OCR、二维码、哈希和官方ASR优先；不确定的文本与视觉内容分别进入 `TextModerator` 和 `VisionModerator`。
- 远程AI默认关闭并按群显式启用；只发送完成审核所必需的最小脱敏内容，真实密钥只由环境变量或系统凭据提供。
- 单个模型结果只作为软证据。只有确定性硬证据，或主模型置信度不低于0.90且由配置为独立的复核模型确认，才允许形成自动处罚建议。
- AI超时、限流、预算耗尽、熔断或响应不合约时降级为只记录；模型永远不能直接撤回、禁言或踢人。

### 原因

- 一个模型处理所有消息成本和故障半径过大，音视频仍需抽帧/转写；分层级联可以保留现有可解释规则并控制隐私、费用和延迟。
- 文本和视觉接口分离后可以先由同一供应商提供，后续又能独立替换或评测。

### 影响

- D-014的“零预算、不把图片发第三方”不再是全部项目的绝对约束：它继续作为AI关闭时的本地降级基线；只有负责人明确启用的群和数据类别可以使用远程AI。
- T-203已有复核门/成本/熔断基础不等于远程模型完成；实际接入、隐私最小化、缓存和评测由T-204完成。
- 真实MiMo型号、账户区域、预算和数据范围在本地配置与验收，不把API Key写入仓库。

---

## 决策 D-017：规则数据库版本化，反馈只生成候选规则

> 状态：**反馈真值和人工发布规则仍有效；NapCat仅作未来可选输入的定位已被D-019替代**。NapCat撤回通知可作为待标注事件，仍不自动成为违规真值。

### 决策日期

2026-09-06

### 决策内容

- 正式规则从Python常量迁移为数据库中的版本化结构数据，支持全局/群级作用域、草稿、模拟、发布、热更新和回滚；系统安全约束不可由群级规则关闭。
- 首版规则类型限定为关键词/短语组合、域名、二维码载荷、分享来源、媒体哈希、联系方式组合和行为阈值，不支持任意代码、SQL或不受限正则。
- 管理员确认的违规、正常和误判是权威反馈；未知原因撤回保持待标注，未撤回消息保持未标注。
- AI或统计任务可以生成候选规则并执行历史回放，但候选只能进入草稿，必须由管理员查看影响、确认并发布；不存在自动发布或自动提高处罚等级的路径。
- QQ官方机器人是否提供普通群管理员手工撤回事件尚未验证，首版以后台人工标注为权威；NapCat/Milky被动撤回输入须在T-303另行批准。

### 原因

- 把“管理员撤回”直接等同于违规会混入发错、重复、隐私和其他管理原因；把“未撤回”等同于正常会把漏审内容错误标成负例。
- 无人监督自动修改处罚规则容易形成自我强化误判和被群成员投毒，必须保留人工发布门。
- 版本化和历史回放允许在不重启的前提下安全更新，同时能追溯和回滚误伤规则。

### 影响

- 新增T-105动态规则与T-205反馈学习；每条审核判定必须保存规则版本和模型/提示词版本。
- 所有规则编辑、发布、回滚和反馈标注必须鉴权、CSRF保护并写审计。
- 完整数据模型、流程和验收标准见 `docs/superpowers/specs/2026-09-06-ai-rule-learning-design.md`。

---

## 决策 D-018：真实官方动作保持显式开关和影子默认

> 状态：**保留为QQ官方适配器的可选安全规则**。生产大群的NapCat动作模式由 D-019 及 T-305至T-307单独设计。

### 决策日期

2026-09-06

### 决策内容

- 运行时动作模式分为 `SHADOW` 与 `OFFICIAL`，默认 `SHADOW`。
- `OFFICIAL`只允许QQ官方撤回、分级禁言和首次警告，不包含踢人；启用前必须通过R-103、W1/W2对应门禁并由负责人明确确认。
- 外部动作前必须持久化意图和幂等键；数据库写入失败时不执行，结果未知时转人工复核且不盲目重放。

### 原因

- 当前影子链路尚有已复现的并发和误判问题，代码同步或服务重启不能意外开启真实处罚。

### 影响

- T-106实现官方动作编排，但完成代码不等于获准对真实群启用。
- NapCat踢人继续受D-019和T-304的独立人工审批约束。

---

## 决策 D-019：NapCat改为目标大群主通道，官方机器人降为可选通道

### 决策日期

2026-09-07

### 已验证的现实约束

- 项目负责人已完成QQ机器人平台个人开发者认证。
- 个人主体下，开发者后台的“允许机器人被添加到任意群聊使用（提供公开服务）”能力需要企业认证，当前个人主体无法开启。
- 机器人可在负责人自建的少人测试群中显示和使用，但在负责人仅拥有管理员权限的数百人、数千人目标群中，邀请机器人列表不显示该机器人。
- 因此，T-001/D-012证明的是“官方接口在可接入的小型测试群中技术可用”，不能推导为“个人主体可进入目标大群生产运行”。

### 决策内容

- **NapCatQQ + OneBot 11 改为目标大群的主消息与管理动作通道**，使用没有重要资产的专用QQ管理员账号和专用Windows 10电脑。
- QQ官方机器人保留为小群测试、可接入群和未来企业认证后的可选适配器，不再是生产大群上线的前置。
- 审核核心必须保持传输中立：官方事件和OneBot事件均由各自Adapter转换为统一消息契约，规则、AI、证据、违规阶梯、案件和报告不得依赖某一供应商的字段。
- 主通道切换采用增量迁移：先增加中立ID与兼容层，再接入NapCat，稳定后才停止核心代码读取官方专用字段；不原地改名或删除旧数据列。
- NapCat主通道群直接使用OneBot数字 `group_id` 和 `user_id`，不需要官方OpenID到数字QQ号的映射。只有同一群同时配置官方与NapCat时，才需要受控的跨通道映射。
- 每个群默认只能有一个激活的消息入口和一个自动撤回/禁言/警告动作出口，避免双通道重复判定和重复处罚。
- NapCat可在高置信决策后执行撤回、禁言和首次警告；默认必须是影子模式，完成隔离群实机验收后才能按群开启真实动作。
- **踢人规则不变**：不得由规则、AI、群消息或通用Agent直接触发。人工可在QQ客户端执行；如启用NapCat踢人，必须经白名单管理员预览、一次性确认和执行前角色复核。

### 替代方案

- **企业认证后继续官方主通道**：官方支持风险更低，但需企业主体、审核与可能的额外成本；当前不选作为生产前置。
- **纯人工管理**：账号风险最低，但无法满足24×7及时撤回目标。
- **NapCat只提醒、所有动作人工执行**：可作为首个生产阶段的降风险模式，但垃圾信息暴露时间会增加。

### 影响和风险

- D-001中“QQ官方机器人为主通道”、D-004中“默认关闭NapCat”、D-006中“NapCat失败不影响官方审核”和D-010的旧W1至W5阶段定义被本决策替代。
- 官方机器人现有Adapter、回归样本和T-106动作编排保留，不删除；以后可在可接入群或企业认证后继使用。
- NapCat变为核心运行依赖后，掉线、QQ安全验证、版本不兼容或账号限制会同时中断自动监督和处罚。系统必须立即告警、停止自动动作并转人工监管，不得将“进程存活”误报为“群消息正常接收”。
- 项目不承诺NapCat不会触发QQ风控，不实现设备指纹伪装、代理轮换、随机模拟真人或自动解决安全验证。
- 实施任务调整为 R-104、T-305、T-306、T-307、T-304、T-404 和T-403，详见 `NEXT_TASKS.md`。

---

## 决策 D-020：传输中立契约与 expand/migrate/contract 数据迁移（T-305）

### 决策日期

2026-09-08

### 决策内容

T-305 已在分支 `feature/t305-neutral-contracts` 实现并完成主审整改；提交 `b3a107b` 的CI运行 `34200777456` 三项全绿，已验收：

- **中立身份键**：`provider + external_group_id + external_user_id + external_message_id`。`Provider` 字面量集合为 `qq_official | onebot`；审核核心只识别该集合，不感知任何一方原始事件结构。
- **契约上移**：`StandardMessage`/`Sender`/`Attachment`/`ShareCardInfo`/`ActionResult` 及 `MessageSource`/`ModerationActionClient` 两个 seam 协议定义在 `app/core/contracts.py`；`app/adapters/qq_official/contract.py` 与 `actions.py` 变为兼容再导出（既有导入方与全部官方 fixture 回归测试不受影响）。审核、案件、动作意图、报告模块顶层**零**供应商 Adapter 导入；官方客户端仅由组合根 `app/actions/official_wiring.py` 惰性构建。
- **expand 阶段兼容**：`group_openid`/`member_openid` 等旧字段在领域模型与数据库中**全部保留**，与中立字段构造时双向同步、写入时双写；Alembic 迁移 `b8e2f6a4c1d9` 以 `batch_alter_table` 加列（SQLite 安全）并回填历史数据（provider='qq_official'，幂等只补空），可完整 downgrade 回滚。旧列的删除（contract 阶段）由独立任务另行评估，本任务不做。
- **镜像不等于跨通道等价**：`provider="onebot"` 的消息其镜像字段虽填充数字ID字符串，仅为过渡期读取兼容；**不得把 OneBot 数字 ID 伪装成 OpenID**，不得调用官方 API。
- **按群路由**：`group_provider_routes` 以 `message_provider + external_group_id` 为联合主键，避免两个provider中相同字符串冲突。未配置官方群仅为兼容历史链路保留官方默认；未配置OneBot、非法路由和无可验证身份映射的交叉provider均fail-closed。
- **动作模式隔离**：`ACTION_MODE=OFFICIAL` 只能调用QQ官方Adapter。T-305不得通过注入客户端提前启用OneBot真实动作；OneBot路由在T-307前只记录SKIPPED。
- **主审整改**：违规累计/案件复用与动作幂等已纳入provider及中立身份；契约增加通用`MessageSegment`；通用去重上移`app/core/dedup.py`；新增纠正迁移`d4f7a9c2e601`。
- **测试封闭性**：`tests/conftest.py` 强制 `AI_ENABLED=false`——修复了开发机 `.env` 配置真实 AI 时测试会真实外呼 MiMo、导致判定非确定（限流/超时使 `record_only`/`allow` 漂移）的问题。项目规则本就要求 AI 测试只用固定假响应。
- **未来 NapCat Adapter 的接入面**：入站实现 `MessageSource`（T-306），出站实现 `ModerationActionClient`（T-307，不含踢人）；持久去重键的 `provider + self_id + message_id` 演进留待T-306在真实OneBot契约下完成。

### 原因

- D-019 确定目标大群主通道为 NapCat/OneBot，但现有数据库与领域模型全部是官方 OpenID 命名；原地改名或删列会破坏历史数据与在线升级路径。
- 官方 fixture 与既有回归测试是资产，必须零改动继续通过；expand/双写是同时满足"新链路中立"与"旧链路不破坏"的最小路径。

### 影响

- 核心模块（moderation/cases/actions/reports/core）不导入任何 Adapter，可被官方与 OneBot fixture 共同驱动（已有自动化测试证明）。
- 数据库新旧行混合期间读取口径以中立列为准，并兼容仅有旧镜像列的行（有自动化测试证明）。
- T-306/T-307 直接在本 seam 上实现，不需要再动核心契约；contract 阶段（删旧列）另立任务并需再次评审。
- 测试套件不再依赖真实远程AI，跨平台 CI 结果不受开发机 `.env` 影响。

---

## 决策 D-021：NapCat/OneBot 反向WebSocket入站与就绪状态语义（T-306）

> 状态：已验收。主审修复提交 `444b368` 的远程CI运行 `34219858155` 在Ubuntu、Windows和干净运行时三项全绿。

### 决策日期

2026-09-08

### 决策内容

- **传输形态**：OneBot 11 反向 WebSocket（NapCat 主动连入）。入站端点随管理后台同进程（FastAPI）挂载，路径 `ONEBOT_WS_PATH`（默认 `/onebot/ws`），**默认关闭**（`ONEBOT_WS_ENABLED=false`）。
- **接入安全（fail-closed）**：启用必须设置非空 `ONEBOT_ACCESS_TOKEN`，WebSocket与详细状态端点只接受 `Authorization: Bearer` 并常量时间比较；URL查询参数令牌被拒绝，避免进入访问日志。`WEB_HOST` 只允许回环、RFC1918 IPv4或IPv6 ULA，明确拒绝公网、`0.0.0.0`和`::`。
- **事件结构校验在入口完成**：群消息必须具备 `self_id + message_id + group_id + user_id` 四要素，缺失即拒绝并计入 `invalid_total`，不入队、不落库；JSON 非法帧计数并容忍（连续50帧非法才断开）；notice/请求/私聊仅计数不处理。
- **解析边界**：`app/adapters/onebot/parser.py` 为纯转换（无 I/O）。文字/图片/GIF/表情/语音/视频/文件/回复引用/合并转发/JSON卡片/链接分享映射到 T-305 中立契约；**未知段保留"段类型名+数据键名"元数据**转为 `kind="unknown"` 中立段；CQ码字符串形态因媒体结构不可靠还原同样标记降级；合并转发内容不在事件内，转人工。
- **不可解析内容守卫（核心级，通道中立）**：流水线对 `kind` 或任意段为 `unknown/forward_record` 的消息强制 `record_only` 转人工——既不判正常，也不作为处罚依据；影子记录 detail 附带中立段摘要。
- **媒体下载**：复用 R-102-4 统一媒体安全实现，且持久化认领成功后才执行下载，重复事件不重复下载。下载失败后不得回退供应商原始文件名；流水线解析前验证文件名为单层名称并在解析符号链接后仍位于受管媒体目录，路径逃逸一律按媒体缺失转人工。
- **去重键升级**：持久化键为 ``provider + self_id + message_id``（形如 `onebot:{self_id}:{message_id}`）；缺失`self_id`拒绝处理。影子表内部`message_id`保存组合事件键，新增`external_message_id`保存原始消息ID，避免不同OneBot账号同ID相互覆盖；官方路径行为不变。
- **影子强制**：OneBot Adapter 包内不存在任何管理动作实现（撤回/禁言/警告/踢人），有 AST 测试守护；OneBot 事件不产生任何动作意图；真实 NapCat 动作留待 T-307 独立开关。
- **就绪语义与隐私**：`ready`要求已连接、QQ登录态online、心跳新鲜和队列未满；心跳`status.online=false`或`good=false`立即降级。鉴权后的`/onebot/status`提供含每群最后事件的详细证据；公开`/healthz`只提供不含QQ号、群号和内部错误的聚合状态。
- **背压不丢事件**：WS 接收循环将事件放入有界队列（`ONEBOT_QUEUE_MAX`），队满时阻塞接收形成 TCP 背压，不静默丢弃；进程崩溃时未处理事件会丢失，由 T-303 实机验证消息缺口与 NapCat 侧重推能力。

### 原因

- 反向WS使 NapCat 主动连入本机，避免暴露出站服务端口；同进程挂载复用现有绑定/健康检查设施。
- 入口结构校验 + 解析器纯函数化，使全部契约测试无需 NapCat 真机即可运行（CI 友好）。

### 影响

- T-303（Windows隔离群）可直接按 `docs/windows-operations.md` W1 标准 using `/onebot/status` 取证。
- T-307 的 OneBot 动作 Adapter 只需实现 `ModerationActionClient` 并经 `group_provider_routes` 按群显式开启；本任务的影子路径不动。
- 队列进程内、非持久化：重启窗口的消息缺口属已知风险，须在 T-303/W1 实测量化。

---

## 决策 D-022：条件复核、受控 Agent、持久接收和交付边界（R-105）

日期：2026-09-10。依据：负责人明确选择“第二模型仅疑难调用”，允许 Agent 自然语言协助管理，并授权主审修复 PR #5 全部可实施项、验证后合并。此授权不等于已开启真实群动作，不替代 Windows 验收。

- **条件复核**：确定性策略选择是否调用第二视觉模型。正常不复核；主模型置信度灰区、needs_review、规则冲突、未知类别或故障进入复核。明确广告/诈骗、主≥0.90、无复核标志与本地冲突可直接形成高置信建议；疑难必须主≥0.60、独立复核≥0.90、同类别且无残留冲突，才可升级。阈值来自配置且 low<direct。次模型不能与主模型同 ID；不同 ID 也不能证明真实供应商独立性，部署方须核查别名和路由。分歧、复核失败/缺失、同模型、正常复核、保护对象均不回退单模型处罚。模型只能产生结构化证据，实际处罚仍经安全编排。
- **缓存与费用**：缓存隔离消息provider/群、模型、角色、媒体、prompt内置版本与判定阈值，命中仍保持主/次来源；禁止旧缓存把双模型偷换为单模型。prompt升级v4。记录实际主/次调用、缓存、额度阻断、token、策略版本；日调用硬上限默认1000，从日志恢复。供应商未配置价格时费用“未核算”，金额0不代表免费；供应商账单为费用核验来源。
- **学习是人工维护闭环，不是在线训练**：保留人工标签，但停止将单条/最近标签即时放入所有请求的通用提示词。自动挖掘至少3条独立消息、2名成员，随后草稿、回放、人工预览/发布、版本化与回滚。负责人明确制定规则可直接建草稿并发布，不受自动挖掘样本数门槛约束。未撤回/未知撤回不是负例；跨provider同raw群号的旧候选与旧规则作用域存在歧义时拒绝泛化/处罚，不能猜身份。
- **通用Agent权限**：管理员密码与Agent读/写令牌分离；读令牌不能写，写令牌默认也仅project:read，按scope最小授权。有权限写不等于能自行批准。完整动作、全部参数、provider/群、发起身份、状态版本和过期时间绑定DB计划；真人通过后台会话+CSRF预览批准，Agent仅执行自己已获批且未变化的计划；原子单次消费，重放/换参数/跨群/过期/状态变化拒绝。开动作、改路由、发布/回滚规则、解除急停均受该边界约束。安全方向的激活急停即时执行并审计，不等待批准。自然语言不直接成为任意数据库或系统命令。
- **身份及动作**：新ProviderGroupSettings使用provider+external_group_id；旧GroupSettings保留回滚，迁移关闭历史动作位，禁止隐式跨provider继承。路由显式动作所有者，不能双出口。连接绑定ONEBOT_SELF_ID（真实动作必填），单账号单连接，旧socket响应不能确认新连接动作。OneBot严格整数retcode=0且status=ok才成功；async、缺字段、矛盾、超时与发送后断线UNKNOWN，不能盲目重试。
- **急停与持久化**：急停状态写共享SQLite，动作链每个外呼前重新读，不使用仅Web进程内变量。WS事件校验后先提交durable inbox，再唤醒worker；原子领取、可续租、最多3次退避、容量限制、终态保留/清理。只承诺已提交事件恢复，不承诺上游未发出/断线期间消息无损。完成payload立即清除，未完成原始数据30天、元数据180天默认清理。补足并替代D-021内存队列条款。
- **部署边界**：保持单机、单Web/OneBot实例、SQLite，不新增分布式架构。ACTION_MODE=OFFICIAL保留历史名称作为全局真实动作门禁；实际Adapter由provider独立路由，OneBot还必须开ONEBOT_ACTIONS_ENABLED、固定账号和真人批准按群动作。OneBot-only无需无关QQ官方凭据；官方Adapter仍缺凭据拒绝，不得伪装通道。本条替代D-020/AGENTS中T-307实现前限制，不授权跳过W3。
- **交付边界**：P1-13真实T-303证据、P1-14独立W2效果必须由Windows实际取证，不能通过写文档关闭。代码可以安全默认关闭地合并；W1–W4通过后才进W5试运行。D-015页面通知仍非主动到达人的告警；若要无人值守商业交付，客户必须确定独立通知渠道与接管人并演练。验收标准、排期和取证见docs/windows-delivery-checklist.md。

原因：小公司的维护成本应花在可控处罚、少误伤、能发现失效和人工接管，而不是为每条消息付双倍模型费或堆叠平台。代价是疑难转人工、单实例上限、真机证据不能在开发机代做。后续以真实人工工作量、漏报与误伤决定扩展，不预设在线训练或全自动Agent运维。

主审交叉复核补充：不可审段/缺失附件/未审非图媒体在所有AI之后仍保留最终人工veto；模型confidence拒绝布尔、字符串与非有限概率；后台备份使用配置库的SQLite在线快照（含WAL）并quick_check，不复制主文件假装可恢复；计划清理提供CLI和最近执行状态，Windows任务本身仍需实机注册。ONEBOT_ACTION_STAGE默认recall_only，full才允许禁言/警告；这是全局阶段配合逐群开关，变更须本机负责人确认并重启，不新增Agent越权入口。仅撤回也保留有效违规历史。自动候选使用最新明确标签，撤销真值后未发布候选失效，已发布规则仍由真人回滚。

最终一致性补充：人工明确正常/误判经Shadow显式中立身份映射撤销有效违规并同事务审计；无映射不猜、不自动解除禁言或关闭案件，positive不恢复历史处罚。动作开始及每次外呼前终检纠错，真值以新增记录ID排序，避免时钟回拨。SQLite写事务串行分配违规阶梯；同provider/群/成员完整动作链在单运行实例内串行，防止24h禁言被迟到1h覆盖；等待30秒超时安全SKIPPED并审计转人工，不额外累计，取消/异常释放空闲锁。跨进程边界依赖OneBot独占runtime锁，不宣称支持多实例。补登记既有历史表/列/索引，新增迁移与metadata一致性回归，禁止把未登记误当作可以删库字段。

---

## 决策 D-023：管理员主动通知、独立故障监测与接手（R-106）

日期：2026-09-10。依据：负责人要求把此前讨论的QQ管理员通知、独立故障通知和外部健康探针方案加入项目。仅替代D-015“告警只在后台展示”和D-022引用该限制的通知部分，不改处罚、学习、Agent权限及实机交付边界。

- 固定QQ管理员群接收新待审案件/UNKNOWN动作提醒、默认15分钟疑难摘要；邮件承担故障/恢复、QQ失败回退和15分钟未接手的备份升级。确认接手只保存真人身份与审计，不能批准踢人或执行处罚。成员群中的违规警告仍走原动作编排。
- 所有通知默认关闭；只有负责人本机独立配置并启用才外发。通知可在SHADOW启用，但处罚调用仍为零；验收分开统计二者。固定收件人/群/心跳地址不由群消息、模型或通用Agent指定，不提供任意内容发送API。
- 沿用单机SQLite新增通知、投递和状态三表，迁移 `d3f5a7b9c111` 仅增表。通知先提交再外发、原子领取、发送前确认/租约终检；120秒租约过期/发送后不确定均UNKNOWN不重放。确定未发送的失败最多3次60/300秒退避。每批最多4条，给每个启用通道预留1个名额后按FIFO补齐，避免单通道积压挤掉独立故障通知；同通道串行、QQ/邮件独立并行，批次故障取消并等待兄弟任务，不阻塞审核主流程。
- 初次启用摘要存量，不逐条补历史。已确认/恢复的过期通知可随判断保留期清理；UNKNOWN来源与QQ回退最小去重标记独立保留，避免清理重新触发已接手事项。尚未确认/恢复的事项不静默丢弃。
- SMTP使用校验证书的STARTTLS或SSL、固定名单及总超时。发送结果未知不等于失败未发，邮件接受不等于人已读。通知仅包含内部编号、计数和固定说明，不发原始群消息、QQ号、媒体URL或模型回答；异常日志不含凭据/目的地。
- 独立Windows计划每分钟访问带专用只读令牌的回环 `/health/ready`；健康才向固定Healthchecks HTTPS UUID地址发成功信号，不健康发失败信号。外部收件人及约3分钟缺失检测由部署方单独配置，不能依赖同一台已关机电脑自报故障。应用内15分钟升级不能覆盖整机停机，外部需同时配置首位/备份与提醒策略。
- 健康检查包含DB读写、QQ就绪、审核/通知任务、积压和已知邮件投递故障；正常静默不判故障。AI连续失败按同模型/角色聚合，缓存/静默/别的模型成功不恢复，只有新非缓存成功启动恢复观察，不为健康监测额外花模型调用费用。
- 本轮不创建外部账号、不发送真实通知、不注册Windows任务、不修改生产配置；默认关闭可回退。页面功能测试不等于视觉验收，实际送达/接手/升级/关机失联/恢复须按 `docs/proactive-notifications.md` 在Windows验证。

理由：让需要负责的人能主动发现项目失效，比增加模型数量更能补足无人值守缺口；保持现有技术栈，不增加消息队列服务或运维平台。限制：外部服务可达性、邮箱垃圾箱/退信、QQ登录和实际人工响应必须现场验证；本机通知不能独立保证端到端SLA。

---

## 决策 D-024：当前部署原始内容仅保留 15 天（R-107）

日期：2026-09-12。依据：主审说明第 16–30 天再次违规可能缺少首违原图/快照后，负责人明确选择“全部原始内容仍只保留 15 天，接受首违内容缺失”。本条更新当前部署数据生命周期取舍，不改变 30 天累犯阶梯、误判纠错、人工踢人或真实动作授权边界。

- 普通消息与违规原内容均按已报告的 RAW_RETENTION_DAYS=15 保留，不新增违规证据 30 天豁免。DECISION_RETENTION_DAYS=15 仍为部署配置；软件模板的 30/180 默认值保留，主审不读取或改写生产配置。
- 违规计数、provider/群/成员/消息映射、反馈标签与案件关系等最小业务元数据可继续用于 30 天累犯与误判撤销；不能声称所有记录都在第 15 天删除。首违原内容过期后的案件要如实显示证据已清，不能从备份静默取回再延长保存。
- 受管影子判定/反馈/缓存等原文副本纳入清理边界；无近期有效来源支持的未确认自动候选原句亦清除，保留 ID/关联并禁止继续复制。已人工复制/发布的结构化规则是业务配置，不因源样本过期而自动删除/回滚。
- 媒体快照、数据库备份、手工 W2 标注文件和其他导出同样含原内容，Windows 负责人须盘点并落实期限。未经核对不递归删除未知目录；本轮不在生产执行任何清理，不宣称备份策略已完成。
- 清理权限/I/O 失败必须显示 failed；可能已经部分删除，不能声称数据库回滚恢复了文件。当前 04:00 登录用户任务不等于无人值守清理保证，漏跑、失败及恢复仍需 W4 实测。

代价：减少原内容长期暴露，但第 16–30 天人工复核/申诉可能无法重看首违；自动累犯计数不因此失效。延迟门槛及条件二审保持不变，不能把该保留策略决定当成延迟或 W3 放行授权。

## 决策 D-025：负责人授权开启真实动作梯度试点（recall_only）

> 2026-09-14 状态补记：Windows Agent 回报已关闭 ONEBOT_ACTIONS_ENABLED；主审本轮未访问生产核验。D-025 是历史试点授权，不是 R-108 修复后自动恢复授权，当前边界见 D-026。

日期：2026-09-12。决定：负责人明确授权"现在就开始真实动作（自动撤回），边跑边收集样本"。据此将
ACTION_MODE 切换为 OFFICIAL、ONEBOT_ACTIONS_ENABLED=true、ONEBOT_ACTION_STAGE=recall_only，
并解除 runtime_emergency_stop（由 Windows Agent 执行，服务重启加载）。

- 边界：仅撤回（recall），**不禁言、不踢人**；stage 升级需再次授权。
- 分母保护：群级「真实动作」开关默认全关，仅负责人逐群显式授权后生效（当前 0 个群开启）。
- 背景：主审 R-107 明确「W3 真实动作未放行」；本条为负责人在生产试点上的明确授权，
  与 W2 新样本复测并行推进，不替代 W3 演练记录。
- 已知风险：AI 误判可能撤错消息（有通知/后台可查/急停三重兜底）；NapCat 高频动作的账号
  风控由小规模起步缓解；video 类型未参与 W2（NOT_TESTED）。
- 撤销方式（任一即停）：后台急停按钮 / .env EMERGENCY_STOP=true / ONEBOT_ACTIONS_ENABLED=false。

## 决策 D-026：R-108 复验边界与统一原内容期限

日期：2026-09-14。依据：负责人明确要求复审 `7d1f761`，可本地修复的直接修复，剩余安排交给 Windows Agent；并已确认监测一百多个群时，原始内容不必长期留，长期保留有效规则与测试沉淀。

- 统一保留决定细化 D-024：所有原内容及可恢复副本按原消息时间最多 15 天，不因复制、重下载、导出、备份或验收用途重新计时。替代旧“验收原件永久留、W2 等复测后清、仅留两份备份”的冲突例外；接受 16–30 天首违原件缺失。人工确认的规则/版本/原因/回滚、合成或充分脱敏回归与统计可长期留；最少违规计数、案件与纠错关联独立支持 30 天累计及未结案需要，并单独定义到期。政策确定不等于生产清理已完成；不得按未知目录批量删除。
- 本轮不合并、不部署或重启 Windows、不恢复真实处罚、不重写或强推 Git 历史、不删除生产原始内容。修复使用合成数据本地验证，报告和补丁交接不代表 Windows 全部通过。历史重写仍需另行明确授权及影响盘点。
- 技术实现沿用既有优先级：本地确认的办证广告例外不能被后续 AI 的同类别结果重新开启处罚；诈骗、色情、暴力和显式动态规则不因此豁免。发现的混合词首项分类错误按原严重类别修复，不新增敏感词或降低处罚门槛。旧提示词的“绝对放行”与严重类别优先级仍须负责人最终统一，不能用技术修复代替业务授权。
- 紧邻判断以真实发送时间和完整前驱为准。前图仍在下载、AI 或持久队列中时，广告文字配对资格未知，保守转人工、不自动补罚；不是授予白名单或保证人工已接手。同秒/缺时间不猜先后，不据此授予白名单。已消费绑定必须跨源图重试保留。
- 不把处理中消息提前写成完成的影子判定，避免破坏通知 ID 游标和统计。未完成队列以现有 durable inbox 为恢复真值；活跃 worker 仅持有无原文的短期排序元数据，结束/取消按 token 清除，不替代持久化，也不支持多实例并发部署。
- Fake-IP 兼容只连接同次 DNS 验证的具体 IP，限明确媒体主机并逐跳校验；真实 TUN/透明代理兼容性需 Windows 证明，失败转人工，不能退回未固定域名连接或扩大整域白名单。

结果与剩余验收入口：`docs/pr7-r108-review.md`。一百多个群是当前交付目标，旧 1–10 群/日 10,000 条仅为最初假设；须测量峰值入站、积压、端到端延迟、费用和存储，再决定容量及分批放量，不凭测试数量宣称支持目标规模。

## 决策 D-027：R-109 数据管理与已发布规则保护

日期：2026-09-14—15。状态：本地实现验证通过，Windows尚未验收。依据：负责人要求检查edf618a新增功能，直接修复可确认问题，并与R-108合成交接；edf提交记录载明新增归档15天、归档90天后清非必要内容、AI明细90天、未处理候选30天等功能。本轮保留这些新功能意图，技术修复不视为新的生产/删除授权。

- 归档只改变列表可见性，限真正CLOSED且有结案时间。90天逻辑清理须检查合法时间链、所有案件JSON/反向case_id、记录年龄与精确身份；异常/共享暂缓并计数。保留最小违规行/ID、案件编号、必要审批状态链，以免SQLite复用ID把旧审计串到新对象；原文独立TTL不因暂缓延期。lifecycle_purged_at保证幂等和恢复边界；恢复不重开、不还原原文，下次维护仍可能归档。
- AIUsageLog是可能含群/消息标识的调用明细元数据，90天删除，不是匿名长期汇总。候选过期仅追加原因；已驳回候选清内容但保留PURGED编号占位，已复制草稿关联不由列表清理删除；占位不可重新复制/复活。同批有效反馈可以另产新候选，本轮不新增永久排除规则。
- 已发布规则版本不可原地修改。草稿显式enable/disable；ACTIVE启停全量复制为新DRAFT，重复/并发复用；ARCHIVED只能走已有回滚预览。仍由人工预览批准发布后改变运行规则，紧急止损用现有急停/动作开关，不用未发布草稿替代。原文来源不额外写入启停审计。
- D-024/D-026原消息15天上限不变，不因案件90天或候选30天获得原文例外；所有副本源时间、最终最小元数据到期/匿名化和容量仍待工程验收。规则理由/训练沉淀不得成为复制完整原话的永久容器。
- 新补丁没有再增迁移；edf自身新增af66bc20f1ed必须验证。schema downgrade丢归档标记，代码回退不还原任何已经删除的内容。本轮没有生产清理、部署/重启、动作恢复、推送/合并或Git历史重写。

理由与取舍：延续现有SQLite/服务端页面，保留编号和必要关联比物理删除后重建关系更安全；草稿复核保住AI学习的版本与人工批准链。代价是最小元数据继续增长、异常关联可能整体暂缓清理、启停多一步发布确认；需测百群容量，不以破坏历史换取表面“删干净”。当前统一交接：`docs/pr7-r109-review.md`。

## 决策 D-028：B-2 严重类别策略与人工反馈类别核对（R-111 / v13.1）

日期：2026-09-15。状态：本地实现验证通过（修复提交 `feb821c`），Windows 未部署本轮修复；新 SHA CI 待 PR #7 检查页。依据：负责人在对话中直接确认方案 **B-2**（主审 R-111 复验报告已核认该确认有效），并强调"办证相关保证通过、不判违规"。

- 行为矩阵：严重类别（诈骗/色情/暴力违禁品）**不因校园墙特征或办证词豁免**；本地规则高置信命中 → 直接违规 + 处罚建议；高置信确认 + 独立二审消疑 → 照常进入处罚流程；**低置信疑似严重类别 → record_only 保留类别转人工（不处罚、不撤回、不静默放行）**；纯办证与办证混合低置信疑似 → record_only 不处罚。
- 主审 R-111 复验（对 `4c57b6c`）：B-2 主要行为通过（16 端到端场景）；新增 P2——混合内容转人工时 ad 类别覆盖严重疑似类别（旧逻辑先取 local/首个可用结果，severe 仅 None 时补救），且该类别经 pipeline 持久化进入人工反馈表单隐藏字段，可能影响候选规则类别。
- 本轮修复（`feb821c`）：转人工分支**严重疑似优先**选类别（多严重类别按置信降序 + 固定序 fraud<porn<violence 确定性选择）；**类别与置信度同源**（不得把本地广告高置信度充当严重疑似置信度）；verdict 恒 record_only、处罚建议恒空——只改类别标记供人工核对，**不改变处罚判定**；主审建议第 4 条同步落地——反馈表单类别改可核对/纠正下拉（默认人工上次保存值、其次系统判定值），提交路由白名单校验、非法值回退 other；反馈类别仍仅作人工确认的候选规则参考，不自动发布。
- 边界：不部署、不合并、不重启 Windows、不恢复真实动作、不清理生产数据、不重写 Git 历史；本轮修复是否部署由负责人在主审复验新 SHA 后决定。

## 决策 D-029："15 天全副本工程"——登记式副本自动清理（2026-09-15）

日期：2026-09-15。依据：负责人要求"那些不在自动清理范围的数据也要进行一个管理、自动定期清理"（D-024/D-026 "所有原内容副本按原消息时间最多 15 天"政策的工程收尾）。

- **范围与原则**：媒体清理新增"登记表"机制（`app/reports/cleanup.py::_MANAGED_COPIES`）——**只清理登记在册的路径**，未知目录永不触碰（延续 D-026"不得按未知目录批量删除"）；新增副本目录必须登记（走代码审查）。
- **当前登记**：`media_snapshot_*`（15 天）、`t002_media`（15 天）、`backup_shadow_*.json`（15 天）、`_dbg_tmp`（3 天）、`media/_frames`（15 天残留）、`backups`（15 天 + **保底最新 1 个备份集**——恢复生命线）、`purge_drill`（7 天）、`loadtest`（7 天）。待标注的 `sample_pool` 不登记（使用中，不触碰）。
- **实现要点**：登记根跟随 `pipeline.MEDIA_DIR` 的数据根（测试/演练天然隔离，不碰真实 data/）；保底排序按"条目内最新文件 mtime"（目录 mtime 不代表内容时间）；空目录自动移除；`plan_managed_copies` 提供 dry-run 清单；统计键并入 maintenance 白名单（`managed_copy_files_deleted` / `managed_copy_bytes_freed` / `managed_copy_dirs_removed`）。
- **首次生产 dry-run（2026-09-15）：0 待删**（副本均在窗口内；预计 09-20 起 `t002_media`、≈09-25 `media_snapshot` 依次到期自动清理）。04:00 的 `QQBotAutoCleanup` 任务自动携带本能力（maintenance 进程读取工作树代码，无需服务重启）。
- **边界**：备份"保底最新 1 份"不等于备份策略合格——备份频率/异地存放/恢复演练仍属现场事项（本次已做恢复校验一次）。
- **R-112 N03 补注（2026-09-15 主审复验）**：本实现与 D-026"按原消息时间最多 15 天，复制/导出/备份/验收不重新计时"仍有差距——（1）到期判定用文件 mtime，复制会重计时；（2）备份保底"最新 1 份"构成事实豁免；（3）`sample_pool` 未登记即不触碰（含原文）。三项排入下一整改批：持久记录源消息时间/到期时间、备份不豁免原内容期限（靠持续生成合规备份保住恢复能力）、长期材料转充分脱敏或合成样本。**完成前不再声称"15 天全副本工程已关闭"**（D-026 确认保留的规则/版本/统计/必要业务元数据不在此列）。

---

## 决策 D-030：真实动作恢复（W3+W5 合并进行）（2026-09-15 晚）

日期：2026-09-15。依据：负责人明确指示"外部心跳不考虑，其他两个（W3 单群实弹 + W5 全量）全部进行"，即授权恢复 5 个已授权群的**真实撤回**。

- **范围**：`ONEBOT_ACTIONS_ENABLED=false → true`（2026-09-15 19:37 本地）；`ONEBOT_ACTION_STAGE` 保持 `recall_only`（只撤回，禁言/警告在编排层跳过）；**11 个授权群**——首批 5 个（`group_action_owners`：17598122 / 470794920 / 6638171 / 869142826 / 894723164）+ 2026-09-15 19:53 负责人经后台扩容 6 个（330380007 / 476796763 / 544140282 / 67602330 / 868995385 / 983434859，均走 plan_create→approve→execute 三步流程、审计 #710–730 可查）；保护机制全部保留（急停入口：后台/API 的 **DB 急停**跨进程、不重启即生效——编排外发前重读；`EMERGENCY_STOP` 环境变量与运行时急停属进程内/需重启加载（R-112 N02 更正）；保护角色拦截 / `violation_high` 前置 / 人工纠正在场检查 / 群级开关）。
- **生效证据**：服务重启（NSSM 两服务，19:38）后**首条真实撤回执行成功**——intent `#1013` `recall/SUCCEEDED`（群 `17598122`、消息 `2098062009`、`{"ok":true,"attempts":1}`、11:39:58Z）；重启前同场景为"开关未开启"SKIPPED（intent #1010）；未授权群继续被"群动作已禁用"拦截（intent #1012）。扩容后新群同样连续成功：#1016 群 544140282（12:02Z）、#1017 群 869142826（12:03Z）——至记录 **3 条全成功 0 失败**。详见 `docs/evidence/2026-09-15-actions-resume.md`。
- **回退**：立即在后台群管理页点"立即停止全部外部动作"，或调用 `POST /admin/api/emergency-stop`（需鉴权 + `emergency:stop` scope；**DB 急停**——跨进程、不重启即生效；在途已发出的动作不可撤销）；`.env` 改回 `false` 需重启两个服务才加载。（R-113 F05 更正：此前误写为无 `/admin` 前缀的短地址。）
- **历史背景**：09-11~09-14 曾 live 运行 **254 次真实撤回成功**、11 次 NapCat 撤回接口超时失败（均记录 + 转人工，不静默）；09-14 起审计冻结期暂停至本轮恢复。NapCat 撤回超时为观察项。
- **未包含**：外部心跳（负责人明确不考虑，风险自留）；禁言/踢人（stage 未提升）；本证据不替代主审独立复验。群范围变更仅经负责人后台操作（首批 5 → 19:53 扩容至 11），非代码或自动路径。

---

## 决策 D-031：办证/学历类内容完全放行（2026-09-16）

日期：2026-09-16。背景：负责人复验发现"办证"文案被**真实撤回 2 次**（09-16 00:08 / 11:17）——根因链：09-08 反馈把"办证"样本标为 fraud（含 1 条 RULETEST 测试消息）→ 反馈挖掘生成"办证→fraud"动态规则（#1→#12→#47→#281，**DR_167/168/186**）→ 09-09 随"反馈挖掘全量"发布 → 动作恢复后把办证文案判为诈骗高置信并按 `recall_only` 撤回。负责人明确新口径 **A：办证/学历类完全放行（不处罚、不转人工）**。

- **代码实现（全链路）**：本地规则层命中办证特征且类别非严重（category∈{None,ad}）→ `allow` + 豁免标记；**AI 结果（含独立二审确认/严重疑似）与动态规则不得升级或转人工**；媒体层"媒体部分转人工"不得降级豁免；DR 证据携带 pattern，"办证自身"触发的 DR 只记录。
- **止血（已完成 2026-09-16）**：停用全部办证类动态规则项（"办证"/"学历提升(学信网可查)"/"学历提升低价"，版本 v53–v55，`rule_audits` 可查）；候选 #281（PROPOSED"办证=fraud"）→ DISMISSED。
- **保留例外（不放宽）**：①本地文本含严重类别词（category∈porn/violence/fraud，如办证+博彩/裸聊——B-2 保留）；②非办证类的显式动态规则照常生效（对照回归保留）。
- **防再犯**：`tests/test_r113_certificate_dr_protection.py`（办证自身 DR 不升级 / 非办证 DR 仍拦 / AI 严重确认不升级）；DR evidence 携带 pattern 提升可诊断性。
- **历史关系**：D-027（"不撤回、转记录"）与 B-2 的"纯办证转人工"子项被本决策更新；B-2 的严重类别部分保留。
- **已撤回的 2 条不可恢复**（QQ 撤回不可逆，负责人已知悉）；不改变 11 群授权范围与 stage。

---

## 决策 D-032：群主/管理员卡片完全放行（2026-09-16）

日期：2026-09-16。负责人选定口径："**所有管理员/群主发的卡片都放行**"（此前为 record_only 转人工）。

- **范围**：本地规则层——受保护角色（群主/管理员）分享卡片（`share_card`）→ `allow` + 政策标记 `POLICY_PROTECTED_CARD_ALLOW`；**非卡片**的受保护消息行为不变（仍 record_only 不处罚，"保护角色"主体规则不变）。
- **全链路保护**（与 D-031 办证豁免同机制）：动态规则合并不升级（仅记录）；AI 合并（含独立二审确认）不升级不转人工；媒体层"媒体部分转人工"不降级。
- **对照保留**：普通成员的未知来源卡片仍转人工；允许来源（万能校园墙）卡片逻辑不变。
- **回归**：`tests/test_protected_card_allow.py`（owner/admin 卡片 allow / 普通成员对照 / 非卡片范围不变 / DR 不升级 / AI 不升级）。
