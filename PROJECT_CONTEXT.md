# 项目上下文

**CASE-BATCH-5000-20260926（已上线）**：生产 Web 已加载 `4e2a578`。批量标记已人工处理上限从 200 案提高到 5000 案，沿用跨页选择、默认“人工处理”和预览确认；预览明确列出并跳过已关闭及历史证据身份错连案件，让正常案件继续，异常案及原证据不变。确认仍严格核对冻结名单，证据分块读取、统一提交，不产生群动作。D 盘备份、服务恢复和配置不变已核验，见 [上线记录](docs/2026-09-23-case-batch.md#批量-5000-授权部署2026-09-26)。负责人已反馈真实后台全页 CSV 导出成功，并亲眼确认所核对群消息已撤回；通知自动关联仍是独立待办，不将用户观察扩大为全量撤回率。

**STAT-RECALL-20260926（源码候选）**：统计大盘新增最近 7 天的 OneBot 撤回证据概览，复用现有日报/周报的动作意图和匹配通知口径，另列 API 成功。按请求时间和确认保留期限定窗口，不宣称真实客户端撤回率；无迁移或群动作改动。源码与生产状态见 [撤回确认记录](docs/2026-09-24-recall-confirmation.md#stat-recall-20260926统计大盘显示撤回证据源码候选) 和 [交接](HANDOFF.md)。

配套交付补正：历史任务列表仍只读读取最近合规任务，SQL 时间限额从连接准备完成后计算，保留查询中断和文件/结构保护；不改变巡检判据、扫描节奏或数据。验证见 [交付整改](docs/2026-09-25-companion-delivery.md#companion-ready-20260926)。

**COMPANION-READY-20260926**：公司配套包新增默认只预览的 NapCat 服务安装、固定发行清单的无 Git 主库备份、巡检独立离线备份/隔离恢复。安装器不替换已有服务，首次安装要求 SHADOW/动作关闭，有限恢复配置回读后仍待公司故障实演。巡检备份显式指定操作员与导出位置，不收集会话，不自动覆盖 AppData。许可遵循 D-035 的 MIT、单公司 ZIP，未公开发布。工程验证不替代公司现场登录和长期运行验收，见 [本轮记录](docs/2026-09-25-companion-delivery.md#companion-ready-20260926)。

**CASE-ARCHIVE-30D-20260926（D-043）**：负责人把已关闭案件自动归档等待期由结案满 15 天延长为满 30 天；仅作用于尚未归档案件，不批量恢复既有归档。归档仍只是从默认列表隐藏，归档满 90 天后的有条件精简和原文独立 15 天清理不变。此项不改待审立案、自动撤回或群动作开关；源码验证及生产加载以本轮交接记录为准。

**CASE-REOFFEND-BATCH-20260925 已上线**：当前运行源码为 `25bf617`；D-042 恢复第二次有效高置信违规和人工确认结案后再犯的待审立案，自动群动作仍仅撤回。跨页批量人工结案会跳过已关闭重复项。完整本地/CI、备份、双服务重启和健康证据见 [本轮记录](docs/2026-09-25-case-reoffend-batch.md)；真人页面操作与自然再犯待验收。

**CASE-REOFFEND-BATCH-20260925（D-042 设计口径；上线状态见上）**：高置信违规的自动群动作只有撤回；同群同成员近 30 天两次有效违规建立待审案，人工确认结案后下一次再犯重新建立新待审案，即使旧案已归档或旧违规超出 30 天。批量人工结案预览跳过已关闭项并明确计数，确认仍严格核对待审案与证据。当时的结案满 15 天归档等待期已被 D-043 调为 30 天；归档满 90 天且安全时清非必要内容。D-041 中“取消再次违规立案”已被 D-042 取代，禁言/群内警告/自动踢人禁令仍有效。

**此前 RECALL-ONLY-20260925（D-041，部分被 D-042 取代）**：取消自动“首违禁言 1 小时并群内警告、再次违规禁言 24 小时”的整套阶梯。OneBot 与官方出口均不得因旧 `full` 配置、历史动作意图或重试发出禁言/警告。逐条证据、旧决定、既有案件和处罚记录保留用于审计；不能把此前生产已设 `recall_only` 当作源码已移除阶梯。

**COMPANION-DELIVERY-20260925**：负责人选择方案 A，群管理主项目与空间巡检同仓库、同提交、两套入口配套交付。接收方文档集中于 [DELIVERY.md](DELIVERY.md) 和 `docs/delivery/`，双方手册互链。当前仅候选源码包；不将公司试用等同正式验收，不创建新仓库或擅自发布 Release。待办和实际验证见 [本轮记录](docs/2026-09-25-companion-delivery.md)。

**SPACE-EXPORT-PATH-20260925**：负责人要求自定义巡检导出位置，新增目录选择及本机持久化；不可用目录明确报错，保留旧任务和旧导出，不改变扫描判据、频率或生产服务。具体行为与验收证据见 [巡检记录](docs/2026-09-21-space-inspector.md#自定义导出位置space-export-path-20260925)。

**RECALL-CONFIRM-20260924：已获授权上线并完成迁移、服务恢复及新旧备份恢复验证。** 部署源码 `2fc0d55`，OneBot ready，急停 false、recall_only 与群授权未变；跨页案件勾选补正一并加载。撤回 API 成功与 QQ 通知单独记录，当前自然撤回尚未匹配，实机效果待观察。验证与恢复证据见 [本轮记录](docs/2026-09-24-recall-confirmation.md)。

**CASE-SELECT-20260923：跨页选择与人工处理补正已随 `2fc0d55` 加载。** 实现 `4855a4a`、入库回归 `07ec9ba`；同一标签页/登录/筛选内累计多页勾选，取消理由输入，默认审计“人工处理”，保留预览确认。完整本地测试、门禁与无网络前端脚本模拟已通过；原方案见 [案件批量补正](docs/2026-09-23-case-batch.md#case-select-20260923跨页选择与人工处理补正待上线)，实际上线见 [本轮记录](docs/2026-09-24-recall-confirmation.md#授权部署2026-09-24)。真人浏览器跨页操作尚待验收，未改群开关或处罚配置。

**CASE-BATCH-20260923：已推送并经负责人授权上线。** 部署提交 `22e60d1` 的应用源码与全量验证的 `d3e2d3c` 一致：案件摘要 CSV、冻结预览后批量 KEEP/CLOSED、撤回回执超时 UNKNOWN 均已加载。D 盘数据库备份校验、Web/Runtime 新进程、OneBot ready、自然消息恢复与配置不变已核验；急停 false、recall_only、群开关保持原样。未代用户结案、导出生产名单或重放历史动作。执行 SHA、命令、CI 与实机边界见 [本轮记录](docs/2026-09-23-case-batch.md#授权部署2026-09-23)。N03 与整机故障恢复仍独立待办。

**最新 CARD-RECALL-20260923 已上线**：源码 `23d0a1f` 补正群名片并追加微信小程序分享卡撤回；保留保护角色、成员白名单与校园墙图片窗口。五个缺失群已启用，绵阳免费家教群 `628717263` 按负责人要求审核/动作双关闭。新进程及业务恢复已核验；急停 false、recall_only。执行 SHA、命令、数量、CI 与自然卡片验收边界见 [本轮记录](docs/2026-09-23-card-recall.md)。下文旧版当前状态均为历史。

## SPACE-EXPORT-20260923：巡检导出可辨识性

负责人反馈任务/导出层级难找且编码名不能对应群。独立工具新导出移到桌面“QQ空间巡检结果”的单层结果目录，以群快照的群名、群号和本机导出时间命名；每群结果独立 CSV，兼容总表及 JSON 保留。内部任务目录不重命名，保证续扫、锁、历史观察来源仍有效；旧任务无需重扫即可重新导出。窗口分别显示当前任务、续扫数据、最近导出和总目录。代码与验收边界见 [巡检记录](docs/2026-09-21-space-inspector.md#导出目录与群结果辨识space-export-20260923)。不改巡检判据、访问频率或生产服务。

**完整恢复验收位置（负责人 2026-09-22 决定）**：完整副本使用 D/E 盘全新私有目录，避免挤占系统盘；已授权清理的三份 C 盘副本仅保留验收清单/收据，E 盘备份和小型状态配置保留。执行事实及后续空间/清理约束见 [每日备份](docs/2026-09-22-daily-backup.md#c-盘恢复副本清理与后续位置2026-09-22)。

**DAILY-BACKUP-20260922（已启用并完成首跑/恢复验收）**：每日 04:30 的 SYSTEM 明文备份已注册，排除服务密码和 Token，新备份不使用恢复密钥。源码 `7b040b8` 全量/门禁、源码及文档 HEAD `4182767` 的 CI 均通过；实际任务执行 SHA `4182767`、完整隔离恢复和开关/原进程不变证据见 [每日备份](docs/2026-09-22-daily-backup.md)。旧备份与证据保留，通知仍不启用；整机故障演练和 N03 处置分别待维护窗口/清单授权。


## SPACE-OPTIMIZE-20260922：巡检自动化与接口验证

负责人授权优化现有巡检并验证直接接口；本任务为唯一仓库写入者。新增自动续批、可调间隔/批间休息、轻量加载、同查看账号的短期历史观察复用，以及未知成员页面的显式“留待复查并继续”。默认仍为每人 30 秒、每批 300 人，自动续批时批间休息 60 秒；这些是可调试验参数，不是平台允许阈值。登录、身份不符和 WAF 仍停止；未识别页面不自动算正常或违规。

接口研究未得到可交付的直接 CGI 判据；具体请求限制、主页 HTML 观察和验证证据统一见 [巡检记录](docs/2026-09-21-space-inspector.md) 的“自动续批与性能优化”段。旧任务可载入；采用新复用依据的任务须用新版继续。新版桌面真实续扫、导出及整群持续运行需单独验收，本轮不重启或修改生产机器人。

**PROJECT-AUDIT-20260922：全面复查修复已推送并上线，急停保持开启**。冻结源码 `1c17ba09e5bef268a6162864830396e8d36c6107` 的全量、原主审、门禁与精确源码 CI 已验证；认证、群备注并发、连接资源、报告快照、备份边界与媒体依据修复及真实运维缺口见 [本轮审查](docs/2026-09-22-project-audit.md)。生产已正常重启并加载 `1c17ba0` / `t204-v18`；备份、配置不变和自然消息核验见本轮审查的部署收据。急停 true；无生产清理、权限、计划任务或通知变更。以下旧批次保留历史，当时的“当前/未部署”不覆盖本条状态。

**此前 CAMPUS-TEMPLATE-20260922 上线记录（当前加载状态见上）**：当次加载 `06324fedab4baf8d0ee14e09991c5385627a01ed`，提示词 `t204-v18`。认可分享卡按图标、完整页脚和同卡布局联合识别；单图及普通文字配图均可建立同成员两分钟窗口。源码、真实原图复验、离线窗口回放、CI 与两次部署证据见 [本轮记录](docs/2026-09-22-campus-source-scope.md)。未解除急停、未修改群授权或动作开关；新规则不重判历史消息。

**此前 CAMPUS-SCOPE-20260922 上线记录（当前加载状态见上）**：加载 `8f4617edf6f73d8b98683994d575b022b9f2a0e4`、提示词 `t204-v17`。通用小程序码恢复常规审核，仅明确确认的万能校园墙来源保留既有豁免；生产健康、新视觉记录及执行 SHA/命令见 [本轮记录](docs/2026-09-22-campus-source-scope.md)。无迁移，群授权、阈值与动作开关保持原值。

## CAMPUS-SCOPE-20260922：校园墙来源豁免收窄

负责人已选择 A：只对明确确认的万能校园墙来源保留原有豁免，其他小程序恢复正常审核。新契约、原始探针封存与逐处适配见 [本轮记录](docs/2026-09-22-campus-source-scope.md)。隔离候选现已验证并上线，生产加载证据以该记录为准；不得把本次范围变更解释为启用 image_hash enforce 或修改群授权、阈值。


**A2-DEPLOY-20260922 已上线**：负责人明确授权“方案 A 上线”；真实备份副本验证后，已正常停服、迁移/回填、身份对账并恢复服务。A2 当次加载源码 `53683d5998588f3b7490f4e73774df313b17b51d`，新 revision `e1c7d4b8a902`。命令、证据和未完成边界见 [上线记录](docs/2026-09-22-authority-a2-deployment.md)。此前“生产未部署/待窗口”为历史状态；整机恢复与 N03 仍待办。

A2 候选分支：图片审核决定同库保存、JSON 可重试导出已实现；裁定、适配、源码与隔离验证见 [A2 候选验收](docs/2026-09-22-authority-a2-validation.md)。本分支包含新 migration，不能在未安排维护窗口时合入生产。

CORE-CLOSEOUT-20260922：本轮兼容修复与证据已完成，本机冻结验证见 [收尾记录](docs/2026-09-22-core-closeout.md)。方案 A 已裁定并在独立候选分支实现，生产未迁移；通知保留关闭，Windows 整机演练待排期，N03 原件处置仍须具体清单确认。独立巡检不计入主项目验收。

## 2026-09-21 电脑空间限制巡检

2026-09-22 新增非好友访问维护页适配：实页提示“QQ空间相关功能升级维护,暂不支持非好友访问”；此前被记为未知页面，同成员另一次访问曾实际跳转登录页。现按已核对的完整系统模板记待确认、保存原文并继续；不计违规、不推断账号正常，真实登录失效/WAF/未知页面仍暂停。300 人批次和 30 秒间隔不变。执行 SHA、命令及实机边界见 [巡检记录](docs/2026-09-21-space-inspector.md) 的“非好友访问维护页面”段。

2026-09-22 负责人要求将每次开始/继续检查的上限由 10 人调为 300 人试用；保留批内间隔 30 秒、批末手动续批、手动暂停和遇到平台拦截/未知页面即停。此为试验批次上限，不是腾讯允许阈值，也不是加速请求。旧窗口需暂停并重开后载入原任务；300 人真实批次尚未验收。代码、执行 SHA/命令见 [巡检记录](docs/2026-09-21-space-inspector.md) 的“每批上限调整为 300 人”段。

最新跟进：负责人续扫时遇到另一种官方限制提示模板（全角冒号、句号），旧识别器将其记作未知页面。已按实页核验补精确模板，原文保存/恢复/导出同步支持；不放宽账号、页面来源与加载校验。真实页面转录验证、只读任务进度及冻结版本测试见 [巡检记录](docs/2026-09-21-space-inspector.md) 的“第二种限制提示模板”段。负责人重开后反馈已越过暂停位置，保存记录确认该成员追加明确限制观察且旧历史保留；新增导出待验证。整群可靠性和接口替代仍未验证。

此前已选低频分批：每批最多 10 人、批内间隔 30 秒、手动续批，属于实验参数，不能据新增进度推断腾讯阈值或完整批次安全性。

最新实机结论：整群连续访问遭腾讯 WAF 拦截，一次扫完整群验收未通过。当前仅已验证有界续扫和部分结果导出，不能承诺稳定全群批量巡检；暂停并保存结果，恢复时间与低频分批可行性未知。

负责人已选择 SPACE-INSPECT-20260921：电脑选群后自动检查 QQ 空间明确限制提示，未命中待确认，正式使用不连接手机。现有仓库新增独立桌面入口、专用浏览器与自有任务库；群目录来源为已配置机器人账号。与生产判断、授权和动作隔离。实现、执行 SHA/命令、实页证据与验收缺口统一见 [使用与验证记录](docs/2026-09-21-space-inspector.md)。

实机遇到的申请访问、加好友访问及未开通空间布局已补适配，均记待确认并继续；已有任务通过窗口内列表载入，也可直接打开任务目录。自动化验证与各版本实机续扫分别记录，不能据此推断整群扫描完成。

## 2026-09-21 生产媒体容量恢复

负责人同意 MEDIA-QUOTA-20260921：容量配置化、先扩至 20 GiB、容量预警及失败原因留存。保留期、判断与处罚规则不变，不删除证据。已备份并重启主服务；准确验证与生产边界见 [恢复记录](docs/2026-09-21-media-quota-recovery.md)。

## 2026-09-21 追加范围：手机辅助巡检

负责人已授权 PHONE-INSPECT-20260921：独立桌面进程读取当前群成员资料卡的明确异常提示，保存 QQ/群身份和观察证据并导出。主项目只承载源码和回归，工具使用自己的 SQLite 任务目录，不接入生产判定、授权或动作。拒绝名单匹配及昵称/头像推测。验收区分合成测试、有界实机、全群遍历，见 [使用与验证记录](docs/2026-09-21-phone-inspector.md)。下文保留历史阶段，不代表本批生产已加载。

## 基本信息

- 项目名称：QQ 群多模态智能管理机器人
- 文档初始化日期：2026-09-03
- **当前阶段（以本行"截至时间"为准：2026-09-18 18:00 本地 / 10:00 UTC）**：
  **D-037 / D-038 / D-039 与 D-036 两个补丁已全部实现、提交（`84a8b47`）并部署**——
  部署证据：16:24 / 17:06 / 17:18 三次提权重启；`alembic current == head == c9a1f4d27e30`；
  提示词版本 `t204-v15` 已加载（**代码内已升 `t204-v16`，待下次授权重启生效**）；
  healthz `ok / onebot ready+connected`；13 个群启用真实动作（`stage=recall_only` 只撤回）。
  **r132 主审复核（PR #45 / `84a8b47`）结论：不通过**（6×P1 + 4×P2）；独立复核与逐条属实性见
  `docs/2026-09-18-r132-reviewer-verification.md`；**整改已落地（新 SHA 待重新送审）**——
  F01/F08 窗口豁免按全部有效证据判据、F02 小程序码不得替未定论附件放行、F03 群名片只认结构化信号、
  F04 结构性规则按卡片结构判定、F05 导入确认绑定服务端计划、F06 回滚手册按已提交版本重写、F07 名单身份不被通用 veto 降级。
- **历史阶段（保留备查，勿当现行状态）**：2026-09-16 三项负责人政策 + R-114 复验整改落地并部署
  （D-031 办证/学历完全放行含事故止血与全链路保护、D-032 群主/管理员卡片放行、D-033 全局白名单
  仅豁免广告类且逐消息直读）；**已验证 main 为 `68a94b9`（r127「口径 C」P1 关闭）**。
- **测试（本机 Windows，绑定环境）**：**1418 passed / 15 skipped / 0 failed**；
  `ruff check` + `ruff format --check`（238 文件）、`mypy`（88 源文件）通过。
- **遗留**：N03（源时间持久化/备份不豁免/样本池）、容量 B/C 重跑、完整恢复 + 整机故障演练（停机窗口）、
  **图片哈希白名单缺样本**、**群名片真实样本**、单模型直接决定 85% 图片处罚是否收紧、对外交付前置（README 重写等）。
  详见 `HANDOFF.md` 顶部与 `NEXT_TASKS.md`。
- 目标规模：负责人最新明确为一百多个 QQ 群；最初 1–10 群/日不超过 10,000 条是历史容量假设，尚未证明百群峰值、延迟、费用与磁盘上限。
- 时区：`Asia/Shanghai`
- 项目负责人职责：确定需求、规则和最终验收；具体搭建任务可以由其他 Agent 按 `NEXT_TASKS.md` 分工执行

## 项目背景

本项目用于 24×7 监督多个 QQ 群，识别并处理群成员发布的垃圾广告、引流、诈骗及其他群规禁止内容。审核对象不仅包括文字，还包括图片、二维码、GIF、表情包、视频、语音、文件、转发记录和分享卡片。

项目优先目标是减少垃圾信息暴露时间，同时尽量避免误伤正常成员。高置信违规只自动撤回消息；不自动禁言或发送群内违规警告。踢人仍须人工批准。

## 目标用户和核心目标

### 目标用户

- QQ 群主和被授权的人工管理员。
- 负责审核规则维护、案件复核、人工踢人或批准 NapCat 踢人的运营人员。

### 核心目标

1. 实时接收和标准化目标群中的全部可用消息类型。
2. 使用规则、行为特征、OCR、二维码、ASR、视频抽帧及多模态模型识别垃圾信息。
3. 对高置信违规仅自动撤回消息，不使用首次/再次违规处罚阶梯。
4. 保留逐条违规证据及既有案件供人工审核；同群同成员近 30 天第二次有效违规或人工确认结案后再犯，建立新的待审案。
5. 允许人工选择在 QQ 客户端手动踢人，或明确授权 NapCat 执行踢人。
6. 定期汇总群聊情况、异常事件、系统动作、误判和运行状态。
7. 保存可审计证据，使每次判断和动作都能追溯到规则、模型版本和人工审批人。
8. 允许管理员在后台编辑、测试、发布和回滚规则，发布后无需改代码或重启。
9. 将人工确认的违规、正常和误判沉淀为反馈样本，生成候选规则并经人工批准后更新。

## 已确定的业务规则

### 违规范围

- 广告、引流、联系方式、二维码、外部群邀请。
- 诈骗、钓鱼、仿冒客服及高风险交易。
- 色情、赌博、毒品及其他违禁交易。
- 刷屏、重复推广、恶意骚扰和人身攻击。
- 泄露手机号、身份证、住址等个人隐私。
- 群主后续提供的自定义禁区、黑名单和例外。

### 置信度原则

- 中等风险：只记录并进入人工复核，不自动撤回或累计有效违规。
- 高置信违规必须满足以下任一条件：
  - 命中明确黑名单、已确认恶意域名、二维码、相似广告图或可确定的刷屏规则；
  - D-022条件复核：明确视觉广告/诈骗且主≥0.90、无需复核/无冲突可形成高置信建议；疑难只在主≥0.60与独立复核≥0.90同类别确认且无残留冲突时升级；失败、分歧、缺复核一律只记录。所有建议仍经过保护角色、身份、动作门禁与幂等编排。
- 媒体无法下载或分析失败本身不能作为处罚理由。
- 群主、管理员和显式白名单成员只记录和告警，不自动处罚。
- **成员白名单（D-037，2026-09-18）**：按 QQ 号维护的白名单（`allowlist_members`）**优先级最高**，
  命中者**全类别完全放行**（含诈骗/色情/暴力/刷屏；负责人在知悉"等于诈骗入口完全敞开"的代价后
  明确选择，**不守 B-2 底线**）：不撤回、不处罚、不转人工，且 AI（含二审）、动态规则、媒体层
  一律不得升级（`POLICY_ALLOWLIST_MEMBER_ALLOW` ∈ `POLICY_ALLOW_RULE_IDS`）。仅对 NapCat/OneBot
  主通道的数字 QQ 号生效，官方通道 openid 不适用、不跨通道匹配；消息仍**逐条落库记录**可追溯。
  名单在后台「白名单」页用一份文本文件整表同步（上传 → 差异预览 → 确认 → 写入；空文件拒绝；
  停用幅度大时二次确认）。
- **含小程序二维码的图片（D-039，2026-09-18）**：图片中出现**微信小程序二维码**时**一律通过**（不撤回、不处罚、
  不转人工），由视觉模型结构化字段 `has_miniprogram_code` 触发（严格布尔、仅视觉通道有效）。
  **保留两条例外**：①**色情 / 暴力违禁品**不放行（**诈骗不再例外**——负责人 2026-09-18 晚修订，
  含小程序码的诈骗内容同样放行）；②本地硬证据（R001/R003/R006/DR_）不被覆盖。
  **QQ 群二维码、个人名片码、普通链接二维码不属于小程序码**，含这类码的引流仍按广告处理。
- **合并转发与群名片（D-038，2026-09-18）**：**一律撤回**（本地结构性确定性规则 `R_FORWARD_RECORD` / `R_GROUP_CARD`，无需展开
  转发内容、零外部调用）；**群主、管理员、成员白名单成员不撤回**。群名片识别为多信号宽容判定
  （识别不出则按普通卡片走既有分支，不误撤音乐/新闻/小程序卡片），**尚未用真实样本验证**。

### 自动撤回与历史案件

高置信违规逐条记录；同通道、同群、同成员的近 30 天有效违规用于首次待审立案，不驱动自动处罚升级：

1. 高置信违规：保存证据并仅自动撤回该消息；不论历史违规次数，都不自动禁言或群内警告。
2. 近 30 天第二次有效违规建立待审案；已有待审案继续关联新证据。经人工保留或确认踢出后结案，再次高置信违规新建待审案，旧案不重开、旧证据不重新计入；误判结案不作为再犯依据。
3. 任何违规后都不自动踢人，也不自动拒绝再次入群。
4. 人工审核可以选择保留、标记误判、在 QQ 客户端手动踢人，或批准 NapCat 踢人。
5. 标记误判/确认正常后，经精确的消息与中立身份映射撤销对应有效违规记录，保留历史与审计，并将样本用于规则和模型复盘。无映射不猜测；重新标违规不自动恢复旧处罚；不自动解禁或恢复已撤回消息，不自动关闭整案。尚未发送的撤回动作终检人工纠错，已发送动作由人工核对补救。

## 最终架构决策

```text
QQ群消息
   ↓
NapCatQQ / OneBot 11（目标大群主通道）
   ↓ Adapter验证与标准化
传输中立消息契约
   ├─→ 审核服务：规则、行为、多模态识别、独立复核
   ├─→ 案件中心：违规累计、证据、编号、审批、报告
   └─→ 动作编排：仅撤回 → NapCat Adapter
                            ↓ 违规证据与既有案件供人工处理
                       人工审核批准
                     ┌──────┴──────┐
               QQ客户端手动踢人   NapCat持令牌踢人
                     └──────┬──────┘
                         审计与结果

QQ官方机器人 → 官方Adapter → 同一中立契约
（仅用于实际可接入的测试/小群或未来企业认证场景）
```

### 组件职责

- **NapCat/OneBot**：目标大群的主消息与动作Adapter；接收普通QQ管理员账号可见的群消息，并在审核编排授权后仅自动撤回。它不负责理解内容。
- **QQ 官方机器人**：保留的可选Adapter；只用于机器人实际可进入的群，不再是目标大群上线前置。
- **消息标准化层**：在Adapter seam后把OneBot和官方事件转成统一消息封装，显式保留 `provider`、外部群/成员/消息ID、时间、消息段、附件和上下文。
- **审核服务**：执行规则检测、行为检测、OCR、二维码解析、图片语义理解、GIF/视频抽帧、ASR 和模型复核。
- **案件与证据中心**：保存违规历史、证据、处罚结果、审核批次、候选编号和人工结论。
- **管理后台**：查看证据、规则、映射状态、白名单、待审核案件、日报和审计日志；提供暂停处罚和紧急降级开关。
- **人工 QQ 客户端**：默认且风险最低的踢人出口，也是 NapCat 不可用或身份不明确时的回退出口。
- **OpenClaw/Hermes**：首版不接入核心链路；后期只可作为自然语言报告或管理入口，不能拥有未经审批的处罚权限。
- **Codex**：用于开发、测试、审查和最终验收，不作为 24×7 运行时。

## 运行模式

### 影子模式（默认）

- NapCat接收目标群消息并运行完整审核、证据、案件和报告链路。
- 只保存拟执行的撤回意图，外部动作调用数必须为0。
- 用于首次NapCat实机接入、每次大版本/规则/模型变更和异常恢复后观察。

### 实时管理模式（分群启用）

- NapCat负责接收，审核核心产生确定性撤回意图，NapCat动作Adapter在幂等、急停、角色保护和断线保护通过后执行撤回。
- 踢人默认仍在QQ客户端人工执行；只有T-304通过后，才允许人工批准的NapCat踢人。
- NapCat断线、QQ安全验证、动作连续失败或状态未知时，立即停止真实动作并转人工监管。

### 可选混合模式

- 官方机器人可继续服务它能进入的测试/小群，NapCat服务目标大群。
- 每个群必须显式选择一个消息入口和一个管理动作出口，不允许两个Adapter对同一条消息同时执行处罚。

## 平台身份和数据迁移

- 统一契约的目标形式是 `provider + external_group_id + external_user_id + external_message_id`；对NapCat即OneBot数字 `group_id`/`user_id`，对官方Adapter则是 `group_openid`/`member_openid`。
- 当前数据库与领域类仍使用 `group_openid`/`member_openid`，T-305必须采用expand/migrate/contract迁移：先增加中立字段与兼容读写，再回填和切换读取，最后才在独立任务评估是否删除旧列。
- **T-305 expand 阶段已实现并完成主审整改（D-020）**：中立契约位于 `app/core/contracts.py`，包含通用消息段；中立列、双写、回填与混合版本读取已就位；路由以 `message_provider + external_group_id` 联合定位，未配置OneBot默认拒绝；违规累计和动作幂等按provider隔离。迁移 `b8e2f6a4c1d9` 与纠正迁移 `d4f7a9c2e601` 可完整升降级。旧列删除留待contract阶段独立评审。
- NapCat主通道群直接使用数字ID，不需要OpenID映射才能撤回；单独人工审批的踢人仍遵守身份与出口门禁。
- 只有同一群配置两种Adapter时才需要跨通道映射；不得仅凭昵称、头像、群名片或模糊时间建立可执行映射。

## 人工审核与踢人

审核服务为每个批次生成不可变编号，例如 `R20260903-01`，批次内候选成员使用 `1、2、3……` 编号。编号只在当前批次、当前群有效。

支持的审核表达包括：

```text
审核 R20260903-01：踢 2,5,8
审核 R20260903-01：保留 1,3,13，其他踢
审核 R20260903-01：全部保留
审核 R20260903-01：全部踢
审核 R20260903-01：重新查看 3
```

自然语言不能直接触发动作。系统必须先转换成结构化名单、显示预览并要求一次性确认码。超过 10 人的批量踢出增加一次确认；该数字是项目初始安全阈值，不代表 QQ 官方风控阈值。

双出口状态必须互斥：

```text
PENDING_REVIEW
  ├─→ APPROVED_MANUAL → MANUAL_PENDING → KICKED / CANCELLED
  ├─→ APPROVED_NAPCAT → CONFIRM_PENDING → EXECUTING → KICKED / FAILED
  ├─→ KEEP → CLOSED
  └─→ FALSE_POSITIVE → STRIKE_REVOKED → CLOSED
```

同一案件不得同时由人工客户端和NapCat成功执行。NapCat失败后只转人工队列，不无限重试。

## 多模态处理要求

- 图片：OCR、二维码解析、感知哈希、视觉语义识别。
- GIF和动态表情：最多抽取8个代表帧后综合判断。
- 视频：场景切分后最多取16帧，执行OCR并转写音轨；同步处理上限为10分钟或200MB。
- 语音：优先使用可用的官方转写，否则调用云端ASR。
- 文件：只提取PDF、Office和纯文本；不执行文件。压缩包和可执行文件只做元数据和安全扫描。
- 转发和卡片：递归提取文字、链接、预览图和来源信息。

## AI辅助与规则学习

- 审核采用分层级联：确定性规则、行为、OCR、二维码、哈希和ASR优先；不确定内容再进入文本或视觉AI。
- 文本与视觉使用独立任务接口，但可由同一远程供应商提供；首个候选是兼容OpenAI API的小米MiMo。
- 远程AI按群显式启用，默认关闭；只上传最小脱敏内容，完整视频、完整文档和无关身份信息不上传。
- 单个模型只提供软证据；未配置独立复核模型时，AI结果不直接触发自动处罚。
- 动态规则以数据库中的不可变版本为权威，支持草稿、历史回放、发布、热更新和回滚；每条判定保存规则版本。
- 管理员确认的违规、正常和误判是权威反馈；未知原因撤回保持待标注，未撤回消息保持未标注。
- AI可以聚类样本并生成结构化候选规则，但不能自动发布、提高处罚等级或执行动作。
- 首版以后台人工标注为权威反馈入口。NapCat主通道的撤回通知在T-306/T-303验证后可作为待标注事件，但不得自动视为违规真值。
- 完整设计见 `docs/superpowers/specs/2026-09-06-ai-rule-learning-design.md`。

### 当前实现状态（2026-09-06）

- 动态规则已落库：全局/群级规则集、草稿、发布、回滚、审计、5秒缓存热更新和每条判定规则版本追踪已实现。
- 远程AI已实现基础适配：`TextModerator`/`VisionModerator` 接口、OpenAI-compatible/MiMo类适配器、按群显式启用、脱敏文本、严格JSON响应、缓存、限流/预算和降级记录；真实MiMo模型ID、区域、计费和效果尚未由负责人本地验证。
- AI结果当前只作为软证据：单模型高置信只会把本地 `allow` 升为 `record_only`，不会自动撤回或踢人。
- 反馈学习已实现基础闭环：后台可对影子判定标注反馈，本地挖掘候选规则并复制为动态规则草稿；候选仍需人工在规则页发布才会生效。
- 官方动作编排默认关闭：`ACTION_MODE=SHADOW` 不调用外部处罚接口；`OFFICIAL` 仅允许自动撤回，不包含自动禁言、群内警告或踢人，启用前仍需生产配置和实机验收。

## 数据、报告和运维

### 数据

- 首版使用 SQLite WAL 保存消息元数据、案件、判断、动作、动作意图、AI缓存/用量、反馈、候选规则、规则版本和审计记录。
- 原始媒体与数据库分离并加密存储。
- 原始消息和媒体默认保存30天；判断、动作、证据索引和统计保存180天后清理。
  2026-09-12 部署例外（D-024）：负责人明确选择原始内容只保留 15 天、接受第 16–30 天原内容缺失；沿用已报告的 RAW_RETENTION_DAYS=15 / DECISION_RETENTION_DAYS=15 部署设置，软件模板默认值不因此改动。原 30 天累计所需的最小违规/身份/反馈标签元数据继续用于待审立案、历史兼容与纠错，不用于自动升级处罚；不把记录关联当成原证据完整。已发布规则不作为原始消息副本清理。受管原文副本须按原始期清除，备份/手工样本须由 Windows 另行落实同等期限，未核验不得宣称所有副本都已删除。
- 云模型只接收完成审核所必需的最小数据。
- 远程AI属于负责人新授权的可选能力，是否启用、允许的数据类别、供应商区域和每日预算按群配置；关闭后不影响本地审核。
- 密钥必须使用环境变量或系统凭据存储，不写入代码、Markdown、测试数据或日志。

### 报告

- 严重事件实时通知。
- R-106实现：新待审案件/UNKNOWN动作下个通知轮询提醒，普通record_only默认15分钟摘要；持续故障独立邮件，15分钟未确认page升级备份邮箱一次。人工确认只接手、不批准处罚。通知全部默认关闭；外部Healthchecks与本机每分钟探针必须由部署负责人另配并实测。故障恢复不自动恢复处罚，业务摘要投递不代表完成复核；详见D-023。
- 每天09:00和21:00生成日报。
- 每周一09:10生成周报。
- 报告至少包含消息量、异常数、违规类别、撤回结果、待审核案件、误判、NapCat状态、队列积压和模型费用；历史禁言记录仅作审计展示。

### 运维降级

- 监控NapCat/OneBot连接、QQ登录与安全验证状态、每群最后事件时间、事件延迟、媒体队列、模型失败率、动作失败、磁盘空间和报告状态。
- 模型不可用、规则损坏、证据不完整或动作结果不确定时，降级为只记录和告警。
- NapCat发生安全验证、异常掉线、连续失败或实际不再接收群消息时，目标大群的自动监督已中断。系统必须停止全部NapCat真实动作、立即告警并转人工监管，不得声称“官方审核仍在运行”来掩盖目标群已失去消息入口。
- 官方机器人若在其他可接入群运行，只能继续服务那些群，不能作为NapCat大群的自动备接管通道。

### Windows 24×7运行与恢复

- 正式整机测试环境已确认为项目负责人提供的Windows 10专业版电脑（2026-09-04，决策D-011）；首次测试时记录实际build号、架构、补丁、电源和网络条件。
- 该电脑分阶段投入：W0基础兼容已通过；R-104与T-305/T-306后验证NapCat影子接收；核心契约与审核链路通过后验证影子端到端；T-307通过后验证撤回及无自动禁言/警告；T-404后验证锁屏、重启、断网、更新和恢复；最后才分阶段上线。
- GitHub Actions的Windows任务用于代码兼容性门禁，不能替代真实QQ登录、NapCat、Windows Service或24×7整机测试。
- 屏幕可以关闭并锁屏，但专用机不得进入睡眠或休眠；睡眠期间无法保证实时接收消息。
- 审核服务、媒体任务、报告调度和健康监控必须在Windows启动后自动拉起，不依赖人工打开终端。
- 启动顺序固定为数据与迁移检查、审核服务、任务执行器、QQ客户端与NapCat/OneBot、健康监控；可选官方Adapter不得阻塞NapCat主链路启动。
- 系统更新采用受控维护窗口：暂停新处罚、排空可安全任务、备份数据库、重启、健康检查通过后再恢复自动动作。
- 重启前结果不确定的撤回或历史处罚动作不得自动重放，必须先查询状态或转人工复核。
- BIOS应支持来电自动开机；条件允许时使用UPS并定期进行备份恢复演练。
- NapCat需要扫码、设备确认或安全验证时只告警并等待人工，不得反复自动登录。
- 详细要求和验收步骤见 `docs/windows-operations.md`，实施任务见 `NEXT_TASKS.md` 的 `T-404`。

### Windows测试阶段门槛

1. **W0 基础兼容性**：R-101全部阻塞项关闭，Linux/Windows CI真实通过后，在空白Windows电脑完成干净安装、迁移、配置拒绝、端口、pytest、mypy和ruff验证；不配置QQ或模型密钥。
2. **W1 NapCat影子接收**：R-104、T-305、T-306通过后，在隔离群验证OneBot连接、全量事件、数字身份、媒体下载、断线重连和去重；所有外部处罚调用数为0。
3. **W2 核心影子端到端**：W1及核心审核、案件人工工作流、多模态和报告任务通过后，验证NapCat消息到规则/AI、违规证据、待审立案、报告和人工QQ客户端踢人闭环；影子模式仍不执行自动群动作。
4. **W3 NapCat管理动作**：T-307通过后，在隔离群从影子开始，验证撤回、保护角色、幂等、超时和 `UNKNOWN` 人工复核，并证明首违/再次违规都没有自动禁言或警告。T-304踢人始终保持单独批准门槛。
5. **W4 无人值守恢复**：T-404实现后，验证Windows Service、QQ/NapCat会话依赖、锁屏、重启、断网、更新、备份恢复、未知动作复核和外部告警。
6. **W5 最终分阶段验收**：按T-403先在目标群影子观察，再按群依次开放NapCat撤回；不得开放禁言/警告档位。踢人默认人工，连续稳定性观察后才完成整体验收。

## 初步技术栈

- 开发语言：Python 3.12。
- 后端与管理接口：FastAPI。
- 数据访问：SQLAlchemy；数据库迁移方案在脚手架阶段确定并记录到 `DECISIONS.md`。
- 首版数据库：SQLite WAL；规模或并发超过首版假设时再评估 PostgreSQL。
- 主接入与大群动作：NapCatQQ + OneBot 11。
- 可选接入与动作：QQ 官方机器人 API（仅限实际可接入的群）。
- 多媒体：OCR、二维码解析、ASR、视觉模型、FFmpeg。
- 部署：生产大群使用固定Windows 10专业版专用机、没有重要资产的专用管理员QQ和NapCat；审核服务、NapCat与QQ会话健康监控必须共同达到24×7验收。
- 管理后台：首版以服务端页面为主，避免过早引入独立前端工程。

## 验收指标

- 自动撤回建议的离线精确率不低于99%。
- 广告、诈骗等核心类别召回率不低于90%；综合多模态召回率不低于85%。
- P95审核延迟：文字3秒以内，图片/GIF 15秒以内，普通视频90秒以内。
- 影子模式下NapCat撤回和其他处罚外部调用次数必须为0；真实模式自动禁言、群内警告调用次数仍必须为0。
- 重复OneBot事件、WebSocket重连和进程恢复不得导致重复撤回或执行历史禁言、警告意图。
- 系统必须区分“进程存活”和“持续收到目标群事件”；NapCat或QQ会话失效后不得误报为 `ready`。
- 没有人工有效审批令牌时，NapCat踢人接口调用次数必须为0。
- 目标群/成员数字ID缺失、角色未复核或跨通道映射不可靠时，NapCat踢人接口调用次数必须为0。
- 同一案件两个踢人出口同时成功的次数必须为0。
- NapCat出口测试集和7天分级期内误踢人数必须为0。
- 每个主要违规类别至少准备20个违规正例和30个相似合法反例。
- 图片、GIF、视频、语音、转发卡片和二维码分别至少准备10个违规与合法样本。

## 风险边界和非目标

- NapCat属于非官方接入，不能承诺不触发QQ风控，也不设计规避检测、设备指纹伪装、代理轮换或随机模拟真人。
- 首版不追求完全无人值守踢人。
- 首版不让通用Agent、提示词或群消息直接拥有处罚权限。
- 首版不进行无人监督在线训练，不把管理员撤回直接等同于违规，不把未撤回直接等同于正常。
- 首版不执行群成员上传的文件，不把管理接口暴露到公网。
- 任何改变“人工批准才能踢人”“目标大群主通道使用NapCat/OneBot”“单群只有一个激活动作出口”或“身份不明确必须人工处理”的需求，都属于架构变更，必须先更新 `DECISIONS.md` 和本文件。

## 外部参考

- [QQ全量群消息事件](https://bot.q.qq.com/wiki/develop/api-v2/autogen/event/group_message_create.html)
- [QQ撤回群消息](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_groups_group_openid_messages_message_id.delete.html)
- [QQ群成员禁言](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_groups_group_openid_restrict_chat_setting.post.html)
- [NapCatQQ](https://github.com/NapNeko/NapCatQQ)
- [OneBot 11 API](https://github.com/botuniverse/onebot-11/blob/master/api/public.md)


## 2026-09-22 主项目收尾 CORE-CLOSEOUT

报告一致快照、UTC/区间与旧证据保护已修复；固定窗口 shadow、当前参数容量 B/C Mock、合成库恢复与 N03 只读预检已取证。通知暂不开启；原件清理待具体清单确认、整机演练待排期。负责人授权本任务承担方案 A 审查和实施，先修正提案并隔离开发；不自动授权生产迁移。源码/测试/证据/门禁及剩余工作统一见 [本轮记录](docs/2026-09-22-core-closeout.md)，全量与最终 CI 按该记录实时核实。
