# 项目上下文

## 基本信息

- 项目名称：QQ 群多模态智能管理机器人
- 文档初始化日期：2026-09-03
- 当前阶段（2026-09-08）：生产大群主通道为NapCat/OneBot，官方机器人为可选/测试通道。**T-305与T-306已验收关闭；T-306主审修复提交 `444b368` 的远程CI运行 `34219858155` 在Ubuntu、Windows和干净运行时三项全绿。** 当前进入T-303/W1 Windows隔离群影子验证；T-307前OneBot真实管理动作始终禁用。
- 首批规模：1–10 个 QQ 群，日总消息量预计不超过 10,000 条
- 时区：`Asia/Shanghai`
- 项目负责人职责：确定需求、规则和最终验收；具体搭建任务可以由其他 Agent 按 `NEXT_TASKS.md` 分工执行

## 项目背景

本项目用于 24×7 监督多个 QQ 群，识别并处理群成员发布的垃圾广告、引流、诈骗及其他群规禁止内容。审核对象不仅包括文字，还包括图片、二维码、GIF、表情包、视频、语音、文件、转发记录和分享卡片。

项目优先目标是减少垃圾信息暴露时间，同时尽量避免误伤正常成员。撤回和禁言可以在高置信条件下自动执行，但踢人属于高影响操作，必须经过人工批准。

## 目标用户和核心目标

### 目标用户

- QQ 群主和被授权的人工管理员。
- 负责审核规则维护、案件复核、人工踢人或批准 NapCat 踢人的运营人员。

### 核心目标

1. 实时接收和标准化目标群中的全部可用消息类型。
2. 使用规则、行为特征、OCR、二维码、ASR、视频抽帧及多模态模型识别垃圾信息。
3. 对高置信违规按固定阶梯自动撤回和禁言。
4. 合并同一成员两次违规的完整证据，生成编号明确的人工审核案件。
5. 允许人工选择在 QQ 客户端手动踢人，或明确授权 NapCat 执行踢人。
6. 定期汇总群聊情况、异常事件、系统动作、误判和运行状态。
7. 保存可审计证据，使每次判断和动作都能追溯到规则、模型版本和人工审批人。
8. 允许管理员在后台编辑、测试、发布和回滚规则，发布后无需改代码或重启。
9. 将人工确认的违规、正常和误判沉淀为反馈样本，生成候选规则并经人工批准后更新。

## 已确定的业务规则

### 违规范围

- 广告、引流、联系方式、二维码、外部群邀请。
- 诈骗、钓鱼、仿冒客服及高风险交易。
- 色情、赌博、毒品及其他违禁交易。
- 刷屏、重复推广、恶意骚扰和人身攻击。
- 泄露手机号、身份证、住址等个人隐私。
- 群主后续提供的自定义禁区、黑名单和例外。

### 置信度原则

- 中等风险：只记录并进入人工复核，不自动撤回、禁言或累计有效违规。
- 高置信违规必须满足以下任一条件：
  - 命中明确黑名单、已确认恶意域名、二维码、相似广告图或可确定的刷屏规则；
  - 主模型置信度不低于 0.90，并由独立复核模型确认。
- 媒体无法下载或分析失败本身不能作为处罚理由。
- 群主、管理员和显式白名单成员只记录和告警，不自动处罚。

### 处罚阶梯

默认按“同一成员、同一个群、30 天”累计有效违规：

1. 第一次高置信违规：保存证据、撤回消息、禁言 1 小时、群内警告一次。
2. 第二次高置信违规：保存证据、撤回消息、禁言 24 小时、不再公开警告、合并两次证据并生成待审核案件。
3. 第二次违规后绝不自动踢人，也不自动拒绝再次入群。
4. 人工审核可以选择保留、标记误判、在 QQ 客户端手动踢人，或批准 NapCat 踢人。
5. 标记误判后，应撤销对应有效违规记录，并将样本用于规则和模型复盘。

## 最终架构决策

```text
QQ群消息
   ↓
NapCatQQ / OneBot 11（目标大群主通道）
   ↓ Adapter验证与标准化
传输中立消息契约
   ├─→ 审核服务：规则、行为、多模态识别、独立复核
   ├─→ 案件中心：违规累计、证据、编号、审批、报告
   └─→ 动作编排：撤回、禁言、首次警告 → NapCat Adapter
                            ↓ 第二次违规案件
                       人工审核批准
                     ┌──────┴──────┐
               QQ客户端手动踢人   NapCat持令牌踢人
                     └──────┬──────┘
                         审计与结果

QQ官方机器人 → 官方Adapter → 同一中立契约
（仅用于实际可接入的测试/小群或未来企业认证场景）
```

### 组件职责

- **NapCat/OneBot**：目标大群的主消息与动作Adapter；接收普通QQ管理员账号可见的群消息，并在审核编排授权后执行撤回、禁言和首次警告。它不负责理解内容。
- **QQ 官方机器人**：保留的可选Adapter；只用于机器人实际可进入的群，不再是目标大群上线前置。
- **消息标准化层**：在Adapter seam后把OneBot和官方事件转成统一消息封装，显式保留 `provider`、外部群/成员/消息ID、时间、消息段、附件和上下文。
- **审核服务**：执行规则检测、行为检测、OCR、二维码解析、图片语义理解、GIF/视频抽帧、ASR 和模型复核。
- **案件与证据中心**：保存违规历史、证据、处罚结果、审核批次、候选编号和人工结论。
- **管理后台**：查看证据、规则、映射状态、白名单、待审核案件、日报和审计日志；提供暂停处罚和紧急降级开关。
- **人工 QQ 客户端**：默认且风险最低的踢人出口，也是 NapCat 不可用或身份不明确时的回退出口。
- **OpenClaw/Hermes**：首版不接入核心链路；后期只可作为自然语言报告或管理入口，不能拥有未经审批的处罚权限。
- **Codex**：用于开发、测试、审查和最终验收，不作为 24×7 运行时。

## 运行模式

### 影子模式（默认）

- NapCat接收目标群消息并运行完整审核、证据、案件和报告链路。
- 只保存拟执行的撤回、禁言和警告意图，外部动作调用数必须为0。
- 用于首次NapCat实机接入、每次大版本/规则/模型变更和异常恢复后观察。

### 实时管理模式（分群启用）

- NapCat负责接收，审核核心产生确定性动作意图，NapCat动作Adapter在幂等、急停、角色保护和断线保护通过后执行撤回、禁言和首次警告。
- 踢人默认仍在QQ客户端人工执行；只有T-304通过后，才允许人工批准的NapCat踢人。
- NapCat断线、QQ安全验证、动作连续失败或状态未知时，立即停止真实动作并转人工监管。

### 可选混合模式

- 官方机器人可继续服务它能进入的测试/小群，NapCat服务目标大群。
- 每个群必须显式选择一个消息入口和一个管理动作出口，不允许两个Adapter对同一条消息同时执行处罚。

## 平台身份和数据迁移

- 统一契约的目标形式是 `provider + external_group_id + external_user_id + external_message_id`；对NapCat即OneBot数字 `group_id`/`user_id`，对官方Adapter则是 `group_openid`/`member_openid`。
- 当前数据库与领域类仍使用 `group_openid`/`member_openid`，T-305必须采用expand/migrate/contract迁移：先增加中立字段与兼容读写，再回填和切换读取，最后才在独立任务评估是否删除旧列。
- **T-305 expand 阶段已实现并完成主审整改（D-020）**：中立契约位于 `app/core/contracts.py`，包含通用消息段；中立列、双写、回填与混合版本读取已就位；路由以 `message_provider + external_group_id` 联合定位，未配置OneBot默认拒绝；违规累计和动作幂等按provider隔离。迁移 `b8e2f6a4c1d9` 与纠正迁移 `d4f7a9c2e601` 可完整升降级。旧列删除留待contract阶段独立评审。
- NapCat主通道群直接使用数字ID，不需要OpenID映射才能撤回、禁言或踢人。
- 只有同一群配置两种Adapter时才需要跨通道映射；不得仅凭昵称、头像、群名片或模糊时间建立可执行映射。

## 人工审核与踢人

审核服务为每个批次生成不可变编号，例如 `R20260903-01`，批次内候选成员使用 `1、2、3……` 编号。编号只在当前批次、当前群有效。

支持的审核表达包括：

```text
审核 R20260903-01：踢 2,5,8
审核 R20260903-01：保留 1,3,13，其他踢
审核 R20260903-01：全部保留
审核 R20260903-01：全部踢
审核 R20260903-01：重新查看 3
```

自然语言不能直接触发动作。系统必须先转换成结构化名单、显示预览并要求一次性确认码。超过 10 人的批量踢出增加一次确认；该数字是项目初始安全阈值，不代表 QQ 官方风控阈值。

双出口状态必须互斥：

```text
PENDING_REVIEW
  ├─→ APPROVED_MANUAL → MANUAL_PENDING → KICKED / CANCELLED
  ├─→ APPROVED_NAPCAT → CONFIRM_PENDING → EXECUTING → KICKED / FAILED
  ├─→ KEEP → CLOSED
  └─→ FALSE_POSITIVE → STRIKE_REVOKED → CLOSED
```

同一案件不得同时由人工客户端和NapCat成功执行。NapCat失败后只转人工队列，不无限重试。

## 多模态处理要求

- 图片：OCR、二维码解析、感知哈希、视觉语义识别。
- GIF和动态表情：最多抽取8个代表帧后综合判断。
- 视频：场景切分后最多取16帧，执行OCR并转写音轨；同步处理上限为10分钟或200MB。
- 语音：优先使用可用的官方转写，否则调用云端ASR。
- 文件：只提取PDF、Office和纯文本；不执行文件。压缩包和可执行文件只做元数据和安全扫描。
- 转发和卡片：递归提取文字、链接、预览图和来源信息。

## AI辅助与规则学习

- 审核采用分层级联：确定性规则、行为、OCR、二维码、哈希和ASR优先；不确定内容再进入文本或视觉AI。
- 文本与视觉使用独立任务接口，但可由同一远程供应商提供；首个候选是兼容OpenAI API的小米MiMo。
- 远程AI按群显式启用，默认关闭；只上传最小脱敏内容，完整视频、完整文档和无关身份信息不上传。
- 单个模型只提供软证据；未配置独立复核模型时，AI结果不直接触发自动处罚。
- 动态规则以数据库中的不可变版本为权威，支持草稿、历史回放、发布、热更新和回滚；每条判定保存规则版本。
- 管理员确认的违规、正常和误判是权威反馈；未知原因撤回保持待标注，未撤回消息保持未标注。
- AI可以聚类样本并生成结构化候选规则，但不能自动发布、提高处罚等级或执行动作。
- 首版以后台人工标注为权威反馈入口。NapCat主通道的撤回通知在T-306/T-303验证后可作为待标注事件，但不得自动视为违规真值。
- 完整设计见 `docs/superpowers/specs/2026-09-06-ai-rule-learning-design.md`。

### 当前实现状态（2026-09-06）

- 动态规则已落库：全局/群级规则集、草稿、发布、回滚、审计、5秒缓存热更新和每条判定规则版本追踪已实现。
- 远程AI已实现基础适配：`TextModerator`/`VisionModerator` 接口、OpenAI-compatible/MiMo类适配器、按群显式启用、脱敏文本、严格JSON响应、缓存、限流/预算和降级记录；真实MiMo模型ID、区域、计费和效果尚未由负责人本地验证。
- AI结果当前只作为软证据：单模型高置信只会把本地 `allow` 升为 `record_only`，不会自动撤回、禁言或踢人。
- 反馈学习已实现基础闭环：后台可对影子判定标注反馈，本地挖掘候选规则并复制为动态规则草稿；候选仍需人工在规则页发布才会生效。
- 官方动作编排已实现但默认关闭：`ACTION_MODE=SHADOW` 不调用外部处罚接口；`OFFICIAL` 只支持官方撤回、禁言和首次警告，不包含踢人，且启用前需生产配置和实机验收。

## 数据、报告和运维

### 数据

- 首版使用 SQLite WAL 保存消息元数据、案件、判断、动作、动作意图、AI缓存/用量、反馈、候选规则、规则版本和审计记录。
- 原始媒体与数据库分离并加密存储。
- 原始消息和媒体默认保存30天；判断、动作、证据索引和统计保存180天后清理。
- 云模型只接收完成审核所必需的最小数据。
- 远程AI属于负责人新授权的可选能力，是否启用、允许的数据类别、供应商区域和每日预算按群配置；关闭后不影响本地审核。
- 密钥必须使用环境变量或系统凭据存储，不写入代码、Markdown、测试数据或日志。

### 报告

- 严重事件实时通知。
- 每天09:00和21:00生成日报。
- 每周一09:10生成周报。
- 报告至少包含消息量、异常数、违规类别、撤回/禁言结果、待审核案件、误判、NapCat状态、队列积压和模型费用。

### 运维降级

- 监控NapCat/OneBot连接、QQ登录与安全验证状态、每群最后事件时间、事件延迟、媒体队列、模型失败率、动作失败、磁盘空间和报告状态。
- 模型不可用、规则损坏、证据不完整或动作结果不确定时，降级为只记录和告警。
- NapCat发生安全验证、异常掉线、连续失败或实际不再接收群消息时，目标大群的自动监督已中断。系统必须停止全部NapCat真实动作、立即告警并转人工监管，不得声称“官方审核仍在运行”来掩盖目标群已失去消息入口。
- 官方机器人若在其他可接入群运行，只能继续服务那些群，不能作为NapCat大群的自动备接管通道。

### Windows 24×7运行与恢复

- 正式整机测试环境已确认为项目负责人提供的Windows 10专业版电脑（2026-09-04，决策D-011）；首次测试时记录实际build号、架构、补丁、电源和网络条件。
- 该电脑分阶段投入：W0基础兼容已通过；R-104与T-305/T-306后验证NapCat影子接收；核心契约与审核链路通过后验证影子端到端；T-307通过后验证撤回、禁言和警告；T-404后验证锁屏、重启、断网、更新和恢复；最后才分阶段上线。
- GitHub Actions的Windows任务用于代码兼容性门禁，不能替代真实QQ登录、NapCat、Windows Service或24×7整机测试。
- 屏幕可以关闭并锁屏，但专用机不得进入睡眠或休眠；睡眠期间无法保证实时接收消息。
- 审核服务、媒体任务、报告调度和健康监控必须在Windows启动后自动拉起，不依赖人工打开终端。
- 启动顺序固定为数据与迁移检查、审核服务、任务执行器、QQ客户端与NapCat/OneBot、健康监控；可选官方Adapter不得阻塞NapCat主链路启动。
- 系统更新采用受控维护窗口：暂停新处罚、排空可安全任务、备份数据库、重启、健康检查通过后再恢复自动动作。
- 重启前结果不确定的撤回、禁言、警告或踢人动作不得自动重放，必须先查询状态或转人工复核。
- BIOS应支持来电自动开机；条件允许时使用UPS并定期进行备份恢复演练。
- NapCat需要扫码、设备确认或安全验证时只告警并等待人工，不得反复自动登录。
- 详细要求和验收步骤见 `docs/windows-operations.md`，实施任务见 `NEXT_TASKS.md` 的 `T-404`。

### Windows测试阶段门槛

1. **W0 基础兼容性**：R-101全部阻塞项关闭，Linux/Windows CI真实通过后，在空白Windows电脑完成干净安装、迁移、配置拒绝、端口、pytest、mypy和ruff验证；不配置QQ或模型密钥。
2. **W1 NapCat影子接收**：R-104、T-305、T-306通过后，在隔离群验证OneBot连接、全量事件、数字身份、媒体下载、断线重连和去重；所有外部处罚调用数为0。
3. **W2 核心影子端到端**：W1及核心审核、案件、人工工作流、多模态和报告任务通过后，验证NapCat消息到规则/AI、两次违规、案件、报告和人工QQ客户端踢人闭环，仍不执行自动动作。
4. **W3 NapCat管理动作**：T-307通过后，在隔离群从影子开始，分别验证撤回、禁言3,600秒/86,400秒、首次警告、保护角色、幂等、超时和 `UNKNOWN` 人工复核。T-304踢人始终保持单独批准门槛。
5. **W4 无人值守恢复**：T-404实现后，验证Windows Service、QQ/NapCat会话依赖、锁屏、重启、断网、更新、备份恢复、未知动作复核和外部告警。
6. **W5 最终分阶段验收**：按T-403先在目标群影子观察，再按群依次开放NapCat撤回、禁言和警告；踢人默认人工，连续稳定性观察后才完成整体验收。

## 初步技术栈

- 开发语言：Python 3.12。
- 后端与管理接口：FastAPI。
- 数据访问：SQLAlchemy；数据库迁移方案在脚手架阶段确定并记录到 `DECISIONS.md`。
- 首版数据库：SQLite WAL；规模或并发超过首版假设时再评估 PostgreSQL。
- 主接入与大群动作：NapCatQQ + OneBot 11。
- 可选接入与动作：QQ 官方机器人 API（仅限实际可接入的群）。
- 多媒体：OCR、二维码解析、ASR、视觉模型、FFmpeg。
- 部署：生产大群使用固定Windows 10专业版专用机、没有重要资产的专用管理员QQ和NapCat；审核服务、NapCat与QQ会话健康监控必须共同达到24×7验收。
- 管理后台：首版以服务端页面为主，避免过早引入独立前端工程。

## 验收指标

- 自动撤回和禁言的离线精确率不低于99%。
- 广告、诈骗等核心类别召回率不低于90%；综合多模态召回率不低于85%。
- P95审核延迟：文字3秒以内，图片/GIF 15秒以内，普通视频90秒以内。
- 影子模式下NapCat撤回、禁言、警告和踢人外部调用次数必须为0。
- 重复OneBot事件、WebSocket重连和进程恢复不得导致重复撤回、禁言、警告或踢人。
- 系统必须区分“进程存活”和“持续收到目标群事件”；NapCat或QQ会话失效后不得误报为 `ready`。
- 没有人工有效审批令牌时，NapCat踢人接口调用次数必须为0。
- 目标群/成员数字ID缺失、角色未复核或跨通道映射不可靠时，NapCat踢人接口调用次数必须为0。
- 同一案件两个踢人出口同时成功的次数必须为0。
- NapCat出口测试集和7天分级期内误踢人数必须为0。
- 每个主要违规类别至少准备20个违规正例和30个相似合法反例。
- 图片、GIF、视频、语音、转发卡片和二维码分别至少准备10个违规与合法样本。

## 风险边界和非目标

- NapCat属于非官方接入，不能承诺不触发QQ风控，也不设计规避检测、设备指纹伪装、代理轮换或随机模拟真人。
- 首版不追求完全无人值守踢人。
- 首版不让通用Agent、提示词或群消息直接拥有处罚权限。
- 首版不进行无人监督在线训练，不把管理员撤回直接等同于违规，不把未撤回直接等同于正常。
- 首版不执行群成员上传的文件，不把管理接口暴露到公网。
- 任何改变“人工批准才能踢人”“目标大群主通道使用NapCat/OneBot”“单群只有一个激活动作出口”或“身份不明确必须人工处理”的需求，都属于架构变更，必须先更新 `DECISIONS.md` 和本文件。

## 外部参考

- [QQ全量群消息事件](https://bot.q.qq.com/wiki/develop/api-v2/autogen/event/group_message_create.html)
- [QQ撤回群消息](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_groups_group_openid_messages_message_id.delete.html)
- [QQ群成员禁言](https://bot.q.qq.com/wiki/develop/api-v2/autogen/api/v2_groups_group_openid_restrict_chat_setting.post.html)
- [NapCatQQ](https://github.com/NapNeko/NapCatQQ)
- [OneBot 11 API](https://github.com/botuniverse/onebot-11/blob/master/api/public.md)
