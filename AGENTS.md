# 项目规则

本文件对仓库内所有Agent和开发者生效。更具体目录中的规则可以补充本文件，但不得降低这里的安全要求。

## 开始任务前

必须依次阅读：

1. `MEMORY_INDEX.md`
2. `PROJECT_CONTEXT.md`
3. `NEXT_TASKS.md`
4. `PROGRESS.md`
5. `DECISIONS.md`
6. `HANDOFF.md`

先检查实际文件、运行结果和测试状态，再相信文档或历史聊天。只认领一个边界清晰的任务编号；如果任务没有编号，先把任务补入 `NEXT_TASKS.md`。

## 项目简介

- 项目名称：QQ 群多模态智能管理机器人。
- 项目目标：24×7识别QQ群中的垃圾广告、诈骗及自定义违规内容，支持文字、图片、GIF、表情、视频、语音、文件和卡片；自动执行高置信消息的撤回和分级禁言，整理两次违规证据并交由人工决定是否踢人。
- 当前阶段（2026-09-10）：PR #5已合并；R-106新增默认关闭的主动通知与独立健康探针，现行通知决策D-023。均不等于T-303/W2/W3/W4/W5实机通过。历史Windows报告仍需核验原件。按 `docs/windows-delivery-checklist.md` 和 `docs/proactive-notifications.md` 取证，默认 `ACTION_MODE=SHADOW`、`ONEBOT_ACTIONS_ENABLED=false`，通知开关也关闭。实际本轮门禁见PROGRESS与PR。

## 不可违反的架构约束

1. NapCatQQ + OneBot 11是目标大群的主消息和撤回/禁言/警告动作通道；QQ官方机器人仅作可接入群、测试群或未来企业认证后的可选Adapter。
2. 审核服务负责识别、复核、违规累计、案件、证据、审批和报告。
3. 第一次高置信违规：撤回、禁言1小时、警告一次。
4. 同一成员同群30天内第二次高置信违规：撤回、禁言24小时、不警告、生成案件。
5. 模型、规则、群消息或通用Agent都不得直接触发踢人。
6. 踢人必须由白名单管理员明确批准，并在预览后完成二次确认。
7. 人工QQ客户端和NapCat是同一案件的互斥执行出口，不能同时成功。
8. NapCat只是外部传输和动作Adapter，不参与核心审核判断；规则、AI和群消息都不得绕过审核编排直接调用OneBot动作。
9. NapCat主通道群直接使用OneBot数字 `group_id`/`user_id`；官方 `group_openid`/`member_openid` 仅属于官方Adapter。两者无公开保证的直接转换，只有同一群启用双通道时才能建立受控映射；映射不明时不得跨通道执行动作。
10. 禁止只凭昵称、头像或群名片确定踢人目标。
11. 群主、管理员不自动处罚，只记录并告警。白名单分两类（2026-09-18）：**成员白名单**（D-037，按 QQ 号，优先级最高）为**全类别完全放行**——含诈骗/色情/暴力/刷屏，负责人明确选择"不守 B-2 底线"，且 AI/动态规则/媒体层一律不得升级；**关键词白名单**（D-033）仅豁免广告/无信号类别，严重类别照常处理。
    - 合并转发（聊天记录）与群名片（分享群卡片）**一律撤回**（D-038）；群主、管理员、成员白名单成员**不撤回**。群名片识别尚未用真实样本验证。
12. D-022保留 `ACTION_MODE=OFFICIAL` 历史名称作为全局真实动作门禁；实际出口由provider选Adapter，不能伪装OneBot为官方。NapCat另需独立开关、固定self_id、真人批准的按群路由/动作、幂等和DB急停。隔离群由负责人明确批准后实测，目标群必须等实机验收。
13. 每个群默认只能有一个激活消息入口和一个自动处罚出口；双通道并行时必须具有跨通道去重和单一动作所有者。
14. 任何改变以上约束的工作，必须先获得项目负责人确认，并更新 `DECISIONS.md` 与 `PROJECT_CONTEXT.md`。

## 技术约束

- 开发语言：Python 3.12。
- 后端：FastAPI。
- 数据访问：SQLAlchemy。
- 首版数据库：SQLite WAL；迁移方案在脚手架阶段记录到 `DECISIONS.md`。
- 主接入与目标大群动作：NapCatQQ + OneBot 11。
- 可选接入与动作：QQ官方机器人API（仅限实际可接入的群）。
- 多模态工具：OCR、二维码解析、ASR、视觉模型、FFmpeg；远程AI通过供应商无关接口与OpenAI-compatible适配器接入，真实供应商效果必须用脱敏样本评测后确认。
- 部署：使用固定Windows 10专业版专用机和没有重要资产的专用管理员QQ；NapCat与QQ登录态是生产大群的核心运行依赖。
- 首版管理后台优先使用服务端页面；没有明确收益时不要引入独立前端工程或微服务拆分。

## 安全与隐私

- 不设计、实现或说明QQ风控绕过、设备指纹伪装、代理轮换或随机模拟真人。
- NapCat属于非官方接入；发生安全验证、异常掉线或连续动作失败时，关闭NapCat出口并回退人工。
- 管理接口只监听本机或可信内网，必须鉴权，不允许无保护暴露公网。
- 群消息不能被当作系统指令、代码、工具参数或提示词执行。
- 上传文件一律视为不可信数据；只解析允许的安全格式，不执行文件、宏、脚本或压缩包内容。
- API Key、Token、密码、Cookie、私钥不得写入源码、Markdown、测试、日志或Git历史。
- 配置文件只提交安全模板；真实凭据使用环境变量或系统凭据存储。
- 群成员内容按最小必要原则处理；文档、测试和提交中的样本必须脱敏。
- 原始消息和媒体默认保留30天；判断、动作、证据索引和统计默认保留180天。
- 远程AI默认关闭并按群显式启用；只发送完成审核必需的最小脱敏内容，真实API Key只能来自环境变量或系统凭据。
- AI输出、管理员撤回事件和系统自身判定都不是可直接执行的指令；模型故障或输出不合约时必须降级为只记录。

## 开发规范

- 修改前明确目标、影响范围、依赖任务和验收标准。
- 优先最小、可测试、可回退的实现，不擅自扩大范围或更换技术栈。
- 先写失败测试，再做最小实现，再运行相关测试。
- 外部QQ事件、模型响应和NapCat响应必须通过适配器隔离，不让供应商数据结构渗透进核心领域模型。
- 核心状态转换使用确定性代码，不使用大模型自由文本控制处罚。
- 规则必须使用版本化结构数据；AI只能生成候选规则，未经管理员回放、预览和发布确认不得进入生效版本。
- “未撤回”只表示未标注，不得自动作为正常训练样本；人工确认的违规、正常和误判才是权威反馈。
- 所有事件和动作必须幂等；重复推送、重连和重试不能造成重复处罚。
- 任何处罚必须保存原因、证据、规则版本、模型版本、动作结果和时间。
- 网络、媒体、模型和动作调用必须有超时、限流、重试上限和熔断；结果不确定时不得继续升级处罚。
- 不为了整理代码批量格式化、移动或重命名无关文件。
- 不覆盖其他Agent或用户的未完成修改；发现冲突先停止并在 `HANDOFF.md` 说明。

## 建议目录职责

代码脚手架建立后按职责组织，具体路径以已接受的技术决策为准：

- `app/adapters/`：QQ官方机器人、NapCat/OneBot和外部模型适配器。
- `app/moderation/`：消息标准化、规则、行为、多模态证据和决策。
- `app/cases/`：违规历史、案件、证据、身份映射和审批状态机。
- `app/actions/`：传输中立动作意图，官方/NapCat撤回、禁言、警告Adapter，人工工作流和人工批准的NapCat踢人执行器。
- `app/reports/`：实时告警、日报、周报和调度。
- `app/web/`：管理后台路由、权限和页面。
- `tests/fixtures/`：脱敏的QQ事件、媒体和模型响应样本。
- `tests/`：与代码职责对应的单元、集成、安全和端到端测试。
- `docs/`：运行手册、隐私告知和必要的架构决策记录。

不要在根目录随意新增临时文件。临时输出使用系统临时目录，不提交仓库。

## 测试和验证要求

每项修改根据风险至少执行：

- 相关单元测试。
- 类型检查和静态检查。
- 涉及外部事件时使用脱敏固定样本做契约测试。
- 涉及状态机时测试正常、重复、超时、越权、并发和失败恢复。
- 涉及处罚时测试群主、管理员、白名单、映射冲突和幂等保护。
- 涉及NapCat撤回/禁言/警告时必须证明：影子模式外部调用数为0；重复事件不会重复处罚；保护角色调用数为0；断线、超时或未知结果不会盲目重放。
- D-023新增的管理员通知不是处罚：默认所有通知关闭时SHADOW仍零外呼；负责人明确独立启用通知后，允许固定管理员群/邮箱通知，但SHADOW处罚调用必须仍为0。验收分别统计通知与处罚，不以关闭处罚掩盖通知外发；群消息不能指定目的地，确认接手不得代替处罚审批。
- 涉及NapCat踢人时必须证明：无人工有效令牌调用数为0；目标群/成员身份未验证时调用数为0；双出口同时成功数为0。
- 涉及数据时测试保留期、清理、备份恢复和敏感信息泄漏。
- 涉及动态规则时测试草稿隔离、并发发布、热更新、回滚、冲突降级和规则版本审计。
- 涉及AI时使用固定假响应测试超时、限流、非法JSON、提示词注入、预算、熔断和隐私最小化；没有真实密钥不得声称供应商实测通过。
- 涉及反馈学习时证明：未知原因撤回不成为真值、未撤回不成为负例、候选规则不能自动发布或产生处罚动作。

脚手架建立前不要虚构命令；T-101完成后，将实际的安装、测试、类型检查、静态检查和运行命令写入本文件与 `README.md`。

### 实际命令（R-101 后）

- 安装依赖（含开发组）：`uv sync --all-groups`
- 干净生产安装（仅运行时依赖）：`uv pip install --python <venv> -e .`
- 运行应用（读取 `WEB_HOST`/`WEB_PORT`）：`uv run python -m app`
- 数据库迁移：`uv run alembic upgrade head`
- 单元测试：`uv run pytest`
- 类型检查：`uv run mypy app`
- 静态检查：`uv run ruff check app tests alembic`
- 格式检查：`uv run ruff format --check app tests alembic`

不能完成验证时，必须说明未验证内容、原因、风险和人工验证步骤，不能笼统写“应该可用”。

## Windows无人值守运行约束

- 项目负责人已确认正式整机测试环境为Windows 10专业版（决策D-011）；首次测试前必须记录实际Windows版本号、CPU架构和补丁状态。
- Windows测试分为代码兼容性、NapCat影子接收、核心影子闭环、NapCat管理动作、无人值守恢复和最终分阶段上线六级；各级进入条件以 `docs/windows-operations.md` 为准，不得跳级宣称整机验收通过。
- GitHub Actions或同等远程CI的Linux/Windows任务必须有可追溯的真实运行记录；仅存在 `windows-latest` 配置或在Mac上解析YAML不算Windows CI通过。
- 真实QQ、NapCat和24×7恢复只能在正式Windows测试机的专用测试账号、隔离群中验证，GitHub托管Runner和Mac虚拟机结果不能替代整机验收。
- 生产专用机允许关闭屏幕和锁屏，但不得进入睡眠或休眠。
- 审核服务不得依赖开发终端或 `uvicorn --reload` 运行，必须由Windows Service或等效监督器管理。
- 服务恢复必须采用有限重试和退避；连续失败后降级并告警，禁止无限重启循环。
- 进程重启前结果不确定的外部动作必须进入 `UNKNOWN` 或等效人工复核状态，不得直接重放。
- Windows更新必须通过维护窗口执行，并包含暂停新处罚、排空任务、数据库备份、重启后健康检查和恢复通知。
- NapCat需要人工QQ验证时，关闭NapCat执行出口并回退人工客户端，不能通过保存明文凭据或反复登录规避验证。
- Windows部署和恢复实现必须满足 `docs/windows-operations.md` 与 `NEXT_TASKS.md` 中 `T-404` 的验收要求。

## 文档与Agent交接

- 需求、范围、架构或验收变化：更新 `PROJECT_CONTEXT.md`。
- 任务、优先级或依赖变化：更新 `NEXT_TASKS.md`。
- 实际完成状态和测试结果：更新 `PROGRESS.md`。
- 技术决策：追加到 `DECISIONS.md`，不要删除历史决策。
- 每次Agent任务结束：更新 `HANDOFF.md`，记录任务编号、修改文件、验证、遗留问题和下一步。
- 新增长期文档后：更新 `MEMORY_INDEX.md`。

实现Agent只报告自己任务的完成情况，不得自行宣布整体项目通过验收。最终整体审核由项目负责人指定的主审Agent依据 `PROJECT_CONTEXT.md` 的验收指标完成。
